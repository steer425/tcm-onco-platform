# 版本更新紀錄（tcm_backend）

## v1.39.4 — 2026-08-27（冗餘發現標記與倍率排序：讓畫面看得出證據的真實強度）

### 起因：人參的凋亡與 p53「兩項發現」其實是同一項

驗收時發現這三條都達 FDR 顯著：

| # | 通路 | 命中基因 | 倍率 | q 值 |
|---|---|---|---|---|
| 20 | Apoptosis - multiple species | BAX、BCL2、CASP3、CASP8、CASP9、MAPK8 | 19.2 | 4.91e-6 |
| 42 | PI3K-Akt signaling | AKT1、BCL2、CASP9…（13 個） | 3.68 | 1.97e-4 |
| 47 | p53 signaling | BAX、BCL2、CASP3、CASP8、CASP9、**CDK1** | 8.19 | 3.32e-4 |

**凋亡與 p53 的六個命中基因裡有五個完全相同。** KEGG 的通路定義本來就大量重疊，
所以「凋亡顯著、p53 也顯著、癌症總覽也顯著」看起來像三方交叉印證，
**實際上是同一個觀察被計入三條通路定義**。並列在報告裡會嚴重高估證據強度。

真正的訊號是一句話：人參的活性成分靶點集中在粒線體／caspase 凋亡級聯。
那是**一個**發現，不是三個。

### 一、冗餘標記

一條通路的命中基因如果有 80% 以上已經出現在更高名次的通路裡，標記為「重複」，
並顯示它跟哪一條重疊幾個。門檻 0.8 的理由就是上面的 5/6 = 0.83 這個實例：
訂太低會把真正互補的通路也標成冗餘，訂太高則抓不到這種明顯重複。

每一列另外顯示「新增 N / M 個基因」，命中基因裡**新出現的加粗、已出現過的變灰**——
一眼看得出這一列到底帶來多少新資訊。

摘要行新增「**獨立發現數**」：39 條顯著通路裡有幾條是獨立的基因組合。
**顯著通路數本身是被通路定義的重疊灌水的，獨立發現數才是「有幾項發現」的答案。**

冗餘一律依統計證據順序（p 值）判定，**跟使用者選哪種排序無關**——
誰是誰的重複是資料的事實，不該因為畫面怎麼排而改變。

### 二、依富集倍率排序

p 值天生偏袒基因數多的大通路：k 大則檢定力高。人參的 Apoptosis 是 **19.2 倍**、
全表最高之一，卻排第 20，落在一堆倍率 3–7 倍的通路後面；而 hsa05200
「Pathways in cancer」有 533 個基因、倍率只有 7.4 卻排第 2——那是一條包山包海的
總覽通路，特異性遠低於「凋亡」。

**依 p 值排序會系統性地把最具特異性的發現往後推。** 這不是 bug，是 ORA 的固有特性，
但看排名的人不會知道。所以新增排序切換，並在依倍率排序時仍顯示原本的 p 名次
（`p_rank`），才知道它統計上排第幾。

### 測試

增至 112 項斷言。冗餘標記用純函式驗證（子集、部分重疊剛好低於門檻、完全不重疊三種），
排序切換驗證「換排序不影響 p 值本身」與「p_rank 與依 p 排序時的名次一致」。

### 檔案

- 修改：`app/pathways.py`（`annotate_redundancy`、`enrich` 加 `sort`）、
  `app/routers/pathways.py`、`frontend/pathways.html`、`frontend/js/pathways.js`、
  `tests/test_pathways.py`、`rules.md`

## v1.39.3 — 2026-08-27（修正：顯示篩選會縮小樣本，造成循環論證與倍率灌水）

### 這是一個會灌水但完全沒有徵兆的錯誤

勾選「只看癌症相關通路」之後，人參的樣本數從 **92 掉到 60**，
而倍率飆到 13.75、20.27、17.95，q 值到 1e-15——看起來像重大發現。

原因是 `cancer_only` 與 `exclude_noncancer_disease` 被套在**建立索引之前**，
所以 `n` 變成了「只在癌症通路裡有註解的基因數」。

**那等於先把樣本限縮到癌症通路的基因，再問「這些基因是不是集中在癌症通路」
——循環論證。** 而且母體 `N` 還是全部 9423 個註解基因，分子分母不同源，
倍率自然被拉高。

以 hsa05222（Small cell lung cancer）為例：
錯誤算法 (12/60)/(93/9423) = 20.27；正確算法 (12/92)/(93/9423) = 13.2。

### 正確的做法

`cancer_only` 與 `exclude_noncancer_disease` 是**要測哪些通路**的篩選，
不是**樣本怎麼算**的篩選：

- `n`（樣本）與 `N`（母體）一律用該來源**全部**的通路註解計算，與顯示篩選無關
- 篩選只決定哪些通路進入檢定與 BH 校正（BH 的 m 變小是合理的）
- **一條通路的 p 值，不會因為旁邊多顯示或少顯示幾條而改變**

新增回歸斷言直接檢查這件事：同一條通路在「全部」與「只看癌症」兩種條件下，
p 值與倍率必須完全相同；n 與 N 也必須相同。

### 結果列加上序號

原本要確認 PI3K-Akt 排第幾只能自己數。新增 `#` 欄，達 FDR q<0.05 的序號顯示為綠色，
一眼就看得出顯著的分界線在第幾名。序號由後端 `rank` 欄位給，
不是前端依畫面順序編的——分頁或截斷時才不會對不上。

### 檔案

- 修改：`app/pathways.py`（`enrich` 重寫）、`frontend/pathways.html`、
  `frontend/js/pathways.js`、`tests/test_pathways.py`（增至 98 項斷言）、`rules.md`

## v1.39.2 — 2026-08-27（藥材改為模糊搜尋；補上六個頁面漏掉的 OpenCC）

### 藥材選取：下拉選單換成輸入即時搜尋

502 種藥材塞在一個 `<select>` 裡，實際使用時捲不完也找不到——
使用者要選「人參」得在兩百多筆之間目視搜尋。改成文字輸入框 + 即時模糊比對。

- 比對範圍：中文名、英文學名、拼音、以及 child 名稱
- 排序：完全相同 → 開頭命中 → 出現在中間；同分再依靶點數
- 鍵盤可用：↑↓ 選擇、Enter 確認（沒按方向鍵時 Enter 取第一筆）、Esc 關閉
- 命中的字串會標黃

**繁簡字形一併處理**：藥材名稱在資料庫裡是簡體（`猫爪草`、`鱼腥草`），
使用者用繁體打「貓爪草」直接比對會找不到——**那不是使用者輸錯，是我們的比對沒處理字形差異**。
沿用全站語系機制已經在用的 OpenCC 做正規化，兩邊都轉成簡體再比。
轉換失敗時退回原文比對，不讓搜尋整個壞掉。

選項用 `mousedown` 而不是 `click` 綁定：input 的 `blur` 會先把清單關掉，
用 `click` 的話永遠觸發不到。

另外把清單裡的靶點數標成「靶點 196（**未篩選**）」——那個數字來自
`TcmspHerb.target_count` 這個預先計算欄位，沒有經過 OB／DL 篩選，
跟分析實際使用的靶點數不同。兩個數字擺在同一畫面上不標清楚會讓人以為算錯了。

### 修正：六個頁面漏掉 OpenCC，簡體語系實際上完全沒作用

`site-lang.js` 的簡體轉換靠 CDN 載入的 `opencc-js`，但下列頁面的 `<head>`
從來沒有引入它——切到簡體時 `initOpenCCForSiteLang()` 取不到 `window.OpenCC`，
**畫面靜默維持繁體，不會報錯**：

- `pathways.html`、`target-mapping.html`（v1.38.0／v1.39.0 新增的兩頁，我漏了）
- `darkgene-stats.html`、`dna-test-data.html`、`gencc-diseases.html`、
  `patient-dark-gene-ranking.html`（既有頁面，一併補上）

`rules.md` §5-2 明訂「任何新頁面都要檢查四種語系」，這兩頁我沒做到。
六頁全部補上 script 標籤。`oauth_callback.html` 是純轉址頁，沒有可見文字，不需要。

**這類缺漏沒有任何徵兆**——不會報錯、不會空白，只是語系切了沒反應。
所以檢查方式不能只靠肉眼看畫面，要確認 script 標籤真的在。

### 檔案

- 修改：`frontend/pathways.html`、`frontend/js/pathways.js`、
  `frontend/target-mapping.html`、`frontend/darkgene-stats.html`、
  `frontend/dna-test-data.html`、`frontend/gencc-diseases.html`、
  `frontend/patient-dark-gene-ranking.html`、`frontend/js/i18n-dict.js`
  （en／ko 各補 2 詞條，維持對等：各 684 條）、`rules.md`

## v1.39.1 — 2026-08-27（補上方法學缺的兩道篩選：活性成分與疾病類雜訊通路）

### 起因：人參的第一次分析結果不對勁

v1.39.0 上線後跑人參（目標一指定的首個驗證案例），前七名長這樣：

| 排名 | 通路 | q 值 |
|---|---|---|
| 1 | Neuroactive ligand signaling | 3.21e-6 |
| 2 | C-type lectin receptor signaling | 2.36e-5 |
| 3 | **Lipid and atherosclerosis** | 2.36e-5 |
| 4 | cGMP-PKG signaling | 1.86e-4 |
| 5 | **Tuberculosis** | 1.86e-4 |
| 6 | **Fluid shear stress and atherosclerosis** | 1.86e-4 |

**沒有任何癌症通路進前七名，卻有結核病。** 統計本身沒錯
（fold enrichment 逐項手算對得上，FDR 也正確），錯的是餵進去的東西。
281 條通路裡有 80 條達 q<0.05——**28% 的通路都「顯著」，這個比例本身就是警訊**。

### 問題一：沒有套用 OB／DL 活性成分篩選

`docs/2026_goals.md` 目標一 Step 1 白紙黑字寫著「TCMSP 活性成分篩選
（OB ≥ 30%、DL ≥ 0.18）」，而且記載人參篩選後是「22 個活性成分、
約 109 個不重複靶點基因」。但 v1.39.0 的 `targets_for_herb()` 直接抓了
藥材的**全部**成分——所以畫面顯示 196 個靶點、139 個註解基因。

TCMSP 收錄的是這個藥材裡「偵測得到」的所有化合物，絕大多數口服吸收率極低
或根本不具類藥性，到不了體內任何靶點。不篩就等於宣稱人參的每一個化合物
都在體內作用——**那不是保守，是錯的**。而且 n 從 109 灌水到 139，
在超幾何檢定裡直接推高每一條通路的顯著性。

修正：`targets_for_herb()` 預設套用 OB／DL 篩選，門檻存在系統設定可調
（預設就是 TCMSP 原始論文的建議值，動了就跟文獻方法學對不起來）。
畫面上顯示篩選前後的成分數與靶點數，**篩掉多少一定要看得見**——
篩太兇導致樣本剩沒幾個，結論一樣不可信。

ADME 缺值（`ob`／`dl` 是空字串或 `NA`）一律排除並**單獨計數**：
當成通過會讓篩選形同虛設，當成 0 則無法跟「真的很低」區分。

### 問題二：KEGG 疾病類通路是通用發炎凋亡基因的大雜燴

結核病、動脈粥狀硬化、B 型肝炎這類 KEGG 通路，每一條都塞了
AKT1／BCL2／CASP3/8/9／IL1B／IL6／MAPK／NFKB／RELA／TP53。
**任何**含有這組通用機器的靶點集合都會對它們「顯著富集」。
人參的「結核病 q=1.86e-4」就是這樣來的——不代表人參治結核病，
只代表人參的靶點包含通用發炎凋亡核心。這是 ORA 眾所周知的假象。

修正：新增 `exclude_noncancer_disease`（**預設開啟**）。
但不能把整個 Human Diseases 排掉——癌症通路正好也在這一類底下。
所以依 KEGG 自己的 BRITE B 層子分類做白名單：
`Cancer: overview`、`Cancer: specific types`、`Drug resistance: antineoplastic`
保留，其餘 Human Diseases 排除。判斷依 KEGG 官方分類，不是猜關鍵字。
分類抓不到（category 為空）時一律保留——寧可留著也不要靜默刪資料。

### 這兩道篩選預設都開啟，但可以關

關掉之前要知道自己在關什麼，所以畫面上把兩者的理由直接寫在控制項旁邊，
而且關掉 ADME 篩選時會顯示紅字警告「這個結果只適合當對照，不適合當成正式分析」。

### 測試

`tests/test_pathways.py` 增至 90 項斷言。新增涵蓋：
OB／DL 四種邊界（通過、剛好在門檻上、OB 不足、DL 不足）、
ADME 缺值的兩種寫法（空字串與 `NA`）、門檻可覆寫與設定值壞掉時退回預設、
KEGG 疾病分類白名單的七種分類、以及「只由未通過 ADME 的成分連到的靶點
不該出現在結果裡」。

### 檔案

- 修改：`app/pathways.py`（`active_ingredients`／`adme_thresholds`／`_num`／
  `is_noncancer_disease_pathway`，`targets_for_herb` 改回傳 `(靶點, 成分統計)`）、
  `app/routers/pathways.py`、`frontend/pathways.html`、`frontend/js/pathways.js`、
  `tests/test_pathways.py`、`rules.md`

## v1.39.0 — 2026-08-27（KEGG／Reactome 通路富集分析，分析鏈路第四站）

### 這一版做了什麼

分析鏈路「中藥 → 成分 → 靶點 → 疾病 → 腫瘤細胞 → 研究報告」走到第四站。
從「這個藥材打到哪些蛋白」進到「這些蛋白集中在哪些生物過程」——
前者是一份名單，後者才開始像是一個機轉假說。

### 為什麼這一步比預期便宜

v1.38.0 解析 UniProt 時，順手把每個靶點的 `kegg_id`（`hsa:367`）與
`reactome_ids`（`R-HSA-383280`）一起存進映射表了，**1751 個靶點裡 1258 個帶 KEGG 交叉引用**。
通路分析最貴的那一步（把蛋白對應到通路識別碼）等於已經完成。

兩個來源缺的東西不一樣，這點很容易搞混：

| 來源 | UniProt 交叉引用給的是什麼 | 還缺什麼 |
|---|---|---|
| KEGG | **基因** ID（`hsa:367`） | 基因→通路對應表，要另外抓 |
| Reactome | **通路** ID（`R-HSA-383280`） | 只缺通路名稱與背景基因數 |

`hsa:367` 是 KEGG **基因** ID，`hsa04915` 才是 KEGG **通路** ID——弄混就整個對不起來。
三支來源檔全部是**整包下載**，不是每個靶點打一次：3 次請求 vs 1751 次請求的差別。

### 統計方法

超幾何分布的過度代表分析（ORA），加 Benjamini–Hochberg 多重檢定校正。

**為什麼一定要做 FDR 校正**：一次對數千條通路做檢定，就算全部都是雜訊，
在 p<0.05 這條線下也會有一百多條「顯著」。不校正等於在製造假發現，
而這些結果是要寫進研究報告的。頁面上以 q 值為準呈現，不是 p 值。

**刻意不引入 scipy**：超幾何機率用 `math.lgamma` 在對數空間求和實作。
Render free plan 的建置時間與體積都很緊，為了一個函式裝整包 scipy 不划算。
`math.comb` 再取 log 也不行——`C(20000, 300)` 算得出來，取 log 前就先溢位成 inf。

**兩種背景集，畫面上一定會標示現在用的是哪一種**：

- `genome`（預設，學界慣例）：母體是資料庫收錄的全部人類基因。
  缺點是 TCMSP 的靶點本來就偏向可成藥的蛋白，訊號傳導與代謝類通路會被系統性高估。
- `tcmsp`：母體改成全部已標準化的中藥靶點基因，消掉上面那個偏差，適合比較不同藥材；
  但不是慣例，寫進論文要額外說明。

母體選什麼，結論就跟著變。看到 p 值卻不知道母體是什麼，那個數字沒有意義，
所以頁面上把母體的意義與各自的缺點直接寫在結果上方，不是藏在說明文件裡。

### 背景基因數必須來自外部資料庫

富集檢定的分母（該通路在全人類基因裡有幾個成員）一律取自 KEGG／Reactome 本身。
**拿我們自己手上的靶點當母體是循環論證**，會讓每條通路看起來都顯著。

### 資料表（第 43、44 張）

- `pathways` — 通路主檔。三語系名稱獨立儲存（同 GenCC 的作法，專有名詞不用字形轉換）；
  重新同步會覆蓋名稱與基因數（那是外部資料庫的事實），但**絕不覆蓋人補的翻譯**
  ——跟 `feature_config` 那條「不要覆蓋管理者調整過的欄位」是同一個道理（v1.32.4 的教訓）。
- `target_pathways` — 靶點↔通路關聯，含 `via_symbol`／`via_accession` 記錄「這條連結是怎麼來的」。
  富集結果要寫進研究報告，每一條連結都必須追得回源頭。
  整張表是推導資料，同步時整批砍掉重建而不是增量更新。

### 後台頁面（F1-5「通路富集分析」）

`frontend/pathways.html`：覆蓋率、分來源同步（KEGG／Reactome 各自可重跑，
互不拖累——Reactome 那個檔數十 MB，抓失敗不該把已成功的 KEGG 一起弄壞）、
藥材富集結果表（命中基因、倍率、p 值、q 值，可只看癌症相關通路）。

### 測試

新增 `tests/test_pathways.py`（62 項斷言，`python -m tests.test_pathways`），不連外網。

**重點在數學不在 CRUD**：程式跑不跑得動很容易看出來，但 p 值算錯不會有任何徵兆
——它一樣會給你一個介於 0 和 1 之間、看起來很合理的數字。所以超幾何檢定用
`math.comb` + `Fraction` 做**精確有理數運算**當對照組（跟被測的 lgamma 對數求和
是完全不同的算法，不是把同一條公式抄兩遍），七組參數最大誤差 7.8e-13；
BH 校正對照教科書範例。另外驗證「在基因符號空間去重」——TCMSP 有多個靶點指向
同一基因（同工異構物、次單元），照 tar_id 數會把同一個基因重複計入，命中數灌水、p 值失真。

### 執行方式

部署後到 F1-5 頁面按「同步 KEGG」與「同步 Reactome」各一次。
需要對外連線到 `rest.kegg.jp` 與 `reactome.org`，**必須在 Render 上操作**
（Cowork 沙箱與本機 VM 都連不到，跟 UniProt 一樣）。
`pathways`／`target_pathways` 是全新資料表，`create_all()` 會自動建，不需要跑 `migrate_schema`。

### 順帶記錄：v1.38.0 的上線驗收結果

1751 個靶點解析完成，**1265 個標準化（72.2%）、連線失敗 0 筆**；
癌症基因比對率 **2.6%（32/1245）→ 18.0%（224/1245）**，7 倍。
交付時測試的 UniProt HTTP 層是 fixture 取代的、明確標注「未端對端驗證」，
實跑零失敗代表 fixture 猜的回應結構與真實一致，那個風險沒有實現。

18% 已接近真實上限，不是還有 bug：TCMSP 的靶點偏向傳統可成藥的蛋白，
而 OncoKB 癌症基因裡有大量 undruggable 的轉錄因子與染色質修飾因子。
剩下 486 筆（165 待確認 + 321 查無結果）暫緩——它們現在仍走名稱字詞比對當退路，
並不是隱形的，解析完提升的是精確度與召回率兩邊，淨增加約幾十個基因的量級。

### 上版流程補強（rules.md §9）

打包 v1.39.0 時漏掉了 `.gitignore` 與 `.github/workflows/daily-news.yml`——
`update_local_folder.bat` 用的是 `robocopy /MIR`（鏡射），**zip 少放什麼，
使用者的資料夾就會少什麼，而且不會有任何錯誤訊息**。每日新聞的排程就掛在那個
workflow 檔上。`zip -r` 預設不收 `.` 開頭的項目，這是根本原因。

因此 rules.md §9 新增三條強制規定：

1. **每次交付都必須明講「要不要跑 `update_local_folder.bat` / `git_push.bat`」**——
   不需要跑的時候更要講。只在需要時提醒、不需要時沉默，使用者無從分辨
   「不用跑」和「你忘了講」，而這兩件事的後果差很多。
2. 打包 zip 的必含清單（明列 `.gitignore`、`.github`、`.claude`），
   且打包後必須用 `unzip -l` 驗一次頂層項目，不能憑指令寫對就當作收進去了。
3. 交付 zip 時要一併列出 `/MIR` 會刪掉、需要使用者先備份的檔案
   （`.env`、`data_import/` 等）。

### 檔案

- 新增：`app/pathways.py`、`app/routers/pathways.py`、`frontend/pathways.html`、
  `frontend/js/pathways.js`、`tests/test_pathways.py`
- 修改：`app/models.py`（`Pathway`／`TargetPathway`，共 44 張表）、`app/feature_config.py`（F1-5）、
  `app/main.py`、`app/migrate_schema.py`（註記）、`frontend/js/i18n-dict.js`
  （en／ko 各補 14 詞條，兩語系維持對等：各 682 條）、
  `rules.md`／`README.md`／`app/tcmsp_uniprot.py`／`frontend/target-mapping.html`（補上 18.0% 驗收數字）

## v1.38.0 — 2026-08-26（TCMSP 靶點以 UniProt 標準化；癌症基因比對從 2.6% 解封）

### 這一版在修的是什麼

TCMSP 的靶點欄位存的是**蛋白質全名**（`Androgen receptor`、
`RAC-alpha serine/threonine-protein kinase`），1751 個靶點裡有 1573 個帶空白，
只有 4 個長得像基因符號。但暗黑基因（OncoKB）、GenCC 疾病關聯、新聞實體連結
全都是拿**基因符號**（`AR`、`AKT1`）去比對靶點名稱字串。

`Androgen receptor` 這串字裡永遠不會出現 `AR`，所以這三個功能一直在比對失敗。
實測：1245 個癌症基因只比中 32 個（**2.6%**），而 AR、AKT1、TP53、APC、EGFR、
ESR1、PTGS2 全都在 TCMSP 裡、全都比不到。最麻煩的是**失真是靜默的**——
畫面不會說「比對失敗」，只會顯示「沒有關聯」，看起來就像研究結論。

### 做法：獨立的映射表，不動原始資料

新增資料表 `tcmsp_target_uniprot`（第 42 張），把每個靶點名稱送去 UniProt
查出 accession、基因符號、同義詞、KEGG／Reactome 交叉引用。
刻意不在 `tcmsp_targets` 加欄位：重新匯入 TCMSP 時映射不會被洗掉，
映射自己的來源與可信度也才有地方放；而且一個靶點可能合法對到多筆
（`Estrogen receptor` → ESR1／ESR2），那個取捨應該由人來做。

解析分三級，**信心不足一律不自動採用**：

| 級別 | 查詢方式 | 信心 | 結果 |
| --- | --- | --- | --- |
| 1 | 精確蛋白名稱 | 1.0 | 單一命中 → 自動採用 |
| 2 | 拆詞 AND 查詢 | 0.8 | 單一命中 → 自動採用 |
| 3 | 全文查詢 | 0.5 | **一律進待確認**，就算只回一筆 |

第 3 級不自動採用是刻意的：全文命中代表「條目內文提到這個字串」，
不代表就是同一個蛋白。這是科研平台，一個錯誤的基因映射會污染下游每一次分析，
而且錯誤會被當成結論——寧可留白等人確認，也不要給一個看起來合理的錯答案。

正規化**不剝除結尾數字**：`Prostaglandin G/H synthase 1` 與 `⋯ synthase 2`
是 PTGS1 與 PTGS2 兩個不同的基因，把尾數當雜訊剝掉會讓兩者互相污染，
而那正是這整件事最不能出的錯（測試裡有這條回歸斷言）。

### 比對邏輯集中到一支：`app/target_index.py`

原本有**八個地方**各自就地寫一段「把 target_name 拆成英數字詞」的索引
（暗黑基因統計、逐基因統計、藥材反查、基因詳情、關聯圖、GenCC 疾病比對與反查、
離線重算）。邏輯一樣所以答案一樣，沒出過事；但標準化上線後就不是這樣了——
只要有一個地方沒改到，就會出現「查詢站說這個基因比對到靶點，統計欄位說沒有」
這種自相矛盾且極難追查的畫面。八處全部改呼叫 `app/target_index.py`。

比對規則（兩層，順序有意義）：

1. 已標準化的靶點（`status` 為 `auto`／`confirmed`）→ UniProt 基因符號＋**同義詞**精確比對。
2. 尚未標準化的靶點 → 維持原本的名稱字詞比對。

第 2 層是刻意保留的退路，讓解析可以分批進行、統計不會中途歸零；
但**已標準化的靶點不再走第 2 層**，否則精確符號之外又多出一堆字詞誤中。
`pending`／`rejected`／`unresolved` 一律不採用——待人工確認的東西還不是結論。

### 後台頁面（F1-4「靶點標準化」）

`frontend/target-mapping.html`：覆蓋率、分批解析（可重複按到歸零）、
人工審核（候選點選帶入／人工輸入／否決需填備註）、以基因符號反查靶點。
已人工確認或否決的紀錄，重跑批次時不會被覆蓋。

### 測試

新增 `tests/test_target_mapping.py`（68 項斷言，`python -m tests.test_target_mapping`）。
不連外網：UniProt 的 HTTP 那一層以固定 fixture 取代，fixture 內容照抄真實回應結構。
回歸斷言用實際查證過的已知真值：AR→P10275、TP53→P04637、AKT1→P31749、
PTGS1／PTGS2 不互相污染、同義詞 NR3C4 也要對得到 AR。

### 執行方式（重要）

解析需要對外連線到 `rest.uniprot.org`。**請在 Render 上操作後台頁面**，
或在本機帶 `DATABASE_URL` 執行；Cowork 沙箱與本機 VM 都連不到（代理回 403），
在那裡跑會整批回報「連線失敗」。

### 檔案

- 新增：`app/target_index.py`、`app/tcmsp_uniprot.py`、`app/routers/target_mapping.py`、
  `frontend/target-mapping.html`、`frontend/js/target-mapping.js`、`tests/test_target_mapping.py`
- 修改：`app/models.py`（新增 `TcmspTargetUniprot`）、`app/recompute_stats.py`、
  `app/routers/dark_genes.py`、`app/routers/gencc_diseases.py`、`app/feature_config.py`、
  `app/main.py`、`frontend/js/i18n-dict.js`（en／ko 各補 19 個詞條，兩語系維持對等）

## v1.37.0 — 2026-08-23（新增新聞來源只要貼網址；主題過濾關鍵字改為後台可維護）

### 新增來源：只輸入網址

後台「公告 / 新聞管理 → 新聞來源與關鍵字」可以直接貼一個網址，
系統自己判斷這個站有沒有 RSS、站名叫什麼、是中文還是英文站（`app/news/discover.py`）：

1. 網址本身就是 feed → 直接用
2. `<head>` 有 `rel=alternate` 宣告的 feed，或常見路徑（`/rss`、`/feed`、`/atom.xml`…）→ 逐一試，能解析出項目就用
3. 都沒有 → 通用爬蟲規則挑文章連結

**一定會實際抓一次，抓不到就不讓存。** 這是刻意的：允許存一個抓不到東西的來源，
問題會延到隔天清晨 4:00 的排程才爆，那時沒有人在看，而且錯誤會混在其他來源的統計裡很難發現。
另有「試抓看看」按鈕，可以先看結果再決定要不要加。

連結挑選規則刻意保守——排除導覽列字眼、太短的錨點文字、外站連結、純錨點；
網址帶數字（文章編號或日期）的優先。**寧可少抓也不要把導覽列和廣告當成新聞**。

抓不到而且頁面連結很少、script 很多時，會直接指出「內容多半是瀏覽器執行 JS 之後才產生的，
伺服器端抓不到這種頁面」——像騰訊新聞這類前端渲染的入口網站就屬於這種。

### 證據層級：新增 `general_news`

管理者自訂的來源預設是**一般新聞、最低權重 0.30**，卡片上照實標示。
勾「政府／學術官方來源」才升級為國家衛生政策層級、權重 0.55。

刻意獨立一個層級而不是硬塞進既有的十個之一：卡片上會顯示層級標籤，
把商業新聞標成「癌症臨床與實證」會直接誤導讀者，那比不標更糟。

⚠️ PostgreSQL 的 Enum 是原生型別，新增成員必須 `ALTER TYPE`，而且**不能包在一般交易區塊裡**。
`migrate_schema.py` 依照既有 `oauthprovider` 的先例，放在 `isolation_level="AUTOCOMMIT"`
的獨立區塊執行，不能塞進 `ALTER_STATEMENTS` 迴圈（那裡是 `engine.begin()` 交易）。

### 主題過濾關鍵字改為後台可維護

新增一張 `news_keywords`（第 41 張表）。`sources.py` 的 `CANCER_TERMS`／`TCM_TERMS`
變成**預設值**，第一次執行時種進資料庫，之後以資料庫為準。

**為什麼是這兩組**：新增一個來源之後最常要調的就是它們——來源換了，用語就換了
（中文站講「中成藥」「驗方」，英文站講 botanical、phytotherapy）。這不該每次都要改程式重新部署。

癌別對照、介入方式、臨床前判定詞刻意留在程式裡：那幾組直接影響證據層級判斷與排序哲學，
改壞了會讓臨床前研究排到人體試驗前面，不是日常維護該碰的東西。

**擋下「把某一組全部停用」**：一篇文章要同時命中兩組才算相關，任一組全空時
`relevance()` 會判定所有文章都不相關，當天收集全軍覆沒而且**完全沒有錯誤訊息**——
那是最難查的一種壞法，所以在 API 層直接擋，並說明原因。
`get_terms()` 也在資料庫意外為空時退回程式碼預設值，同樣是為了不讓它靜靜地全滅。

### 自訂來源不會被同步洗掉

`news_sources` 新增 `is_custom` 欄位。`sync_sources()` 只同步程式碼定義的來源，
看到 `is_custom` 一律不碰；刪除也只允許刪自訂的——官方來源刪掉之後下次部署又會被建回來，
變成「刪了又出現」的鬼打牆。已經收過文章的來源也不給刪（文章有外鍵指向來源），改用停用。

自訂來源只存在資料庫裡，收集流程吃的卻是 `SourceDef`，因此新增
`source_def_from_row()` 轉換，並用 `register_runtime_sources()` 註冊進 `SOURCE_BY_SLUG`——
`service.py` 用的是沒有預設值的 `SOURCE_BY_SLUG[slug]`，沒註冊會直接 KeyError 讓整次收集掛掉。

### 已測試驗證

`python -m tests.test_news_e2e` — **164 項斷言全過**（v1.36.4 的 129 項 + 本次 35 項）。

涵蓋：關鍵字正規化（去空白轉小寫）、重複／空白／不合法分組被擋、停用後不進入比對、
**不允許把某一組全部停用**、一般使用者 403；網址自動補 https、RSS/Atom 辨識、
HTML 不被誤判、連結挑選排除導覽列/外站/錨點、slug 產生規則、
`sync_sources` 不動自訂來源、資料庫列轉 SourceDef、官方來源不能刪、稽核留痕。

### 部署注意事項

- **要跑 `python -m app.migrate_schema`**：`news_sources` 新增了 `is_custom` 欄位，
  而且 PostgreSQL 的 `newsevidencelevel` enum 要補 `general_news` 成員。
  這是新聞模組上線以來第一次真的需要跑遷移腳本
- `news_keywords` 是全新資料表，`create_all()` 會自動建；預設詞在第一次開啟後台關鍵字頁
  或第一次收集時自動種入

## v1.36.4 — 2026-08-23（改用 *-latest 別名；把 Google 指名的替代模型拉到訊息最前面）

### 同一天連續踩兩次模型退役

- `gemini-2.0-flash` → 「This model is no longer available.」
- `gemini-2.5-flash` → 「This model is no longer available **to new users**.」

第二次特別值得記：`gemini-2.5-flash` **確實出現在 ListModels 的清單裡**，
但呼叫時仍被拒。也就是說 **ListModels 列的是「存在的」模型，不是「這個帳號叫得動的」模型**。
v1.36.1 把那份清單標成「可用的模型」是過度承諾，實際害使用者照著選了一個一樣不能用的。

### 修正三件事

1. **預設改成 `gemini-flash-latest`**（別名，自動跟著目前的 flash 模型走）。
   固定版本號一定會退役，別名不會。要釘版本的人用 `NEWS_GEMINI_MODEL` 指定即可。
2. **清單標題改成「此金鑰列出的模型（未必每個都能用）」**，不再假裝那是保證可用清單。
3. **把 Google 指名的替代模型抓出來放到訊息最前面**：退役訊息裡本來就寫著
   「Please update your code to use models/X」，那是整段裡最有用的一句，
   卻埋在中間容易被忽略。現在會顯示「→ Google 建議改用：X（設到 NEWS_GEMINI_MODEL）」。

**外部服務給的錯誤訊息裡常常已經包含正確答案。與其自己另外湊一份可能過時的建議清單，
不如把對方指名的那一句解析出來、放到最顯眼的位置。**

### 已測試驗證

`python -m tests.test_news_e2e` — **129 項斷言全過**，新增一項守住「能從錯誤訊息抽出建議的替代模型」。

## v1.36.3 — 2026-08-23（預設 Gemini 模型改為 gemini-2.5-flash，2.0-flash 已退役）

實測結果：`gemini-2.0-flash` 已經下架，檢測回

> HTTP 404 — This model models/gemini-2.0-flash is no longer available.

而 v1.36.1 加的「列出這把金鑰實際可用的模型」剛好發揮作用，直接把
`gemini-2.5-flash`、`gemini-flash-latest` 等名稱列出來，管理者不必等改程式，
設一個 `NEWS_GEMINI_MODEL` 就能繼續用。

預設值同步改成 `gemini-2.5-flash`，避免之後重新部署或有人接手時又踩同一個坑。

**外部模型名稱一定會退役，所以三件事缺一不可**：預設值要跟得上、要能用環境變數覆寫、
出錯時要把可用選項直接列給使用者。第三點才是真正讓人不必求救的那一項。

## v1.36.2 — 2026-08-23（診斷端點一定要很快回來，不能把請求拖到瀏覽器斷線）

### 問題

按「檢測 API 金鑰」得到的是瀏覽器層級的 **`Failed to fetch`**——請求根本沒回到前端。

原因是檢測端點的逾時設成 45 秒（外加列出模型的 20 秒）。對方若不回應，
整個 HTTP 請求會被拖到瀏覽器或反向代理先斷線，使用者只會看到一句
「Failed to fetch」，**比拿到「12 秒內無回應」更沒有資訊**。

**診斷用的端點，最重要的性質是「無論如何都要很快給出答案」**，
而不是「盡量等到對方回應」。可用的金鑰，這種 `max_output_tokens=8` 的請求 1~2 秒就回來了；
等到 45 秒的情況，答案已經確定是「不通」，多等只是把錯誤變得更難懂。

### 修正

- 檢測請求逾時 45 秒 → **12 秒**；列出模型 20 秒 → 8 秒
- 逾時訊息帶出實際端點網址，並說明「可用的金鑰通常 1~2 秒就回應，
  逾時代表端點不對或這台伺服器對外連該網域被擋」，附上可覆寫的環境變數名稱
- 前端遇到 `Failed to fetch`／`NetworkError` 時附上下一步：
  後端可能正在部署或從休眠喚醒，先開一次 `/docs` 叫醒它再試

### 已測試驗證

`python -m tests.test_news_e2e` — **128 項斷言全過**，新增一項守住
「診斷端點逾時設定夠短」，避免日後有人為了『多等一下說不定會通』又把它調回去。

## v1.36.1 — 2026-08-23（修正 Gemini 端點：改用官方的 generateContent，並列出金鑰實際可用的模型）

### 問題

v1.36.0 的 Gemini 端點寫成 `/v1beta/interactions`，實測結果是**連線逾時**
（`連不到 gemini API：The read operation timed out`）。

我當初是照著抓下來的文件摘要實作的，但那份摘要描述的介面與官方 REST 參考不符。
**沒有金鑰就沒辦法端對端驗證，這是那一版明講過的風險，結果真的踩到了。**

### 修正

改用官方 REST 參考的路徑與形狀：

| 項目 | 內容 |
|---|---|
| 端點 | `POST /v1beta/models/{model}:generateContent` |
| 金鑰 | `x-goog-api-key` 標頭（**不用 `?key=`**，查詢字串會被存取日誌與代理記錄下來，與排程密鑰同一條規則）|
| 請求 | `systemInstruction` + `contents[].parts[].text` + `generationConfig`（`responseMimeType: application/json`）|
| 回應 | `candidates[].content.parts[].text`（也相容 `output_text`）|

預設模型改為 `gemini-2.0-flash`。端點可用 `NEWS_GEMINI_BASE`／`NEWS_GEMINI_URL` 覆寫，
模型可用 `NEWS_GEMINI_MODEL` 覆寫。

### 檢測會列出可用的模型

模型名稱會改版，光說「找不到模型」對管理者沒有幫助。Gemini 遇到 400／404 時，
檢測會用同一把金鑰呼叫 `GET /v1beta/models`，把**這把金鑰實際可用、且支援
generateContent 的模型名稱**列出來，並告訴他用 `NEWS_GEMINI_MODEL` 指定。

逾時訊息也改成會帶出實際打的端點網址，並提示可用環境變數覆寫——
上一版只說「連不到」，管理者無從判斷是端點不對還是網路不通。

### 已測試驗證

`python -m tests.test_news_e2e` — **127 項斷言全過**（v1.36.0 的 120 項 + 本次 7 項）。

新增：Gemini 用 generateContent 端點、模型名稱組進網址、金鑰走標頭不走查詢字串、
請求含 systemInstruction/contents、解析 candidates 形狀、也接 output_text、
Anthropic 解析不受影響。

## v1.36.0 — 2026-08-23（新聞 AI 摘要改可用 Gemini 免費層，不再綁單一供應商）

### 為什麼

原本綁死 Anthropic API，而它要儲值才能用。查過之後：

- **DeepL API Free** 官方已不再開放新用戶申請，這條路斷了
- **免費機器翻譯**（MyMemory 等）額度小、醫學與中藥名詞品質差
- **Gemini API 免費層**仍在，**不需要信用卡**，而且能做完整的「摘要＋翻譯＋證據層級判斷」，
  跟原本的功能一模一樣

要注意這個模組要的不是單純翻譯：提示詞要求反映證據層級、禁止療效宣稱、不得杜撰數據。
純機器翻譯只會把英文摘要逐字翻過來，不會壓成 200 字，也不會做證據判斷。

### 新增 `app/news/ai_client.py`

摘要與翻譯這件事跟「找誰做」無關，但兩家的端點、標頭、請求與回應結構完全不同。
把差異收在這一層，`summarizer.py`（收集時的中文標題／摘要）與 `short_summary.py`
（多語系簡短摘要）就只處理提示詞與解析，不必各自散落一份 HTTP 細節。

| 供應商 | 端點 | 金鑰標頭 | 結果位置 |
|---|---|---|---|
| Gemini | `/v1beta/interactions` | `x-goog-api-key` | `output_text`（也相容舊的 `candidates[].content.parts[]`）|
| Anthropic | `/v1/messages` | `x-api-key` | `content[].text` |

**選擇順序**：`NEWS_AI_PROVIDER` 可強制指定（gemini／anthropic／none），
否則自動偵測——`GEMINI_API_KEY` 優先，其次 `ANTHROPIC_API_KEY`，都沒有就走降級。

端點與模型名稱都能用環境變數覆寫（`NEWS_GEMINI_URL`／`NEWS_GEMINI_MODEL` 等）。
這是刻意的：兩家 API 都還在演進，真的改版時可以先改環境變數擋著，不必等重新部署程式。

### 連帶好處：中文標題也會有了

`summarizer.py` 一起換到共用層，所以**收集時產的 `title_zh` 也會跟著出現**——
先前只有摘要能靠「重產」補救，標題永遠是英文。設好金鑰後新收的文章標題就是中文。

### 環境變數

`render.yaml` 補上 `GEMINI_API_KEY` 與 `NEWS_AI_PROVIDER` 的宣告（`sync: false`，值在後台填）。
後台警示文案也改成兩家並列，並標明 Gemini 免費層不需信用卡。

### 已測試驗證

`python -m tests.test_news_e2e` — **120 項斷言全過**（v1.35.4 的 112 項 + 本次 8 項）。

新增：只有 Anthropic 時選 anthropic、兩者都有時 Gemini 優先、檢測回報目前用哪一家、
回報讀的是哪個環境變數、Gemini 前綴檢查用 `AIza`、可強制指定供應商、可強制停用、
停用時摘要走降級。

### 尚未實測

Gemini 這條路徑是照官方文件實作的，**但我手上沒有 Gemini 金鑰，無法端對端實測**。
後台的「檢測 API 金鑰」會把對方的真實回應原文帶出來，第一次設定時請以它為準；
若端點格式與文件不符，可先用 `NEWS_GEMINI_URL`／`NEWS_GEMINI_MODEL` 調整。

## v1.35.4 — 2026-08-23（金鑰檢測回報長度與夾帶空白，401 才有辦法自己查）

### 為什麼

v1.35.2 的檢測按鈕能分辨 401／403／429，但遇到 401 之後就沒有下一步了——
Render 的值欄位是遮蔽的，**貼上時被截斷、或前後夾到換行，光看畫面完全看不出來**，
而這正是 401 最常見的兩個原因。

### 做法

檢測結果加上三項診斷，都不含金鑰的機密部分：

| 欄位 | 用途 |
|---|---|
| `key_length` | Anthropic 金鑰約 100 字元以上，明顯偏短就是貼上時被截斷 |
| `key_prefix` | 前 14 字元。`sk-ant-api03-` 是廠商公開前綴，不是機密；開頭不對代表貼錯服務的金鑰 |
| `had_surrounding_whitespace` | 值前後有沒有夾帶空白或換行 |

401 時這些會直接寫進訊息裡，例如「目前這把金鑰長度 41 字元，開頭 sk-ant-api03-B…
原始值前後夾帶了空白或換行…Anthropic 的金鑰約 100 字元以上，明顯偏短就是貼上時被截斷了。」

### 順帶：取用金鑰時一律 strip

`generate()` 與 `check_api_key()` 取環境變數後都會 `.strip()`，
夾帶換行不會再直接導致每次呼叫都失敗。只有空白的值等同未設定（回 `not_set` 而不是 401）。

**環境變數夾帶換行是很常見的貼上意外，程式應該容錯，但要把這件事講出來，不要默默吃掉。**

### 已測試驗證

`python -m tests.test_news_e2e` — **112 項斷言全過**（v1.35.3 的 107 項 + 本次 5 項）。

新增：回報長度供判斷截斷、前綴只有廠商公開那一段、偵測夾帶空白/換行、
夾帶時長度以去除後計算、只有空白等同未設定。

## v1.35.3 — 2026-08-23（補上 API 金鑰後，舊的降級摘要換不掉的缺口）

### 真實踩到的坑

先在沒有 `ANTHROPIC_API_KEY` 的狀態下產了一批摘要（內容是截斷原文，`is_ai=False`），
之後把金鑰補上，結果畫面上那批摘要**永遠停在英文截斷版**：

- 「回補」說沒有缺——這些文章確實已經有摘要那一列了
- 「重產」說沒有 stale——`include_stale` 只比對 `char_limit`，而字數上限根本沒改過

那批降級摘要就這樣卡住，**沒有任何介面能把它換成 AI 版本**。

### 修正

`include_stale=True` 現在涵蓋兩種，缺一不可：

1. `char_limit` 與目前設定不同（管理者改過字數上限）
2. **`is_ai=False`（產生當下沒有金鑰或呼叫失敗，是降級產物）**

`/news/admin/summaries/stats` 也把這兩種分開統計：`outdated_length` 與 `degraded`，
`stale` 改為兩者的聯集（也就是「需重產」的總數）。後台表格的「需重產」欄位會標明是哪一種，
管理者才知道按下去是在補什麼。

### 前台文案修正

原本降級摘要下方寫「後台未設定 ANTHROPIC_API_KEY」——這句話在金鑰補上之後就變成**錯的**，
但摘要仍是舊的降級版，等於系統在說謊。改成「此摘要在產生當下沒有 AI 金鑰可用，
內容是直接截斷原文。管理者可於後台『摘要與模組設定 → 重產』更新。」

**描述狀態時要講產生當下的事實，不要斷言目前的設定。**

### 已測試驗證

`python -m tests.test_news_e2e` — **107 項斷言全過**（v1.35.2 的 103 項 + 本次 4 項）。

新增：降級摘要計入 `degraded`、計入需重產、字數過期與降級分開統計、
有降級摘要時重產按鈕不會是灰的。

## v1.35.2 — 2026-08-23（後台可實際檢測 ANTHROPIC_API_KEY，不再只看環境變數存不存在）

### 問題

原本後台判斷金鑰的方式是 `os.environ.get("ANTHROPIC_API_KEY")` 有沒有值。
但**金鑰打錯、過期、額度用盡時，環境變數一樣存在**，每次生成都會失敗然後靜靜退回降級——
畫面上的症狀跟「完全沒設」一模一樣，管理者面對的是一個「摘要就是出不來」的黑箱。

### 做法

新增 `GET /news/admin/summaries/test-key`（僅管理者），實際送一個 `max_tokens=1`
的最小請求到 Anthropic API，把結果分類回報：

| 情況 | 回報 |
|---|---|
| 200 | 金鑰有效，附上目前使用的模型名稱 |
| 401 | 金鑰不正確或已撤銷 |
| 403 | 金鑰有效但沒有該模型權限 |
| 404 | 找不到模型（可用 `NEWS_SUMMARY_MODEL` 指定其他模型）|
| 429 | 額度或速率上限已達 |
| 連線失敗 | 連不到 API（附截短後的錯誤） |

後台「摘要與模組設定」分頁多一顆**「檢測 API 金鑰」**按鈕，結果直接顯示在頁面上。

**回應絕不包含金鑰本身**，只有狀態與對方訊息（截短至 300 字）。操作會寫進 `AuditLog`
（只記 ok 與 reason，不記金鑰）。

### 已測試驗證

`python -m tests.test_news_e2e` — **103 項斷言全過**（v1.35.1 的 98 項 + 本次 5 項）。

新增：未設定時回 `not_set`、**用哨兵字串當假金鑰實際跑一次**（確認被判為 `invalid`
且金鑰不會出現在回應裡）、一般使用者 403、操作有留稽核。

## v1.35.1 — 2026-08-23（原文與譯文並列顯示；中文摘要不再用英文原文冒充）

### 顯示方式：左原文、右譯文

新聞卡片的摘要區改為兩欄——左欄原文摘要、右欄目前語系的摘要。
這是科研查證用途，讀者需要能立刻對照原文用字，尤其是劑量、樣本數、統計顯著性
這類「翻譯一旦失準就會誤導」的地方；只顯示譯文等於逼使用者每次都點開原文連結確認。

窄螢幕（`max-width: 900px`）自動改成上下堆疊，否則兩欄各剩三十幾個字元寬，比不分欄更難讀。
沒有原文可對照的來源（部分公告類）維持單欄，不硬擠一個空白左欄。

為此 `_to_article()` 開始回傳 `abstract`（截到 1200 字控制回應大小，完整原文請點連結）。

### 修正：降級時不要拿別的語言冒充

v1.35.0 的中文降級是「直接截斷原文」，但多數來源是英文，截出來仍是英文，
卻被填進「中文摘要」欄位。畫面上看起來摘要有出來，使用者卻看不到任何中文，
**反而更難查出真正原因是沒設 `ANTHROPIC_API_KEY`**。

改成：

| 語系 | 無 API key 時 |
|---|---|
| 韓文 | 一律 `None` |
| 中文 | 只有原文本身含中日韓文字時才截斷沿用；英文來源回 `None` |
| 英文 | 截斷英文原文（多數來源本來就是英文，合理） |

右欄沒有內容時直接寫明「尚未產生。請確認後台已設定 ANTHROPIC_API_KEY，
並於『摘要與模組設定』按重產。」——**降級的正確樣子是誠實的空白加上一句說明，
不是看起來有東西。**

中文語系下若收集時產的 `summary_zh` 真的是中文（有 API key 時才會是），前端會先拿它墊著，
不必等隨選摘要補上；判斷方式是「含不含中日韓文字」。

### 已測試驗證

`python -m tests.test_news_e2e` — **98 項斷言全過**（v1.35.0 的 95 項 + 本次 3 項）。
新增：英文來源在無 API key 時不產生中文摘要、原文是中文時降級仍會截斷沿用、
原文是英文時中文降級回 `None`。

## v1.35.0 — 2026-08-23（每則新聞加上多語系簡短摘要，預設 200 字、長度後台可調）

### 這一版解決什麼

原本卡片上顯示的是**原文摘要直接截斷**——多數來源是英文，中文使用者掃過去只看到一段英文；
而且截斷點落在哪裡完全看運氣，常常斷在半句話。這一版改成產出**定長的簡短摘要**：
預設 200 字，管理者可在後台調成 60–600 字，並且能跟著使用者的全站語系切換。

### 語系只存三種，簡體沿用繁體

`news_article_summaries` 的 `lang` 只有 `zh-TW` / `en` / `ko`。
簡體中文刻意不另外生成——前端全站語系機制已經用 OpenCC 做繁→簡字形轉換
（`frontend/js/site-lang.js`），簡中直接讀繁中那一列即可，再花一次 API 費用產簡體是重複投資。
前端傳 `lang=cn` 時後端回的就是 `zh-TW` 那一列，回應裡的 `summary_lang` 會誠實寫著 `zh-TW`。

這跟 v1.32.5「GenCC 疾病名稱三語系獨立儲存」的決定不衝突：
那邊是**專有名詞**，字形轉換會出錯所以要獨立存；這邊是**敘述句**，OpenCC 綽綽有餘。

### 為什麼獨立一張表，而不是在 news_articles 加欄位

1. 語系會長大。之後要加日文只是多寫入一列，不用 `ALTER TABLE`。
2. 多數文章只會被少數語系讀到。獨立成表才做得到「有人真的用那個語系看，才產生那個語系」。
3. 摘要有自己的生命週期。`char_limit` 與 `model` 記在同一列，之後才判斷得出哪些該重產。

### 產生策略：隨選 + 快取，不在收集時全部做完

每天 10 篇 × 3 語系 = 30 次生成，但實際上絕大多數使用者只看一種語系，其餘等於把錢丟進水裡。
改成**有人真的用那個語系開這一頁，才產生並存起來**，冷門語系完全不花錢。

因此端點刻意拆成兩支：

| 端點 | 行為 |
|---|---|
| `GET /news/daily?lang=`、`GET /news/archive?lang=` | **只讀快取**，永遠很快 |
| `POST /news/summaries` | 產生缺的（單次上限 12 篇），前端在畫面出來之後才呼叫 |

前端因此是「新聞先顯示、摘要後補」——不會因為要等 AI 而讓整張卡片卡住。
補不到就維持原本收集時產的繁中摘要，不影響閱讀。

### 沒有 API key 時的降級：韓文寧可留白

無 `ANTHROPIC_API_KEY` 時降級為截斷原文：繁中與英文仍有內容，**但韓文回 `None`、不寫入**。
沒有翻譯能力時硬塞中文再標成韓文摘要，比留白更容易讓使用者誤判成系統壞掉。
後台設定頁會直接把這件事寫出來，而不是讓管理者自己猜為什麼韓文一直是空的。

### 後台：「摘要與模組設定」分頁

`announcements.html` 第五個分頁。可調整啟用開關與字數上限，並顯示各語系覆蓋率
（已有／尚未產生／非目前字數／AI 產生），附「回補」與「重產」按鈕。

**改字數上限不會自動重產既有摘要**——那會讓管理者一改設定就觸發大量 API 呼叫。
舊摘要會被標成 `stale`，要不要重產由管理者按按鈕決定。回補與重產都是一次最多 50 篇，
回傳還剩幾篇，按到歸零為止。

### 新增

- 資料表 `news_article_summaries`（第 40 張），可攜型別，`(article_id, lang)` 唯一
- `app/news/short_summary.py`：語系對應、提示詞、批次生成、截斷降級
- `news_settings`：`summary_enabled`（預設 true）、`summary_length`（預設 200）
- `POST /news/summaries`、`POST /news/admin/summaries/backfill`、`GET /news/admin/summaries/stats`

### 已測試驗證

`python -m tests.test_news_e2e` — **95 項斷言全過**（v1.34.0 的 63 項 + 本次新增 32 項）。

新增涵蓋：字數上下限驗證、`lang=cn` 對應到 `zh-TW`、語系參數打錯不會讓每日新聞失敗、
無 API key 時標記為非 AI、摘要長度不超過設定上限、韓文不硬塞中文、
一次最多 12 篇、一般使用者不能回補（403）、全部都有摘要時回補不重複計費、
改字數後標記 stale 且不自動重產、`include_stale` 重產且總筆數不變、關閉開關後不再產生。

### 部署注意事項

- **不需要跑 `migrate_schema`**：只新增一張全新資料表，`create_all()` 會處理
- 摘要品質取決於 `ANTHROPIC_API_KEY`（Render 環境變數，選填）。未設定不會壞，只是降級為截斷
- 既有文章不會自動補摘要，到後台「摘要與模組設定」按「回補」

## v1.34.0 — 2026-08-22（每日重點新聞區：10 個權威官方來源自動彙整，新聞可點連回藥材／疾病／靶點）

### 新功能

Dashboard 新增「📰 每日重點新聞」卡片（功能代碼 `F0-13-6`），每日清晨 4:00（台北時間）自動追蹤《中藥與腫瘤｜10 個權威官方追蹤網站》所列來源，產出當日 10 篇重點新聞。使用者可勾選「保留」存進個人收藏；管理者在「公告 / 新聞管理」頁維護。

### 10 個來源與抓取方式

| 來源 | 方式 | 備注 |
|---|---|---|
| WHO 傳統醫學 | RSS | `who.int/rss-feeds/news-english.xml`（全站 feed，一定要開主題過濾） |
| NCI | RSS | `ncinewsreleases.rss` + `cancer-currents-blog.rss`。**`/syndication/rss.xml` 會回 403，不要用** |
| NCI OCCAM | 爬蟲 | 站台無 feed |
| NCCIH | 爬蟲 | **`/news/rss` 實測 404**（網路上很多地方仍在引用這個網址），頁面也沒有 `rel=alternate` |
| PubMed | API | E-utilities（esearch → efetch），鎖定 MeSH `Medicine, Chinese Traditional` + `Drugs, Chinese Herbal` |
| ClinicalTrials.gov | API | v2，5 個介入關鍵字分次查詢後依 NCT 去重 |
| MSK 整合醫學 | 爬蟲 | `/news` 未暴露 RSS |
| MSK About Herbs | 爬蟲 | 資料庫型，一律標記為安全訊號來源 |
| 中國衛健委 / 中醫藥管理局 | 爬蟲 | 設 `allow_failure`，海外節點可能不通，失敗只記錄不中斷當日流程 |

### 排序哲學：不看熱度，看證據可信度

主題相關度 0.30、來源權威度 0.22、證據成熟度 0.20、研究設計 0.16、時效性 0.12，安全訊號另加 0.12。

- **安全訊號優先**（藥物-草藥交互作用、CYP/P-gp、肝腎毒性、警訊）——這個模組的定位是安全監測與證據查證，不是療效推薦
- **臨床前研究自動降權**：偵測到 in vitro / 細胞株 / 分子對接 / 網路藥理 / 動物模型時標記 `preclinical`，排序權重 0.40（人體證據是 1.00），且前台強制顯示「不可推論至病患層級」
- **臨床試驗登錄**一律帶出狀態／期別／收案數，前台標示「僅為登錄，不代表有效」
- 單一來源每日上限 4 篇，避免 PubMed 洗版

### 新聞接回關聯網絡（`app/news/entity_linker.py`）

這是這次改版的重點：新聞如果只是一則則獨立連結，跟平台其他部分是斷開的。比對器把新聞內文對回 TCMSP 主檔，前台顯示為可點標籤：

- 藥材 → `tcmsp_query.html?herb={herb_id}`
- 疾病 → `disease_query.html?dis={dis_id}`（**本次為 `disease_query.html` 新增 `?dis=` 參數支援**，寫法比照既有的 `?herb=`）
- 靶點 → 沒有專屬查詢站頁面，改開彈窗（`GET /news/targets/{tar_id}` 回傳關聯藥材與疾病）
- 成分 → 目前只記錄不產生連結

比對規則刻意保守，寧可漏抓也不要誤連：

- 拉丁字母以「詞」為單位做 1~4 連字詞比對，需完整詞界；長度 <5 或落在停用詞表（cancer / protein / water 等）的一律不建索引
- 中文以 2~8 字滑動視窗比對（中文沒有詞界可用）
- **基因/靶點符號大小寫敏感**——否則 `AKT1` 會誤中一堆無關字串，實測 `akt1`（小寫）正確不命中
- 成分名稱門檻更嚴（≥6 字元），避免 water / glucose 這類通用字把每篇新聞都連上

**效能**：用「詞典查表 + n-gram」而不是 29k 詞的正規表示式聯集（後者光編譯就要數秒）。實測 499 藥材 + 3,311 靶點 + 837 疾病 + 29,384 成分共 34,491 條索引：索引建立 201 ms、每篇文章比對 1.2 ms，一次收集 120 篇只多 0.15 秒。

比對到的實體中文名會併進 `search_blob`，所以後台搜「胃癌」也找得到標題是英文的 gastric cancer 研究——沒設 `ANTHROPIC_API_KEY`（無 AI 翻譯）時尤其重要。

### 沿用既有基礎建設，不重複造輪子

| 需求 | 用的是 |
|---|---|
| 取得登入者 | `app.deps.get_current_user`（JWT Bearer） |
| 管理者判定 | `app.deps.require_admin`（角色「管理者」） |
| 稽核紀錄 | `app.deps.write_audit_log` → 既有的 `AuditLog` 表，**不另建新聞專用稽核表** |
| 功能開關／權限矩陣 | `feature_config.FEATURE_CONFIG` + `/nav/menu` |

管理者操作寫進 `AuditLog` 的 action 為 `news_soft_delete` / `news_restore` / `news_pin` / `news_manual_collect` / `news_update_source` / `news_update_settings`，可在「稽核 / 登入紀錄」頁一起查詢。

### 新增 7 張資料表

`news_sources`、`news_articles`、`news_article_entities`、`news_daily_digests`、`user_news_bookmarks`、`news_collection_runs`、`news_settings`。

全部沿用全站慣例：`String` + `gen_id()` UUID 主鍵、可攜型別（`String`／`DateTime`／`Text`／`Integer`／`Boolean`／`Enum`），陣列型資料以 JSON 字串存在 `Text` 欄位。**刻意不用 PostgreSQL 專屬的 `ENUM`／`ARRAY`／`JSONB`／`INET`／`GIN`**——`database.py` 在本機沒設 `DATABASE_URL` 時會退回 SQLite，塞專屬型別進去會讓本機開發環境直接壞掉。

### 刪除舊新聞的設計

- **軟刪除為主**：前台隱藏，後台仍可查詢與還原
- **註記必填**：`note` 為必要欄位，同時寫進文章本身與 `AuditLog`
- **保護已被使用者保留的新聞**：`exclude_bookmarked` 預設 `true`，批次刪除不會動到有人勾選保留的項目，回應會回報保護了幾筆；使用者的「我的保留」仍看得到該文並標記 `article_removed`
- **可先試算**：`dry_run: true` 只回報影響筆數，不寫入、**不留稽核紀錄**

### 後台併入「公告管理」頁

`announcements.html` 改名為「公告 / 新聞管理」，用 `logs.html` 那套現成的 tabs 樣式加三個分頁：新聞管理、新聞來源健康度、收集執行紀錄。新增權限代碼 `F0-19`（`page_url` 為 `None`，不產生導覽項目，只作為權限矩陣可指派的功能項）。

爬蟲類來源的網站改版時會抓不到資料，連續失敗次數會在「新聞來源健康度」分頁累計；選擇器等設定存在 `news_sources.config`，可直接於後台修改，**不需要改程式重新部署**。

### 安全性：第三方內容一律跳脫

新聞內容來自 WHO / NCI / PubMed / 中國官方站等第三方網站，`news-widget.js` 與 `news-admin.js` 一律經 `escNews()` / `escNewsAdmin()` 跳脫後才寫入 DOM。這點跟站內的公告不同——公告是管理者自己輸入的內容，新聞不是。

### 每日 04:00 排程

**Render free plan 沒有 Cron Job**（付費方案才有），所以由外部排程呼叫 `POST /news/admin/collect/scheduled`。

**密鑰走 `Authorization: Bearer` 標頭，不放在網址查詢字串**——查詢字串會被 Render 的存取日誌、反向代理與瀏覽器歷史記錄下來，標頭不會。比對用 `secrets.compare_digest` 避免時間差攻擊。需在 Render 後台設環境變數 `NEWS_COLLECT_SECRET`，**未設定時端點回 503 停用**（而不是變成公開端點）。

新增 `.github/workflows/daily-news.yml`：每天 UTC 20:00（台北 04:00）觸發，密鑰存 GitHub repo secret（不會出現在程式碼或提示詞裡），含喚醒預熱、3 次重試（401/503 不重試）、結果摘要與失敗來源列表。另有一個 Claude 排程任務作為替代方案。⚠️ free plan 會休眠，第一次呼叫要等 30–60 秒冷啟動。

### 已測試驗證

`tests/test_news_e2e.py` — **63 項斷言全過、零警告**（執行方式：`python -m tests.test_news_e2e`，不是 pytest）。腳本開頭會自動刪掉上一輪的 `test.db`——沒有這一步的話第二次執行會沿用前一次的每日精選，在依索引取文章的地方 `IndexError`，很容易被誤判成程式壞掉。對真實 app（165 條路由全載入）、真實 JWT 登入、真實資料表執行，只有外網抓取以假資料替換。

涵蓋：功能代碼經 `seed_default_data()` 建立、未登入 401、一般使用者查後台 403、管理者 200、主題過濾、近似重複去重、安全訊號排 #1、臨床前排末位、每篇都有解讀注意事項、實體連結三種類型與連結網址正確、靶點彈窗帶出關聯藥材與疾病、收藏跨使用者隔離、中文關鍵字搜尋、刪除註記必填、試算與實際刪除筆數一致、批次刪除保護已保留文章、前台隱藏但後台查得到、還原、稽核寫進 `AuditLog` 且試算不留紀錄、來源健康度、執行紀錄、設定驗證，以及排程端點的四種拒絕路徑（未設密鑰 503／無標頭 401／錯誤密鑰 401／密鑰放查詢字串無效）與正確密鑰 200。

另外做了**正式環境升級模擬**：用升級前的 models 建一個有資料的資料庫（32 張表），換成新版 models 後跑 `create_all()`，確認 7 張新表正確建立、原有資料完好、新表可正常寫入。

### 開發過程中發現並修正的問題

1. **中文搜尋查不到英文原文的文章**——沒設 `ANTHROPIC_API_KEY` 時 `title_zh` 是空的，後台搜「胃癌」找不到那篇 gastric cancer 研究。改成把比對到的實體中文名併進 `search_blob`。
2. **SQLAlchemy 2.x 的 subquery 強制轉型警告**——`in_()` 收 `subquery()` 會發 `SAWarning: Coercing Subquery object into a select()`，未來版本會變成錯誤。全部改成直接傳 query。
3. **`older_than_days` 條件不會誤刪當天剛收的新聞**——這點寫成了測試斷言，避免日後改動時退化。

### 未解禁（embargo）內容：權限控管而不是整支端點擋掉

臨床研究新聞常有禁發期（embargo）。`news_articles` 增加 `is_embargoed` 與 `embargo_until`
兩個欄位，並新增功能代碼 **`F0-20`「中藥腫瘤新聞追蹤（未解禁內容存取）」**——
這不是頁面、也不是「能不能管理新聞」，而是「**能否提早看到尚未解禁的文章**」這一個開關。
管理者永遠看得到（沿用既有 bypass），其餘角色要在權限矩陣把它的 `can_view` 打開。

為此在 `app/deps.py` 新增 **`has_permission()`**：`require_permission()` 的非阻斷版本，
回傳布林值而不是拋 403。差別很重要——未解禁新聞的正確行為是「**這一列不給你看**」，
不是「整支 `/news/daily` 回 403」。沒有權限的使用者在查詢階段就被過濾掉那些列，
因此回應裡一律帶 `is_embargoed` 欄位是安全的：拿得到這個欄位的人本來就有權限看，
欄位只是讓他知道「這篇是提前存取」。

解禁條件是 `is_embargoed = false` **或** `embargo_until <= 現在`，
所以設了 `is_embargoed=true` 但沒填 `embargo_until` 等於無限期封存，這是刻意的。

### 唯讀模式：新聞內容進本機端快取，帳號資料不進

依 `.claude/rules/backend.md` 的 session 路由規則，`/news/daily`、`/news/archive`
同時注入兩個 session：新聞內文／來源／每日精選走 `get_query_db`（唯讀模式下讀本機端
SQLite 快取），收藏、帳號、模組設定走 `get_db`（一律連遠端）。
`app/local_cache.py` 的快取白名單只加入 `news_sources`／`news_articles`／
`news_article_entities`／`news_daily_digests` 四張——**刻意不快取 `user_news_bookmarks`
（個人收藏，綁 users 外鍵）與 `news_settings`（管理設定，讀取量小）**，
維持既有「不快取帳號／使用者相關資料」的原則。

### 系統與文件同步

版本號在專案裡共出現於五個地方，這一版把**過去容易漏掉的兩個**也一併補上並寫成規則：

| 檔案 | 內容 |
|---|---|
| `app/main.py` | `FastAPI(version=)` 1.33.0 → 1.34.0，description 加「每日重點新聞」 |
| `app/routers/project_info.py` | `APP_VERSION` 同步（版本資訊頁讀這個值） |
| `README.md` | 標頭「目前版本」同步；功能對照表補 `F0-13-6`、`F0-19` 兩列 |
| `CHANGELOG.md` | 本條目 |
| `git_push.bat` | `DEFAULT_MSG` 同步——**不改的話 commit 訊息會沿用上一版**，`git log` 與實際版本對不上 |

`rules.md` 新增 **第九章「上版流程」**：完整的 PowerShell 步驟、上面這張版本同步表、
以及「哪些一次性腳本要跑、哪些不用」的對照（`migrate_schema` 只在改既有資料表欄位時跑；
`seed_gencc_translations` 只在改 GenCC 翻譯時跑；本版兩者都不需要）。

會寫這一章，是因為這套上版流程原本只存在於對話紀錄裡、沒有落在任何檔案上，
換一個開發脈絡就無從得知。**流程要寫進 repo 才會被繼承。**
同章也記下一條環境限制：Claude 的裝置橋接無法刪除檔案，而 git 每個寫入操作都要建立再刪除
`.git/index.lock`，因此 git 一律由使用者在 PowerShell 執行。

### 部署注意事項

- **正式環境不需要跑 `python -m app.migrate_schema`**：7 張新聞資料表對正式資料庫而言全都是新表，`Base.metadata.create_all()` 在伺服器啟動時會連同 `is_embargoed`／`embargo_until` 一起建好（已實測驗證）。**但本機或測試資料庫如果已經建過舊版的 `news_articles`（沒有 embargo 兩欄），就要跑一次 `migrate_schema` 補欄位**——腳本已加入對應的 `ALTER TABLE`
- 新增相依套件 `beautifulsoup4==4.12.3`（HTML 爬蟲類來源解析用）。AI 摘要直接以 `httpx` 呼叫 Anthropic API，不另裝 SDK，省下建置時間與相依體積
- Render 環境變數：`NEWS_COLLECT_SECRET`（必要）、`ANTHROPIC_API_KEY`（選填，未設會降級為規則式摘要，流程不中斷）、`PUBMED_API_KEY`／`NEWS_CONTACT_EMAIL`（選填）
- 首次部署後請到「公告 / 新聞管理 → 收集執行紀錄 → 立即執行一次收集」跑一次，再看「新聞來源健康度」分頁確認中國兩個官方站在 Render 新加坡節點是否可達

## v1.33.0 — 2026-08-06（全部六個查詢站的關聯網絡圖，新增可自訂顏色的圖例）

### 新功能

全部六個查詢站的關聯網絡圖（主畫面小圖 + 全螢幕大圖）左上角，新增顏色圖例，顯示每種節點類型（藥材/成分/靶點/疾病/基因）對應的顏色，點色塊可以自訂顏色，自訂後存進 `localStorage`，重新整理頁面後還會記得。

### 新增 `frontend/js/graph-legend.js`

全站共用的獨立元件，不是每個查詢站各自寫一份：

- `createGraphLegend(pageKey, items, onColorChange)`：`pageKey` 讓每個頁面的自訂顏色互不干擾，`items` 定義這個頁面有哪些節點類型，`onColorChange` 是顏色改變後要重繪圖表的回呼
- `legend.getColor(key)`：取得目前顏色（使用者自訂過就回傳自訂值，否則回傳預設值）
- `legend.mount(containerId)`：把圖例掛到指定容器左上角，圖例本身有「點色塊開啟顏色選擇器」跟「還原預設顏色」的功能

### 全站統一配色

藥材＝金黃 `#c99a3d`、成分＝藍 `#3b82c4`、靶點＝綠 `#3a9d63`、TCMSP 疾病＝橘紅 `#c0533e`、基因/暗黑基因/GenCC疾病＝紫 `#8b5fbf`——這本來就是系統原有的配色慣例，這次只是把寫死的 hex 值改成透過 `graphLegend.getColor()` 動態讀取，讓使用者可以自訂，配色本身沒有變動。

### 整合範圍

`tcmsp_query.html`（藥材站）、`disease_query.html`（疾病站）、`darkgene_query.html`（暗黑基因站）、`ginseng_darkgene.html`（藥材與暗黑基因關聯）、`gencc_disease_query.html`（可編碼蛋白區中藥與疾病關聯）、`gencc_disease_list_query.html`（可編碼蛋白區疾病關聯查詢站）——全部六個查詢站的節點顏色跟連接到該節點的邊線顏色，都改成動態讀取，不再寫死 hex 字串。

### 已測試驗證

- `graph-legend.js` 獨立測試：顏色自訂、`localStorage` 持久化（模擬重新整理頁面）、還原預設顏色，三種情境全部驗證通過
- 端到端整合測試：用模擬瀏覽器實際載入疾病查詢站、點選一筆疾病觸發完整渲染流程，確認圖例正確出現在網絡圖容器裡，四種節點類型（疾病/靶點/成分/藥材）都正確顯示
- 全面掃描確認六個頁面都沒有殘留寫死的節點顏色（含容易被忽略的「連到某類節點的邊線顏色」，這次過程中抓到一處遺漏並修正）
- 六個頁面 + `graph-legend.js` 本身，伺服器端測試全部 200 OK

### `rules.md` 更新

新增「關聯網絡圖圖例」章節，記錄這支共用元件的使用規範跟全站統一配色表，之後新增查詢站要沿用同一套元件跟配色，不要各自發明。

### 部署注意事項

不需要跑遷移腳本，這次是純前端功能。

## v1.32.6 — 2026-08-06（GenCC 疾病名稱補上 117 個預設三語系翻譯，涵蓋已比對到中藥靶點的疾病）

### 使用者需求

v1.32.5 加了三語系欄位，但要從零開始逐筆手動輸入 3 萬筆疾病的翻譯不切實際。使用者提出：至少幫「已經比對到中藥靶點、使用者實際會在查詢站看到」的疾病提供預設翻譯。

### 做法

抓出 `has_tcmsp_target=True` 的 279 筆疾病紀錄，去重後找出 **117 個不重複的英文疾病名稱**，逐一撰寫繁體中文／簡體中文／韓文翻譯（醫學疾病名稱，包含罕見症候群、體染色體遺傳模式標註等專業術語）。

新增 `app/seed_gencc_translations.py`：依 `disease_title` 完全比對套用翻譯——同一個疾病名稱可能對應資料庫裡好幾筆不同紀錄（同一疾病被不同基因、不同審查小組各自提交），一次補齊所有符合的紀錄，實際套用到 **1940 筆紀錄**（比 279 多，因為翻譯是依疾病名稱而非依單筆紀錄套用，涵蓋範圍比原本設想的更廣）。

**只補目前是空值的欄位，不覆蓋既有翻譯**（比照 `disease_cn_name_seed.json` 的既有做法）——可以重複執行，也不會洗掉後台管理者事後手動修正過的個別翻譯。

### 已測試驗證

- 執行種子腳本：1940 筆紀錄成功補上翻譯，抽查截圖裡的案例（ATM／ataxia telangiectasia）確認三語系翻譯正確
- 重複執行測試：模擬管理者手動修改某筆翻譯後，重跑腳本，確認手動修改的內容沒有被覆蓋（0 筆被更動，1940 筆判定「已有完整翻譯不需更動」）
- 三個 GenCC 相關頁面伺服器端測試 200 OK

### `rules.md` 更新

補充 `seed_gencc_translations.py` 的說明，記錄涵蓋範圍（只翻已比對到中藥靶點的疾病，不是全部 3 萬筆）跟之後擴充翻譯的方式。

### 部署注意事項

不需要跑遷移腳本（v1.32.5 已經建過欄位）。**匯入 GenCC 資料之後，記得多跑一次翻譯種子腳本**：

```bash
python -m app.seed_gencc_translations
```

## v1.32.5 — 2026-08-06（GenCC 疾病名稱新增三語系獨立儲存欄位，後台可個別編輯）

### 新功能

「可編碼蛋白區疾病關聯查詢站」新增疾病中文名稱（繁體/簡體）與韓文名稱，**三個語言各自獨立儲存於資料庫**，可於後台「可編碼蛋白區疾病管理」頁面個別修改，不是像藥材/疾病（TCMSP）那樣用 OpenCC 自動轉換繁簡、韓文完全不翻。

### 為什麼不用 OpenCC 自動轉換

醫學疾病名稱的繁簡用詞習慣，常常不只是字形不同（兩岸醫學術語慣用詞本來就可能不一樣），單純字形轉換不一定準確；韓文更是完全沒有字形轉換這條路可走。所以這次刻意設計成三語系各自獨立、後台人工建立/校對，而不是延用系統裡其他地方的自動轉換機制。

### 資料庫

`GenccDisease` 新增 `disease_name_cn`（簡體）、`disease_name_ko`（韓文）兩個欄位，`disease_cn_name`（繁體）沿用既有欄位語意不變。

### 後端

`GenccDiseaseOut`／`GenccDiseaseCreate`／`GenccDiseaseUpdate` schema 加入新欄位；更新端點使用 `exclude_unset=True`，只更新這次請求裡有帶到的欄位，個別修改某一語言的翻譯不會動到其他語言已經存在的資料。

### 前端

- 後台管理頁面：清單表格新增「中文（簡）」「한국어」欄位，編輯視窗新增對應輸入框
- 兩個查詢站新增共用函式 `diseaseNameByLang(d)`：依目前介面語系（tw/cn/ko）挑選對應欄位顯示，**任何語言還沒填寫翻譯時一律 fallback 顯示英文原文**，不會顯示空白，也不會借用其他語言的翻譯硬套

### 已測試驗證

- 後端 CRUD 完整測試：新增含三語系翻譯的疾病資料、只更新其中一個語言確認其他語言不受影響、清單端點正確回傳三語系欄位
- 前端顯示邏輯測試：tw/cn/ko 三種語系分別正確挑選對應欄位；刻意測試「韓文翻譯還沒填寫」的情況，確認正確 fallback 顯示英文原文而非空白
- 語法檢查全過；三個 GenCC 相關頁面伺服器端測試 200 OK

### `rules.md` 更新

新增「疾病名稱三語系獨立儲存」章節，說明跟系統其他地方（OpenCC 自動轉換）不同的設計理由，並提醒匯入腳本不會自動產生翻譯內容，需要透過後台持續人工補充。

### 部署注意事項

**這次一定要跑遷移腳本**（新增兩個資料庫欄位）：

```bash
$env:DATABASE_URL="你的 Neon 連線字串"
python -m app.migrate_schema
```

## v1.32.4 — 2026-08-06（重大修正：改名既有功能項目不會真的生效，`seed_default_data()` 只有新增邏輯沒有更新邏輯）

### 使用者回報

側邊選單同時出現「可編碼蛋白區疾病與中藥關聯」（改名前的舊名稱）跟「可編碼蛋白區中藥與疾病關聯」（v1.32.1 改名後的新名稱）兩個項目，點進去內容一樣。

### 根本原因

`app/seed.py` 的 `seed_default_data()`（每次伺服器啟動都會執行）處理 `FEATURE_CONFIG` 的邏輯是：**「代碼不存在才新增」**，完全沒有「代碼已存在但內容變了要更新」的邏輯。v1.32.1 把 GenCC 查詢站拆成兩頁時，F3-12 的**代碼沒變**，只改了 `name`/`nav_label`/`page_url`（從原本單一頁面的舊名稱、舊連結，改成新的疾病為主查詢站名稱、新連結）——但因為代碼還是 `F3-12`，`seed_default_data()` 判斷「已存在」就直接跳過，資料庫裡的 F3-12 卡在改名前的舊狀態，永遠不會自動更新，除非手動執行 `migrate_schema.py`。導覽選單因此同時顯示「資料庫裡卡住的舊 F3-12」（舊名稱、但 `page_url` 還是指向 v1.32.0 唯一那個頁面）跟「新建立的 F3-13」（新名稱，也指向同一個頁面）——兩者剛好都指到同一個頁面，就是使用者看到「兩個項目點進去內容一樣」的原因。

### 修正

`seed_default_data()` 補上更新邏輯：代碼已存在時，同步更新 `module`／`name`／`nav_label`／`page_url`／`sort_order`，不用每次改名都要記得手動跑遷移腳本才會生效（雖然還是建議照舊流程跑一次）。

**過程中發現 `migrate_schema.py` 也有一個更嚴重的潛在問題**：它的回填邏輯原本會**無條件覆蓋** `show_frontend`／`show_backend`——但這兩個欄位在「角色管理→權限矩陣」視窗是管理者可以自行調整的「全站共用設定」，如果每次跑遷移腳本都用 `FEATURE_CONFIG` 的預設值覆蓋回去，等於管理者在後台做的顯示區域調整，每次部署後都會被悄悄洗掉，而且不會有任何錯誤訊息。這次一併修正：兩支腳本現在都**只同步「開發者定義」的內容性欄位**（module/name/nav_label/page_url/sort_order），**故意不覆蓋「管理者可能已經調整過」的欄位**（show_frontend/show_backend/enabled）。

### 已測試驗證

完整重現使用者遇到的情境：手動塞入一筆模擬 v1.32.0 舊狀態的 F3-12（舊名稱、舊連結），並模擬管理者已經手動把 `show_backend` 調整成 `True`，接著執行 `seed_default_data()`（模擬伺服器重啟）——結果：F3-12 的名稱/連結正確更新成最新版本，**管理者手動調整的 `show_backend=True` 正確保留、沒有被洗掉**，F3-12 跟 F3-13 現在正確指向不同頁面。同樣情境也測試了 `migrate_schema.py`，結果一致。伺服器端測試確認新鮮啟動的資料庫，`GET /nav/menu` 回傳 F3-12/F3-13 名稱與連結都正確。

### `rules.md` 更新

新增這次踩到的教訓：修改 `feature_config.py` 裡**既有**功能項目的內容時，`seed_default_data()`（自動）跟 `migrate_schema.py`（手動）這兩支腳本的更新邏輯要保持一致，而且都要記得哪些欄位是「開發者控制」（要同步）、哪些是「管理者可能已自訂」（不能覆蓋）。

### 部署注意事項

**這次一定要跑遷移腳本**（雖然 `seed_default_data()` 現在也會在伺服器重啟時自動修正，但正式環境建議還是照標準流程手動跑一次，確保立即生效不用等下次重啟）：

```bash
$env:DATABASE_URL="你的 Neon 連線字串"
python -m app.migrate_schema
```

部署完成後，麻煩到「角色管理」的權限矩陣視窗確認 F3-12「可編碼蛋白區疾病關聯查詢站」跟 F3-13「可編碼蛋白區中藥與疾病關聯」兩筆資料都存在、名稱正確、且連結到不同頁面。

## v1.32.3 — 2026-08-06（排查 Console 錯誤：確認幾乎全部來自瀏覽器加密貨幣錢包擴充功能，補上 favicon 消除唯一真正相關的 404）

### 使用者回報

Console 出現「許多錯誤」，附上完整截圖。

### 排查結果

截圖裡的錯誤訊息（`zilPay injected`、`TonAdapter`／`SolanaAdapter`／`TronAdapter`／`BitcoinAdapter`／`EthereumAdapter`／`BinanceInjectedProvider` 的 `Broadcast channel unavailable` 等）**全部來自瀏覽器裝的加密貨幣錢包擴充功能**（`inpage.js`／`content.js`／`content-script.js`／`record-api.js` 都是擴充功能自己的檔案名稱，不是這個平台的程式碼），這種擴充功能會被注入到使用者瀏覽的**每一個網站**，跟平台本身完全無關，不管開哪個網站都會出現這些訊息。

截圖裡唯一真正跟這個平台有關的一行是：

```
GET https://t0.gstatic.com/faviconV2?...&url=https://fwc-tcmsp.pages.dev/gencc_disease_query&size=32  404
```

這是瀏覽器自動嘗試抓取網站圖示（favicon）沒抓到——純粹缺一個圖示檔案，**不影響任何功能**，但既然發現了就順手修掉。

### 修正

幫全部 32 個前端頁面補上 favicon（用內嵌 SVG data URI，不需要額外的圖片檔案，深藍色圓形配「藥」字，符合平台主題色），統一寫成一支批次腳本插入，確認沒有重複插入、沒有破壞既有 HTML 結構。

### 已測試驗證

HTML 結構檢查（`<head>` 標籤數量、有無重複插入 favicon）全部通過；32 個頁面 `<script>` 內容語法檢查全部通過；用 `TestClient` 測試全部 32 個頁面，確認 200 OK。

### 給使用者的說明

之後如果 Console 又跳出很多錯誤，麻煩先看一下錯誤訊息裡提到的**檔案名稱**——如果是 `inpage.js`、`content.js`、`content-script.js`、或提到 `wallet`／`blockchain`／`crypto`／`Adapter` 這類字眼，通常都是瀏覽器擴充功能（尤其是加密貨幣錢包）的訊息，跟這個平台無關，可以直接忽略；真正跟平台有關的錯誤訊息，檔案名稱通常會是我們自己的 `.js` 檔案（例如 `site-lang.js`、`api.js`、或頁面自己的 inline script）。

### 部署注意事項

不需要跑遷移腳本，這次是純前端靜態內容新增（favicon）。

## v1.32.2 — 2026-08-06（清理 v1.32.1 改名後殘留的舊頁面名稱引用；徹底排查簡繁混雜回報但無法重現）

### 使用者回報

截圖顯示「可編碼蛋白區中藥與疾病關聯」頁面切到簡體中文後，標題出現「可編碼蛋白區中药与疾病关联」這種繁簡混雜的情況（前半段沒轉換、後半段轉換了）。

### 徹底排查過程

1. 用真實 OpenCC 套件（不是假的 mock）直接測試這段文字的轉換結果——**完全正確**，全部字元一致轉換為簡體
2. 發現本機測試用的 OpenCC 版本（npm 裝的 1.4.1）跟頁面實際載入的 CDN 版本（1.0.5）不一致，特地額外安裝真正的 1.0.5 版本重新測試——**結果依然完全正確**
3. 用模擬瀏覽器環境，對三個 GenCC 相關頁面各自跑「簡體→韓文→繁體→簡體」連續 4 輪語言切換測試——**全部頁面、全部輪次都正確**，沒有任何繁簡混雜的情況
4. 逐字元比對「可編碼蛋白區中藥與疾病關聯」→「可编码蛋白区中药与疾病关联」的轉換結果，確認「編/碼/區/藥/與/關/聯」都有正確轉換，「可/蛋/白/中/疾/病」這幾個字本來就是繁簡同形不需要轉換——**不是殘留，是正常結果**

**結論：目前程式碼經過徹底測試找不到這個 bug，判斷是瀏覽器快取了 v1.32.1 改名前的舊版本頁面/腳本所致**，不是程式碼本身的問題。

### 順帶清理的真實問題

排查過程中發現一個真實的殘留 bug：v1.32.1 把 `gencc_disease_query.html` 改名時，**忘記同步更新後台管理頁面（`gencc-diseases.html`）說明文字裡對舊頁面名稱的引用**，還在講「請到「可編碼蛋白區疾病與中藥關聯」查詢站」，這個頁面已經不存在這個名稱了。已修正為正確引用兩個新查詢站（疾病為主／藥材為主）。同時清理 `i18n-dict.js`、`gencc_diseases.py`、`models.py` 裡改名前殘留的舊名稱字典 key 跟程式碼註解。

### 已測試驗證

三個 GenCC 頁面伺服器端測試全部 200 OK；語法檢查全過；上述四輪語言切換模擬測試全部通過。

### 給使用者的建議

麻煩**強制重新整理頁面**（Windows/Linux 按 `Ctrl+Shift+R` 或 `Ctrl+F5`，跳過瀏覽器快取直接抓最新版本）後再測試一次。如果強制重新整理後問題依然存在，麻煩截圖並附上瀏覽器開發者工具 Network 分頁裡 `site-lang.js`／`i18n-dict.js` 這兩個檔案的回應內容（確認瀏覽器實際抓到的版本內容），我會再往下查。

### 部署注意事項

不需要跑遷移腳本，這次是純前端文字清理。

## v1.32.1 — 2026-08-06（GenCC 功能補齊「疾病為主」查詢站；改名區分兩種瀏覽方向；用真實 3 萬筆資料完整測試）

### 使用者需求

v1.32.0 只做了「藥材為主」的查詢站（左側選藥材、右側看比對到的疾病）。這次補上比照暗黑基因兩頁模式的「疾病為主」查詢站（左側選疾病、右側看比對到的靶點/藥材），並把原本的頁面改名區分方向。

### 頁面改名

`gencc_disease_query.html` 從「可編碼蛋白區疾病與中藥關聯」改名為**「可編碼蛋白區中藥與疾病關聯」**（中藥為主，藥材在前），比照「藥材與暗黑基因關聯」的命名方式。

### 新增「疾病為主」查詢站

新增 `gencc_disease_list_query.html`，複製 `darkgene_query.html` 改造，比照暗黑基因關聯查詢站的瀏覽方向：左側選一個可編碼蛋白區疾病，右側顯示疾病說明與 TCMSP 中藥靶點關聯。

後端新增 `GET /gencc-diseases/{disease_id}/tcmsp-links`，比照 `get_gene_tcmsp_links` 的邏輯：拿這筆疾病的 gene_symbol 去比對 TCMSP 靶點名稱，列出候選藥材。

### 重要架構決策：資料量太大，不能整包載入前端

暗黑基因只有 1245 筆，可以整包載入瀏覽器後做 client-side 篩選；GenCC 有 3 萬筆，這樣做會嚴重拖慢載入速度。改成**搜尋驅動**：

- 左側清單不預先載入全部資料，初始只顯示前 100 筆當預覽，使用者輸入關鍵字才觸發查詢，350ms debounce 避免每個按鍵都送一次 API 請求
- **修正一個真實 bug**：`/gencc-diseases/public/list` 端點原本沒有筆數限制，沒有篩選條件時會整包 3 萬筆回傳——這次補上限制（無篩選條件回傳前 100 筆，有篩選條件最多 500 筆），開發過程中發現並修正，沒有上線過

### 已測試驗證（用真實完整資料集，非測試樣本）

- 沒有篩選條件的清單查詢：100 筆，0.1 秒
- 搜尋 BRCA1：11 筆，0.05 秒
- 疾病反查靶點/藥材：0.036 秒，1 個靶點、12 個候選藥材（跟 v1.32.0 的 herb-links 反查結果互相印證一致）
- 只顯示有中藥靶點的疾病：279 筆（跟統計數字一致）
- 三個 GenCC 相關頁面伺服器端測試全部 200 OK
- 遷移腳本測試：40 筆功能項目正確回填（新增 F3-13）

### 功能項目

F3-12 從「可編碼蛋白區疾病與中藥關聯查詢站」改為「可編碼蛋白區疾病關聯查詢站」（疾病為主，對應新頁面）；新增 F3-13「可編碼蛋白區中藥與疾病關聯」（中藥為主，對應改名後的原頁面）。

### 前端翻譯

補齊兩個查詢站的英文/韓文翻譯（15 筆新詞條）。

### `rules.md` 更新

新增「兩頁查詢站模式，完全比照暗黑基因」跟「疾病為主查詢站不能整包載入、改用搜尋驅動」的架構說明，記錄這次修正的清單端點筆數限制 bug。

### 部署注意事項

**這次一定要跑遷移腳本**（功能項目設定變動，F3-12/F3-13 需要回填）：

```bash
$env:DATABASE_URL="你的 Neon 連線字串"
python -m app.migrate_schema
```

資料庫結構本身沒有變動（v1.32.0 已經建過 `gencc_diseases` 表），如果還沒匯入過 GenCC 資料，記得執行：

```bash
python -m app.import_gencc_diseases data_import/gencc-submissions.csv
```

## v1.32.0 — 2026-08-06（新功能：可編碼蛋白區疾病與中藥關聯，資料來源 GenCC）

### 新功能

新增「可編碼蛋白區疾病與中藥關聯」，資料來源 GenCC（Gene Curation Coalition，https://thegencc.org/download）——彙整多個專家審查小組（ClinGen、PanelApp 等）對「基因-疾病因果關係」的評估結果。比對邏輯比照暗黑基因（拿 gene_symbol 去比對 TCMSP 靶點名稱），建立「基因 → 靶點 → 成分 → 藥材」的機制層級關聯鏈，畫面設計比照「藥材與暗黑基因關聯」。

### 資料庫

新增 `GenccDisease` 資料表：sgc_id（唯一識別碼）、gene_symbol、disease_title、classification_title（信心等級：Definitive/Strong/Moderate/Limited/Disputed Evidence 等）、moi_title（遺傳模式）等 GenCC 核心欄位，以及預先計算好的 `has_tcmsp_target` 統計欄位。`TcmspHerb` 新增 `gencc_disease_count` 欄位。

### 匯入腳本 `app/import_gencc_diseases.py`

- 依 sgc_id 做 upsert，每 2000 筆分批 commit（資料量遠大於暗黑基因，不能全部讀完才 commit）
- **實際用使用者提供的完整資料集測試**：檔案 `wc -l` 顯示 46,664 行，但 CSV 欄位內容含換行符號導致行數虛增，用 `csv.DictReader` 正確解析出 **30,410 筆邏輯資料**（0 筆缺漏 sgc_id/gene_symbol），匯入耗時 23.1 秒
- 匯入完成自動觸發 `recompute_stats.py`：30,410 筆資料中 **279 筆比對到中藥靶點**；抽查知名癌症基因 BRCA1（11 筆疾病斷言，11 筆比對到）、BRCA2/TP53/APC（比對結果合理）
- 涵蓋最多可編碼蛋白區疾病的藥材：葛花（158 個）、麻黄（128 個）、白果（115 個）——跟暗黑基因功能的排名模式一致（這些藥材本來就有較多 TCMSP 靶點紀錄）

### 後端 API（`app/routers/gencc_diseases.py`）

前台：`/gencc-diseases/public/list`（清單）、`/public/stats`（依信心等級分組統計）、`/herb-links/{herb_id}`（藥材反查，回應結構完全比照暗黑基因版本）、`/public/herb-stats`（中藥覆蓋排行）。後台：CRUD（新增/編輯/軟刪除），清單端點**故意不預設列出全部**（資料量太大），需要關鍵字或信心等級篩選才查詢，且限制 500 筆。

### 前端

- **查詢站**（`gencc_disease_query.html`）：複製「藥材與暗黑基因關聯」改造，UI/網絡圖/顯示層級/下載機制完全一致；跟暗黑基因版本不同的是**不預設鎖定特定藥材**（沒有類似人參的指標藥材）
- **後台管理**（`gencc-diseases.html` + `gencc-diseases.js`）：比照暗黑基因管理頁面，但**沒有瀏覽器上傳功能**（資料量太大不適合），改成引導管理者在伺服器/本機執行匯入腳本
- 兩個新頁面補齊英文/韓文翻譯（33 筆新詞條）

### 統計/唯讀模式整合

`recompute_stats.py` 新增兩個函式（GenCC 疾病的中藥靶點比對、藥材的 GenCC 疾病關聯統計），`app/local_cache.py` 的本機端查詢快取涵蓋範圍加入 `gencc_diseases` 資料表。

### 功能項目

新增 F3-11（可編碼蛋白區疾病管理，後台）、F3-12（可編碼蛋白區疾病與中藥關聯查詢站，前台），比照暗黑基因的 F3-2／F3-3 命名模式。

### 已測試驗證

模型/schema/router 語法檢查全過；用真實 GenCC 完整資料集（30,410 筆）測試匯入、統計重算、API 端點（清單/統計/藥材反查/覆蓋排行/CRUD）全部正確；遷移腳本測試（39 筆功能項目正確回填，統計欄位正確重算）；兩個新頁面伺服器端測試 200 OK。

### `rules.md` 更新

新增「五之四」章節，完整記錄這次的設計決策，特別是「`wc -l` 跟 `csv.DictReader` 算出來的列數可能不一致（欄位內容含換行符號）」這個認知陷阱，跟資料量是暗黑基因 20~30 倍所需要的架構考量（分批 commit、後台不預設列出全部）。

### 部署注意事項

**這次一定要跑遷移腳本**（新增 `gencc_diseases` 資料表跟 `TcmspHerb.gencc_disease_count` 欄位）：

```bash
$env:DATABASE_URL="你的 Neon 連線字串"
python -m app.migrate_schema
```

**接著要匯入 GenCC 資料**（正式環境沒有這份資料，遷移腳本不會自動下載）：

```bash
python -m app.import_gencc_diseases data_import/gencc-submissions.csv
```

GenCC 官網下載連結：https://thegencc.org/download → 選「RECOMMENDED New Format」下的 CSV。

## v1.31.6 — 2026-08-06（補齊查詢站藏在 `<script>` 樣板字串裡的按鈕/UI文字韓文翻譯）

### 問題

使用者截圖指出藥材查詢站的「全選成分」「只選有靶點資料」「只選有疾病資料」等按鈕沒有韓文翻譯。追查後發現：v1.29.0／v1.31.4 用的掃描工具（HTMLParser）**刻意跳過 `<script>` 標籤內容**（避免誤抓程式碼本身），但查詢站類頁面的成分挑選器按鈕、表格空狀態訊息等 UI 文字，大多是寫在 `main.innerHTML = \`...\`` 這種 JS 樣板字串裡動態產生的，不是靜態 HTML——這整類文字完全沒被掃描到。

### 修正

新增一支能掃描 `<script>` 標籤內嵌 HTML 樣板字串的工具（抓 `>文字<` 這種標籤之間的片段、以及看起來像 UI 文字的字串常數），第一輪抓到 532 筆候選，其中：

- **約 400 多筆是動態生物/化學資料**（`ZH_DICT` 對照表的疾病/靶點中文譯名，例如「γ-胺基丁酸受體α1亞基」這類專有名詞）——這類資料在韓語模式下本來就會自動 fallback 顯示英文原文，不會用到字典，過濾掉不處理
- **127 筆是真正的 UI 文字缺漏**：按鈕（全選成分、全不選、只選 OB 前 10、只選有靶點資料、只選有疾病資料、下載按鈕等）、空狀態訊息（尚未勾選任何成分、沒有符合條件的基因等）、載入中文字、詞彙表說明（Mol ID、Tar ID、OB、DL 等欄位定義），逐一撰寫英文/韓文翻譯後補上
- 額外發現 `tcmsp_query.html` 的 `capTgt`/`capDis` 下拉選單用的是「數字 加空格 個靶點/個疾病」格式，跟其他站「數字緊接個靶點/個疾病」的格式不一樣，之前只補了無空格版本，這次一併補上有空格版本
- 又補了 16 筆人參相關的植物照片說明文字（人參根部、紅參、西洋參、竹節參等）

字典從 466 筆擴充到 613 筆（en/ko 各自）。

### 已測試驗證

用模擬瀏覽器直接測試使用者截圖裡點名的 5 個按鈕，套用韓文後全部正確翻譯（例如「只選有靶點資料」→「타겟 데이터가 있는 것만」）。同時重跑 v1.31.5 的全站回歸測試（8 個代表性頁面完整往返切換語言），確認這次補字典沒有破壞任何既有功能，四個查詢站伺服器端測試全部 200 OK。

### `rules.md` 更新

新增兩條規則：（1）掃描查詢站類頁面的翻譯缺漏，一定要連 `<script>` 內的樣板字串一起掃，不能只掃靜態 HTML；（2）掃描出來的候選詞條要先分辨是「固定的操作介面文字」還是「隨資料內容而變的專有名詞」，只有前者需要翻譯，後者（疾病名、靶點名、藥材學名）不需要也不應該加進字典。

### 部署注意事項

不需要跑遷移腳本，純前端字典擴充。

## v1.31.5 — 2026-08-06（重大修正：site-lang.js 連續切換語言會卡在中間狀態，影響全站）

### 問題

使用者截圖顯示藥材查詢站切到韓文後，翻譯不完整——標題、按鈕文字混雜著簡體中文（例如「TCM 中药肿瘤筛选平台」），沒有變成韓文。

追查後發現這是 `site-lang.js`（全站共用的語系轉換核心，v1.28.0 起使用，v1.31.4 才剛讓查詢站也開始用）的一個結構性 bug，**不是只有查詢站，是任何頁面只要連續切換兩次以上語言都會踩到**：

- 轉換邏輯原本是拿「目前畫面上顯示的文字」（`node.nodeValue`）當輸入去轉換
- 如果先切到簡體中文（文字被 OpenCC 轉成簡體），再切到韓文，字典比對法要找「這段文字」在韓文字典裡的翻譯——但字典的 key 全部是原始繁體中文，簡體字完全比對不到，文字就卡在簡體狀態
- 更嚴重的是切回繁體中文時：因為程式判斷「繁體不需要轉換」就直接跳過整個處理流程，文字甚至沒有機會被還原，會維持在切換前的語言狀態出不來

### 修正

用 `WeakMap` 記住每個文字節點「第一次遇到時」的原始文字，之後不管切換幾次語言，永遠拿這個原始值重新轉換，不再拿「目前畫面上可能已經被改過」的文字當輸入。同時修正 `walkAndConvert()` 的節點篩選條件（改用原始文字判斷有沒有中文可轉，不能用目前顯示的文字，不然節點一旦被轉成完全沒中文字元的韓文/英文就會被篩選掉，之後永遠不會被重新考慮），以及 `applySiteLanguage()` 不再因為目標語言是繁體中文就跳過整個轉換流程。

### 已測試驗證

- 重現使用者情境（先簡體、再韓文）：修正前會卡在簡體，修正後正確變成韓文，且確認完全沒有殘留簡體字
- 連續 9 輪語言切換（cn→ko→tw→en→cn→tw→ko→en→tw）壓力測試：16ms 完成，沒有卡死，每一輪結果都正確
- 動態插入新內容後再切換語言：確認新內容也能正確被翻譯
- **8 個代表性頁面**（個人化設定、客戶資料管理、中藥行地理推薦、暗黑基因管理、四個查詢站）各自完整往返切換語言測試：全部通過，速度都在毫秒等級，確認這個共用檔案的修正沒有破壞任何既有頁面

### `rules.md` 更新

新增這次的教訓記錄：語言轉換一定要從「原始文字」出發，不能從「目前畫面上的文字」出發，這是這次真實踩過的 bug，之後修改 `site-lang.js` 核心邏輯時要保留這個原則。同時記錄了使用者提出的另一個架構性問題（「個人化設定」的全站語系跟查詢站自己的語系選單是兩套彼此不同步的機制），列入待補強事項，這次沒有動這塊，需要先想清楚設計方向再處理。

### 部署注意事項

不需要跑遷移腳本，這次是純前端修正。**這是這個 session 修過的第三個語系相關重大 bug**（無窮迴圈 v1.28.2、四查詢站缺韓語 v1.31.4、這次的中間狀態卡住），建議部署後做一次完整測試：任選一個頁面連續切換繁體→簡體→韓文→英文→繁體，確認每一輪畫面都正確切換，沒有殘留上一輪語言的痕跡。

## v1.31.4 — 2026-08-06（四個查詢站補上韓語支援，順便補上簡體中文的靜態文字翻譯缺口）

### 問題

使用者回報四個查詢站（藥材/疾病/暗黑基因關聯查詢站、藥材與暗黑基因關聯）完全沒有韓語選項。追查後發現這四站原本就沒有掛載 `nav.js`（沒有側邊選單，是獨立的頂部導覽列版面），全站語系機制（v1.28.0 起做的 en/ko 字典系統）從一開始就沒有整合進來——這四站各自只有一套獨立的「tw/cn 繁簡切換」，且**連簡體中文都只轉換動態的中藥資料（藥材/疾病名稱），從來沒有真正轉換過靜態的介面文字**（按鈕、說明文字等固定標籤）。

### 修正

不透過 `nav.js`，改成四個頁面自己直接呼叫 `window.applySiteLanguage(lang)`（`site-lang.js` 匯出的函式，本來就不依賴 `nav.js` 才能用）：

- 四個頁面都加入 `<script src="js/i18n-dict.js">` 與 `<script src="js/site-lang.js">`
- `uiLangSelect` 下拉選單新增「한국어」選項
- 語系切換事件除了原本控制 `zhConv()`（給動態中藥資料用）的邏輯，另外呼叫 `applySiteLanguage(uiLang)` 翻譯頁面固定的介面文字——**這個改動同時讓簡體中文模式也變得更完整**，不再只轉換藥材/疾病名稱
- `zhConv()` 加入韓語分支：動態中藥資料（藥材/疾病/成分名稱）沒有機器翻譯資源，韓語模式下維持原文（繁體中文）不轉換，跟字典比對法是兩套不同機制
- `tcmsp_query.html` 額外有一個「圖上文字」選單（`netLangSelect`，控制網絡圖節點文字），也加上韓語選項，行為比照既有的「English（原文）」邏輯

### 字典擴充

掃描四個查詢站的靜態文字缺漏（用跟 v1.29.0 同一套 HTMLParser 掃描工具），第一輪找到 66 筆缺漏，逐一撰寫英文/韓文翻譯後補上，字典從 402 筆擴充到 466 筆（en/ko 各自），重新掃描確認缺漏歸零。

### 已測試驗證

- 語法檢查：四個檔案全部通過
- 模擬瀏覽器測試：四個頁面套用韓語後，靜態介面文字（導覽列、標題、按鈕、說明文字）都正確翻譯成韓文
- 動態內容測試：韓語模式下 `zhConv()` 正確維持中藥資料原文不出錯（`null`/空字串等邊界情況也正常）
- **無窮迴圈防護測試**（避免重蹈 v1.28.2 覆轍）：模擬頁面自己的 JS 動態插入含中文的新內容（例如切換藥材時 `main.innerHTML` 被整個換掉），確認 503ms 內正常完成、沒有卡死，且新插入的內容也正確被翻譯
- 四個頁面伺服器端測試全部 200 OK

### `rules.md` 更新

新增規則：查詢站類頁面（沒有 `nav.js`）要支援多語系的標準做法是頁面自己直接呼叫 `applySiteLanguage(lang)`，不需要（也不能靠）掛載 `nav.js`；並記錄 `zhConv()` 與字典比對法是服務不同內容（動態資料 vs 靜態介面文字）的兩套機制，不要混淆。

### 部署注意事項

不需要跑遷移腳本，這次都是前端修改。

## v1.31.3 — 2026-08-06（唯讀模式真正核心補上：查詢站關聯查詢改讀本機端資料加速）

### 補上原本被漏掉的核心需求

使用者提醒：唯讀模式（連線本機端資料庫）最初的目的，是讓藥材/疾病/暗黑基因查詢站的**關聯查詢改讀伺服器本機端資料，加快查詢速度**——v1.31.0~1.31.2 只做了「鎖住寫入」這個安全限制，完全沒有實作「真的連去本機端資料」這個效能核心，這次補上。

### 新增 `app/local_cache.py`：本機端查詢快取

- 只快取查詢站相關、變動很少的資料表（TCMSP 全系列 + 暗黑基因），**不快取病患資料、帳號、DNA 資料**這類會頻繁異動或高度敏感的資料，縮小快取範圍讓重建更快
- 啟用唯讀模式時，自動用**最新一次成功的備份**內容重建這份本機端 SQLite 快取檔案，不需要另外手動觸發

### 新增 `get_query_db()` 依賴（`app/database.py`）

唯讀模式啟用且本機快取存在時，回傳指向本機 SQLite 快取的 session；其餘情況維持原本連遠端資料庫的行為。11 個查詢站前台讀取端點改用這個依賴：

- `tcmsp.py`：藥材清單/詳情、疾病清單/詳情、`/tcmsp/data/full`
- `dark_genes.py`：暗黑基因統計、逐基因統計、中藥暗黑基因覆蓋統計、暗黑基因清單、`{gene_id}/tcmsp-links`、`herb-links/{herb_id}`

`patient-herb-suggestions`（病患暗黑基因彙總建議）**沒有**切換，因為需要查詢病患／DNA 資料，不在快取範圍內，維持連遠端資料庫。

### 已測試驗證

完整流程測試：建立備份 → 啟用唯讀模式（自動重建本機端快取，108,898 筆資料）→ 藥材清單查詢結果跟切換前（連遠端）完全一致（筆數、內容逐筆比對）→ 疾病清單、暗黑基因清單、中藥暗黑基因覆蓋統計排行第一名（葛花 32）都正確 → 連 11.8MB 的 `/tcmsp/data/full` 完整資料集也正確透過本機端回應 → 關閉唯讀模式後查詢站正常切回遠端資料庫。

### 修正一個過程中發現的真實 bug

備份 JSON 裡的日期時間欄位是用 `.isoformat()` 存成字串，第一次還原寫入本機端 SQLite 時噴出 `SQLite DateTime type only accepts Python datetime and date objects` 錯誤——SQLite 的正式 `DateTime` 欄位型別只接受真正的 Python `datetime` 物件，插入前要轉換回來。已修正並重新驗證通過。

### 前端

系統設定頁面「連線本機端資料庫」卡片：說明文字更新為正確描述效能加速的目的，新增本機端快取建立時間/大小的顯示，啟用/關閉時的提示文字也更新為說明查詢站資料來源會跟著切換。

### `rules.md` 更新

新增「唯讀模式的真正核心」章節，完整記錄這套本機端查詢快取機制的設計、涵蓋範圍、與這次踩過的日期時間型別轉換陷阱。

### 部署注意事項

不需要跑遷移腳本，這次沒有異動任何資料庫欄位（本機端快取是獨立的 SQLite 檔案，跟主資料庫的 schema 無關）。

## v1.31.2 — 2026-08-06（修正唯讀模式自相矛盾死結：啟用後無法再建立備份）

### 問題

使用者截圖顯示 `POST /system-settings/backup-database` 回應 **423 Locked**——不是 v1.31.1 修的「Failed to fetch」逾時問題，是真正的 HTTP 錯誤回應。

追查後發現一個自相矛盾的死結：唯讀模式的白名單只排除了 `/auth/login`、`/auth/logout`、`PUT /system-settings/read-only-mode` 這三個路徑，**忘了排除「建立備份」這個端點**。而 v1.31.1 才剛加的規則是「啟用唯讀模式必須先有成功的備份」——兩條規則加在一起，只要唯讀模式一旦被啟用（例如更早之前測試時就啟用過），備份端點本身就會被唯讀模式攔截，永遠無法再建立新的備份，形成死結。

### 修正

把 `POST /system-settings/backup-database` 加進唯讀模式的白名單。建立備份是「系統自救所必須的操作」（不會修改應用程式資料本身，只是讀取匯出），應該不受唯讀模式限制，跟登入/登出/關閉唯讀模式本身同一類。

### 已測試驗證

重現使用者遇到的完整情境：建立第一次備份 → 啟用唯讀模式 → **唯讀模式啟用中再次建立備份，正確回傳 200（不再是 423）** → 確認一般 CRUD 操作仍然正確被擋（423，其他限制沒有被破壞）→ 關閉唯讀模式恢復正常。

### `rules.md` 更新

記錄這次死結的教訓：設計「唯讀模式白名單」這種安全機制時，不能只想到「開關本身」要放行，還要想清楚「這個開關啟用之後，還有哪些操作是系統健康運作/自救所必須的」，一次列清楚，不要事後才發現漏了一個造成死結。

### 給使用者的立即解法（如果目前卡在這個狀態）

不需要等部署新版本，現在就可以先手動解除：到「系統設定」頁面把「啟用唯讀模式」的勾選框取消，這個操作本身沒有被唯讀模式擋住，可以正常運作。部署這個版本之後，之後就不會再遇到這個死結了。

### 部署注意事項

不需要跑遷移腳本，這次沒有異動任何資料庫欄位。

## v1.31.1 — 2026-08-06（修正備份逾時失敗；唯讀模式啟用前強制要求先有成功的備份）

### 問題 1：備份請求「Failed to fetch」

使用者回報正式環境按「建立備份並下載」出現「備份失敗：Failed to fetch」。原因：v1.31.0 的實作是在同一次 HTTP 請求裡「同步」處理完整個備份（逐一掃過幾十張資料表、幾萬筆關聯資料、再加密）才回應——本機測試資料量小感覺不出問題，但正式環境資料量大時，這個同步處理容易超過 Render 或瀏覽器端的逾時限制，連線被中斷，前端看到的就是網路層級的「Failed to fetch」，不是一個正常的 HTTP 錯誤回應。

### 修正 1

`POST /system-settings/backup-database` 改用 FastAPI 的 `BackgroundTasks`：端點只負責建立一筆 `running` 狀態的紀錄就立刻回應（本機測試耗時 0.01~0.04 秒），實際的匯出/加密工作丟到背景執行。前端改成每 2 秒輪詢一次 `GET /backup-jobs`，看到狀態變成 `success`/`failed` 才顯示結果並觸發下載，最多等 2 分鐘（超過的話會提示使用者背景作業仍在執行、可以稍後重新整理頁面查看）。

### 問題 2：唯讀模式應該要求先有備份

使用者指出：「連線本機端資料庫（唯讀模式）」這個設定，邏輯上應該要先有至少一次成功的備份才能啟用——如果本機端根本沒有任何資料庫內容，這個設定形同虛設，甚至可能誤導使用者以為系統真的有一份可用的本機備援。

### 修正 2

`PUT /system-settings/read-only-mode` 在啟用（`enabled=true`）時，後端會先檢查 `BackupJob` 資料表裡是否存在至少一筆 `status=success` 的紀錄，沒有的話回傳 400 錯誤並中止，不會真的啟用。前端也加了主動提示：頁面載入時如果偵測到還沒有任何成功的備份，會先在唯讀模式卡片下方顯示提醒文字，不用等使用者點了勾選框才知道要先備份。

### 已測試驗證

完整流程測試（含真實 HTTP 請求量測耗時）：
- 還沒有任何備份時嘗試啟用唯讀模式 → 正確回傳 400 並附上清楚的錯誤訊息
- 觸發備份 → 真實 HTTP 請求耗時 0.013 秒立即回應（不再同步阻塞）
- 輪詢確認背景任務正確完成、狀態變成 `success`
- 有成功備份之後，啟用唯讀模式 → 正確成功

### `rules.md` 更新

新增兩條規則：（1）任何可能耗時的操作都要用背景任務處理，不能在 HTTP 請求裡同步做完才回應；（2）唯讀模式啟用前的備份存在性檢查一定要放在後端，不能只靠前端擋。

### 部署注意事項

不需要跑遷移腳本，這次沒有異動任何資料庫欄位。

## v1.31.0 — 2026-08-06（系統設定新增：資料庫備份下載到本機、唯讀模式「連線本機端資料庫」）

### 新增功能一：資料庫備份到本機

系統設定頁面新增「資料庫備份到本機」卡片：

- 按「建立備份並下載」，系統會把整個資料庫內容匯出成一份**加密檔案**，自動開始下載到管理者自己的電腦
- 新增 `app/backup_service.py`：用 SQLAlchemy metadata 動態列舉全部資料表逐一匯出（之後新增資料表不需要手動更新這支腳本），用 `cryptography.fernet` 加密後才寫入磁碟，金鑰衍生自既有的 `TCM_JWT_SECRET`（不需要額外設定新的環境變數）
- 路徑由系統自動決定（時間戳記命名，存在專用 `backups/` 目錄），不開放使用者自訂
- 原本就存在但只是骨架的 `BackupJob` 資料表跟 `/backup-jobs` 端點，這次正式串接了真實的備份/下載流程
- 新增 API：`POST /system-settings/backup-database`（觸發備份）、`GET /system-settings/backup-database/{job_id}/download`（下載，串流回傳，維持加密狀態不解密）

### 新增功能二：唯讀模式（「連線本機端資料庫」設定）

系統設定頁面新增「連線本機端資料庫」（唯讀模式）卡片，勾選後**全站禁止任何新增/編輯/刪除操作**，只能瀏覽查詢：

- 用**全站 middleware** 攔截（`app/main.py` 的 `enforce_read_only_mode`），不是逐一去每個 CRUD 端點加檢查，確保之後新增的端點也一定會被擋到
- 攔截條件：`POST`/`PUT`/`PATCH`/`DELETE` 且不在白名單裡，一律回傳 423（Locked）
- **白名單包含登入/登出端點，以及關閉唯讀模式本身這個操作**——這是避免把系統鎖死的關鍵設計，唯讀模式啟用後管理者仍然可以登入、仍然可以把這個設定關掉
- 設定值存在通用的 `SystemSetting` key-value 表，不需要額外資料庫欄位

### 已測試驗證

完整流程測試（含真實 HTTP 請求）：觸發備份 → 下載確認內容確實加密（直接讀取是亂碼，用正確金鑰可解密還原）→ 啟用唯讀模式後 CRUD 操作被擋（423）、GET 查詢不受影響 → 確認登入端點在唯讀模式下仍正常運作 → 確認可以在唯讀模式啟用中把它關掉、不會鎖死 → 關閉後 CRUD 恢復正常。

### 部署注意事項

**不需要跑遷移腳本**，`SystemSetting` 是既有的通用 key-value 表，`BackupJob` 表也早就存在，這次沒有新增/修改任何資料庫欄位。

## v1.30.0 — 2026-08-06（四項統計數字改為預先存入資料庫，不再即時運算）

### 背景

以下四個地方原本是**每次 API 請求都重新掃一次關聯資料算出來的**，改成預先計算好存入資料庫欄位，查詢時直接讀欄位：

1. 疾病關聯查詢站：靶點統計
2. 藥材關聯查詢站：靶點統計
3. 暗黑基因關聯查詢站：有中藥靶點統計
4. 藥材與暗黑基因關聯：有基因關聯統計

### 資料庫變更

新增 4 個統計欄位：`TcmspHerb.target_count`、`TcmspHerb.dark_gene_count`、`TcmspDisease.target_count`、`DarkGene.has_tcmsp_target`。

### 新增 `app/recompute_stats.py`

統一的重算腳本，把原本分散在四個端點裡的即時運算邏輯集中起來，一次算完寫入資料庫：
- `python -m app.recompute_stats` 可以獨立執行
- **匯入 TCMSP 資料或暗黑基因資料時自動觸發**（`import_tcmsp_data.py`／`import_dark_genes.py` 結尾都加了呼叫）
- `migrate_schema.py` 最後一步也會自動觸發（正式環境的舊資料庫剛被 ALTER TABLE 新增這幾個欄位時，值會是預設的 0/False，一定要跑一次重算）
- 後台「系統設定」頁面新增「統計欄位重算」卡片，手動觸發按鈕，供只是透過後台介面手動編輯少量資料時使用（`POST /system-settings/recompute-stats`）

### API 端點變更

- `GET /tcmsp/diseases/public/list` 新增回傳 `target_count`
- `GET /tcmsp/herbs/public/list` 新增回傳 `target_count`、`dark_gene_count`
- `GET /dark-genes/public/list` 的 `has_tcmsp_target` 改讀資料庫欄位，`has_tcmsp_target` 篩選參數現在可以直接下推到 SQL 層（`WHERE`），不用先抓全部再用 Python 篩選
- `GET /dark-genes/public/herb-stats` 的 `dark_gene_count` 改讀資料庫欄位做排序/篩選；`gene_symbols`（比對到的基因清單，用於畫面標籤）因為沒有另外存成關聯表，還是需要現算，但範圍已經限縮在資料庫欄位篩選過的藥材，比全部 502 種藥材下去掃快很多

### 前端變更

- 疾病查詢站、藥材查詢站的左側清單，靶點數 badge 現在**一載入就會顯示**（讀伺服器回傳的統計欄位），不用等「載入全部」或個別項目的詳情資料合併進來才看得到數字
- 藥材查詢站清單新增獨立的「靶點」badge（藍色），跟原本的「成分」badge（綠色）並排顯示
- 暗黑基因查詢站不用改前端，本來就是直接讀 API 回傳的 `has_tcmsp_target` 欄位，後端從即時運算換成讀資料庫欄位對前端完全透明

### 已測試驗證

- 重算腳本本身：本機測試結果跟改版前的即時運算結果完全一致（49 個基因比對到中藥靶點、葛花覆蓋 32 個暗黑基因不重複基因，皆與先前多次驗證過的數字吻合）
- 完整遷移流程測試：模擬正式環境情境（先有舊資料、缺新欄位），執行 `migrate_schema.py` 後確認自動觸發的重算步驟正確執行、統計欄位有被正確填入
- 四個 API 端點與三個相關頁面（藥材站、疾病站、系統設定）伺服器端測試全部通過

### `rules.md` 更新

新增規範：之後任何「查詢站清單要顯示的統計數字」，一律要走「資料庫欄位 + `recompute_stats.py` + 匯入腳本/遷移腳本自動觸發」這套架構，不要再寫「每次請求都即時運算」的邏輯。

### 部署注意事項

**這次一定要跑遷移腳本**，而且順序有意義（遷移腳本本身最後一步就會自動重算，不需要額外手動再跑一次）：

```bash
$env:DATABASE_URL="你的 Neon 連線字串"
python -m app.migrate_schema
```

## v1.29.3 — 2026-08-06（修正藥材查詢站：切換藥材後點「顯示 Selected」關聯圖沒有顯示）

### 問題

使用者回報切換到「艾叶」等非預設藥材後，勾選成分並按「▶ 顯示 Selected」，按鈕文字正確顯示「已套用 10 個」，但關聯網絡圖區域仍停留在最一開始的提示文字「請按上方「▶ 顯示 Selected」查看網絡圖」，圖表沒有真的畫出來。

追查後發現：關聯網絡圖用 `vis.Network` 畫圖，圖表實例存在全域變數 `network` 裡。**切換藥材時，`main.innerHTML` 會整個換掉重建畫面，舊的 `#network` 容器連帶被拿掉、變成 DOM 樹外的孤兒元素——但 `network` 這個全域變數沒有跟著重置**，仍然指向舊藥材那個已經被丟棄的畫布。使用者切到新藥材按「顯示 Selected」時，程式碼判斷 `if(network) network.setData(data)`，誤以為圖表還在，去更新那個使用者看不到的舊畫布，畫面上目前實際顯示的新 `#network` 容器完全沒被碰到，只能維持最一開始的靜態提示文字。

第一次選預設藥材（例如人參）不會踩到這個問題，因為 `network` 一開始是 `null`，會走 `else` 分支正確建立新實例；只有**切換到第二個（含）以後的藥材**才會發生。

### 修正

在 `selectHerb()` 函式一開始（`main.innerHTML` 被覆寫之前）加入：

```js
if (network) { network.destroy(); network = null; }
lastNodes = null; lastEdges = null; lastOptions = null;
```

確保每次切換藥材都會先正確銷毀舊的圖表實例，之後渲染函式判斷 `if(network)` 才不會誤判。全螢幕檢視用的 `fsNetwork` 因為每次渲染都會先 `destroy()` 再重新 `new`，架構上天生沒有這個問題，不需要修改。

### 已測試驗證

用模擬瀏覽器環境重現完整流程：搜尋並選取藥材A、點顯示 Selected（建立第一個 vis.Network 實例並正確綁定到當時畫面上的容器）→ 切換到藥材B（確認舊 `#network` 容器已經是全新元素、藥材A的舊 network 實例已被正確 destroy）→ 點顯示 Selected（確認藥材B建立的新 network 實例正確綁定到畫面上「目前」的 `#network` 容器，不是舊的）。驗證結果全部符合預期。

### `rules.md` 更新

新增這個 bug 模式的教訓記錄：任何會被「切換項目」動作連帶銷毀的 DOM 容器，對應的函式庫實例全域變數必須在切換項目函式一開始就 `.destroy()` 並重置為 `null`，不能只在特殊分支才處理。

## v1.29.2 — 2026-08-06（rules.md 新增規則：後台管理頁面也要單獨檢查四種語系）

### 新增規則

在「上版前的強制檢查規則」裡新增第 4 項：**新增/修改後台管理頁面（CRUD 表單類頁面，例如「疾病中文名稱管理」「中藥行管理」「暗黑基因管理」）時，要分兩層檢查**：

1. **表單本身的固定標籤**（欄位名稱、按鈕文字、說明文字）是靜態 UI 文字，套用四語系規則跟前台頁面完全一樣，一樣要跑覆蓋率掃描
2. **透過表單輸入/編輯出來的內容本身**（例如管理者填入的疾病中文翻譯、店名地址）是動態資料，字典比對法先天不會翻譯這些內容，不管切到哪種語系都會維持原文——這是預期行為，規則裡明確寫清楚這個區別，避免以後誤把「動態資料沒被翻譯」當成漏掉字典詞條去補、或反過來誤以為這是必須修的 bug。

### 說明

這次沒有程式碼變動，純粹是把後台管理頁面（相對於前台展示頁面）的語系檢查範圍跟注意事項寫進規範，確保之後新增這類「有 CRUD 表單」的後台頁面時，不會漏掉表單標籤本身的翻譯，也不會混淆「UI 標籤」跟「使用者輸入資料」這兩種完全不同性質的文字。

## v1.29.1 — 2026-08-06（rules.md 新增規則：上版前新增/修改頁面必須檢查全部四種語系）

### 新增規則

`rules.md` 第五之二節（全站語系機制）新增強制檢查規則：**只要上版有新增頁面或修改既有頁面的文字內容，一律要檢查全部四種語系**（繁中/簡中/英文/韓文），不能只測其中一種就出貨。內容涵蓋：

1. **靜態文字覆蓋率掃描方法**：記錄 v1.29.0 開發時用 Python HTMLParser 模擬瀏覽器 TreeWalker 行為、比對字典缺漏的具體做法，供之後重複使用
2. **功能性模擬測試方法**：用 jsdom + vm 模擬瀏覽器環境，四種語系分別要驗證的重點（tw 不變動／cn 用 OpenCC 轉換且不卡死／en+ko 翻譯完整且兩者一致）
3. **補漏流程**：發現缺漏時的標準處理步驟

同時把舊版「目前字典覆蓋範圍：...少量文字」這段過時描述拿掉（v1.29.0 已經達成 23 個頁面 0 筆缺漏，不再是「少量」），並把 v1.28.2 無窮迴圈 bug 的教訓與防護寫法一併整理進這個章節，跟語系機制放在一起方便查閱（原本散落在 CHANGELOG 裡）。

### 說明

這次沒有程式碼變動，純粹是把「這幾輪語系相關修正裡累積下來的實務經驗」正式寫進開發規範，避免以後新增頁面又重蹈覆轍（漏了語系檢查、字典詞條沒有成對建立、`<b>` 標籤切開文字節點沒處理好、修改 `site-lang.js` 時不小心把無窮迴圈防護拿掉等）。

## v1.29.0 — 2026-08-06（全面系統性補齊英文/韓文翻譯：建立自動掃描工具，一次補齊 310 筆缺漏詞條）

### 背景

前幾版都是使用者截圖回報「哪個頁面哪段文字沒翻譯」，我才逐一補上字典詞條——這種做法治標不治本，只要沒被使用者發現的頁面就會一直是繁體中文。這次改成系統性做法：寫一支掃描工具，比對「23 個掛載全站語系機制的頁面裡所有含中文的靜態文字」跟「字典目前收錄的詞條」，一次找出全部缺漏，而不是等使用者一個一個回報。

### 做法

1. 用 Python 的 HTML parser 模擬瀏覽器 `TreeWalker` 的行為，把每個頁面的 `<script>`/`<style>` 以外的文字節點依標籤邊界切開（例如句子中間包著 `<b>` 標籤，要拆成三段分別比對，跟 `walkAndConvert()` 實際運作方式一致）
2. 比對字典（`i18n-dict.js`）目前收錄的詞條，找出所有「有中文字元、但字典裡沒有」的片段
3. **第一次掃描找到 302 筆缺漏**，逐一撰寫英文/韓文翻譯後寫回字典
4. 補完後**重新掃描**，抓到 2 筆因為文字節點實際組合方式跟預期不同而遺漏的片段（多行文字中間沒有標籤分隔，是同一個文字節點，不能拆開分別建詞條），修正後再次掃描
5. **最終掃描結果：0 筆缺漏**，23 個頁面的靜態 HTML 文字內容全部收錄進字典

### 已測試驗證

- 字典從 91 筆（en/ko 各自）擴充到 402 筆
- 用模擬瀏覽器環境測試 4 個代表性頁面（個人化設定、客戶資料管理、中藥行地理推薦、暗黑基因管理），套用韓文語系後確認翻譯確實生效
- 22 個受影響頁面全部伺服器端測試 200 OK

### 已知限制

- 這次掃描的是**靜態 HTML 內容**，頁面載入後由 JavaScript **動態產生**的文字（例如表格列、Modal 彈出視窗內容、API 回傳資料組合出來的句子）不在這次掃描範圍內，這類文字如果混有動態資料（例如「共 502 種藥材」這種數字），本來就是字典比對法先天無法處理的部分（見 `i18n-dict.js` 檔案開頭的說明），會維持繁體中文顯示，這是已知且無法完全避免的限制，不是這次沒掃到
- 如果之後新增頁面或修改既有頁面的文字，字典需要跟著手動補上——這支掃描工具沒有做成持續整合（CI）自動檢查，只是這次手動跑過一次；如果需要，之後可以考慮把它做成開發流程的一部分

## v1.28.6 — 2026-08-06（補齊「個人化設定」頁面的英文/韓文翻譯字典詞條）

### 問題

使用者回報切換韓文後，「個人化設定」頁面裡「全站語系」「關聯查詢站 CSS 設定」這兩張卡片的標題、說明文字、「深色（預設）」/「淺色」/「目前使用中」都還是繁體中文，沒有跟著翻譯。

這次不是機制壞掉（跟前幾版修的腳本順序、無窮迴圈都無關），單純是**翻譯字典（`i18n-dict.js`）還沒收錄這個頁面的文字**——目前的字典是靠「整段文字完全比對」，沒有比對到的文字就會維持原文，這是正常的字典比對法限制，不是 bug，只是還沒補上而已。

### 修正

補上「個人化設定」頁面所有中文文字的英文/韓文對照，包含兩張卡片的標題與說明文字、深色/淺色主題選項、「目前使用中」標籤。

**特別注意**：頁面上有兩段說明文字中間包著 `<b>` 粗體標籤（例如「套用於**整個後台系統**（所有頁面...）」），瀏覽器會把這種句子拆成三段獨立的文字節點（`<b>` 前、`<b>` 內、`<b>` 後），字典比對法是逐一比對每個文字節點，所以**必須依照同樣的方式把字典詞條拆成三段**，不能整句寫成一個詞條——這次第一次修的時候就漏了這個細節，其中一段說明文字因為詞條沒拆對，完全沒翻譯出來，後來重新測試才抓到，已經修正並重新驗證通過。

### 已測試驗證

用模擬瀏覽器環境重現整個頁面內容，套用韓文語系後確認：兩張卡片標題、兩段包含 `<b>` 標籤的說明文字（含粗體片段本身）、深色/淺色選項、「目前使用中」標籤，全部正確翻譯成韓文，沒有殘留繁體中文片段。

### 提醒

如果之後在其他頁面也發現類似「切換韓文/英文後，某些文字還是繁體中文」的狀況，多半是同樣的原因——字典還沒收錄那段文字，回報給我，我會照這次的方式補上對應詞條（如果句子中間有 `<b>`／`<a>` 等標籤，需要特別注意拆成對應的文字節點段落）。

## v1.28.5 — 2026-08-06（修正 update_local_folder.bat 兩個問題：引號跳脫 bug、直接從 zip 執行沒有防呆）

### 問題 1：`%~dp0` 結尾反斜線觸發 Windows 經典引號跳脫 bug

實際執行出現 robocopy 錯誤「未指定目的地目錄」，來源路徑被印成一長串亂七八糟的字串，裡面混雜了目的地路徑跟所有參數（`/MIR`、`/XD` 等）。

原因：`%~dp0` 這個變數的值**永遠**以反斜線結尾。當它被包進雙引號變成 `"...\tcm_backend\"` 時，Windows 命令列解析規則會把「反斜線緊接著引號」解讀成「跳脫後的字面引號字元」，不是「字串結尾」——這是 Windows 命令列參數解析裡一個很經典、很多人踩過的陷阱。結果就是這個引號沒有真正把字串收尾，後面的目的地路徑跟所有 robocopy 參數全部被吞進同一個參數裡。

修正：在把 `SOURCE` 包進引號之前，先偵測並移除結尾的反斜線（`if "%SOURCE:~-1%"=="\" set "SOURCE=%SOURCE:~0,-1%"`）。

### 問題 2：直接雙擊瀏覽 zip 裡的檔案執行，而不是先完整解壓縮

從使用者提供的錯誤訊息裡，來源路徑是 `...\AppData\Local\Temp\...zip.8b9\tcm_backend`——這代表是在 Windows 檔案總管把 zip 當資料夾瀏覽、直接雙擊裡面的批次檔執行，而不是先「解壓縮全部」到一個真正的資料夾。這種情況下 Windows 只會把使用者點的那一個檔案抽到暫存資料夾執行，`frontend/`、`app/` 等其他要複製的檔案根本不在那裡——就算引號 bug 修好了，複製出來的東西也會不完整，而且**不會跳出任何錯誤訊息**，比現在這個報錯還危險。

修正：批次檔開頭新增偵測，如果執行位置落在 `AppData\Local\Temp` 底下，直接中止並顯示明確的操作說明（先「解壓縮全部」到一個真正的資料夾，再從那個資料夾裡執行）。

### 已測試驗證

- 位元組層級掃描確認檔案 0 個非 ASCII 字元、CRLF 換行符號、括號與引號數量成對
- 用文字模擬驗證修正邏輯：結尾反斜線數量從奇數（1個，會觸發跳脫）變成偶數（0個，正常結束字串）

### 提醒

麻煩這次先照畫面上的說明操作：**右鍵點 zip → 解壓縮全部 → 選一個資料夾 → 打開解壓縮出來的 tcm_backend 資料夾 → 從裡面雙擊 `update_local_folder.bat`**，不要直接在檔案總管瀏覽 zip 內容時點裡面的批次檔執行。

## v1.28.4 — 2026-08-06（修正批次檔中文亂碼導致指令解析失敗的問題）

### 問題

使用者實際執行 `update_local_folder.bat` 出現一連串「不是內部或外部命令」的錯誤，錯誤訊息本身也是亂碼（例如 `'鈭?.git' 不是內部或外部命令`）。

原因：批次檔存成 UTF-8 編碼，靠開頭 `chcp 65001` 切換命令提示字元的編碼頁來正確顯示中文——但這個做法在某些 Windows 環境下不可靠，`chcp` 切換的時機跟批次檔案本身被讀取解析的時機沒有對齊，導致中文註解的位元組被用錯誤的編碼頁去切字，連帶把後面本來是英文的指令行也切壞了（`robocopy ... /XD ".git" ... /NFL /NDL` 這一整行被腰斬成好幾段亂碼字串，每一段都被當成獨立指令去執行，當然全部找不到）。

### 修正

**兩個批次檔全部改成純 ASCII 英文**，註解與提示訊息都改寫成英文，不再依賴 `chcp` 或任何編碼頁設定——ASCII（0~127）在所有編碼頁裡都是同一套字元，不會有這個問題。同時把換行符號統一轉成 Windows 標準的 CRLF（原本是 LF），排除任何潛在的相容性疑慮。

已驗證：兩個檔案位元組層級掃描確認 0 個非 ASCII 字元，`file` 指令確認格式為「DOS batch file, ASCII text, with CRLF line terminators」，括號與雙引號數量成對，功能邏輯與前一版本完全相同（只是所有文字說明改成英文）。

### 提醒

- 這兩個批次檔的**執行時互動文字**（確認提示、完成訊息等）現在是英文，不是中文，這是為了徹底排除編碼問題所做的取捨——功能行為完全不受影響
- 麻煩你重新解壓縮這次的 zip，用新版的 `update_local_folder.bat` 再測一次

## v1.28.3 — 2026-08-06（新增兩個 Windows 批次檔：覆蓋更新本機資料夾、git 一鍵推送）

### 新增內容（工具，非系統功能變動）

- `update_local_folder.bat`：雙擊執行，把「這次解壓縮出來的新版本」用 `robocopy /MIR` 同步覆蓋到 `D:\tcm_backend`，保留 `.git` 資料夾不受影響。執行前會列出來源/目的地路徑並要求輸入 `Y` 確認
- `git_push.bat`：依序執行 `git add .` → `git commit -m "..."` → `git push`。雙擊執行會用批次檔裡預先寫好、對應這次版本的提交訊息；也可以帶自訂訊息執行 `git_push.bat "自訂訊息"`。固定切換到 `D:\tcm_backend` 再執行，路徑不同要自行修改批次檔開頭的 `PROJECT_DIR`
- `README.md` 新增「四之一」章節說明這兩個批次檔的用法與建議流程

### 已測試驗證

這個沙盒環境沒有 Windows/cmd.exe，無法直接執行 `.bat` 檔測試，改用結構性檢查驗證：
- 括號、雙引號數量成對，沒有語法上明顯的缺漏
- 檔案編碼確認為 UTF-8，並在檔案開頭加了 `chcp 65001` 避免中文字在命令提示字元裡顯示成亂碼

麻煩你實際在 Windows 環境測試這兩個批次檔的執行結果，如果有任何錯誤訊息或行為不符預期，回報給我再調整。

### 已知限制

- `update_local_folder.bat` 用 `/MIR` 鏡像同步，除了 `.git` 之外，本機資料夾裡「新版本沒有的檔案」會被刪除——如果你在 `D:\tcm_backend` 裡有自己額外放的本機測試檔案（`.env`、本機 `.db` 等），建議先自行備份，避免被同步刪除
- `git_push.bat` 的預設提交訊息是寫死在批次檔裡的字串，對應**這次交付當下**的版本說明；如果你分好幾次才執行這個批次檔（中間又收到更新的版本），批次檔裡的訊息可能已經不是最新版本的說明，建議善用 `git_push.bat "自訂訊息"` 這個帶參數的用法

## v1.28.2 — 2026-08-06（重大修正：韓文/英文語系導致頁面無窮迴圈當機）

### 問題

使用者回報：切換韓文後，(1) 部分子項目名稱沒有翻譯、(2) 登入後跳出瀏覽器「一個網頁無回應」的錯誤，頁面整個卡死。

追查後發現是 `site-lang.js` 裡的 **MutationObserver 無窮迴圈**：這支腳本用 MutationObserver 監看頁面文字異動（`characterData: true`），一偵測到文字變化就呼叫 `convertText()` 翻譯後**直接賦值回** `node.nodeValue`。問題是「賦值」這個動作本身，不管新值跟舊值是否相同，都會被瀏覽器視為一次新的文字異動，而這正是同一個 observer 在監看的類型——於是「賦值」觸發「異動事件」，「異動事件」又觸發「賦值」，形成無窮迴圈，把主執行緒完全卡死。

這個 bug 平常不容易發現，因為：
- 只有選擇韓文／英文（需要用字典比對翻譯的語系）才會踩到，繁體/簡體（用 OpenCC 轉換）本來就不會被這個路徑影響到規模一樣的迴圈（OpenCC 轉換也有同樣的理論風險，只是因為轉換結果通常穩定收斂沒那麼容易被注意到）
- 只要頁面在語系套用之後，有任何動態插入含中文文字的內容（例如 Dashboard 頁面非同步載入的「主機資訊」「版本資訊」卡片），就會立刻觸發——這也解釋了為什麼使用者看到「部分子項目翻譯成功、部分沒有」：畫面卡死的當下，翻譯到一半就停住了

### 修正

- `walkAndConvert()` 跟 MutationObserver 的回呼函式，全部改成**只在轉換後的文字真的不一樣時才賦值**（`if (converted !== node.nodeValue) node.nodeValue = converted;`）。同一段文字被觀察者攔截後再轉換一次，因為字典裡找不到「已經是韓文的字」對應的詞條，會原樣退回，這時候值沒有變化、不會再賦值，迴圈自然中止
- 同時也在 `characterData` 分支補上跟 `walkAndConvert` 一致的中文字元判斷（`/[\u4e00-\u9fff]/`），避免非中文的文字異動（例如版本號、英數字）也白白跑一次轉換邏輯

### 已測試驗證

用模擬瀏覽器環境重現問題：套用韓文語系後，動態插入含中文的內容（模擬 Dashboard 頁面非同步載入卡片的情境）。修正前這個測試會直接把 Node.js 事件迴圈卡死；修正後 506ms 正常完成，且翻譯結果正確（「主機資訊」→「서버 정보」、「版本資訊」→「버전 정보」），非中文內容（版本號）維持原樣不受影響。

### 重要提醒

這個 bug 是**真正會讓瀏覽器分頁當機**的等級，不是效能變差而已。如果你在測試 v1.28.0／v1.28.1 版本時已經遇過「切韓文後系統卡死」，麻煩重新整理頁面（可能需要強制關閉分頁再重開，卡死的分頁本身可能沒辦法自己恢復），更新到這個版本後再測一次。

## v1.28.1 — 2026-08-06（修正登入頁選韓文/英文後，登入系統畫面沒有跟著翻譯）

### 問題

使用者回報：登入頁選「한국어」，登入後系統畫面（側邊選單等）沒有變成韓文，還是繁體中文。

追查後發現是**腳本載入順序**的問題，跟語言字典內容或後端設定都沒有關係：

- `nav.js` 的 `<script>` 標籤在所有頁面裡都排在 `site-lang.js` 前面
- `nav.js` 最後一行程式碼會檢查 `typeof loadAndApplySiteLanguage === "function"` 再呼叫，但瀏覽器是照 `<script>` 標籤順序同步載入執行——`nav.js` 執行到那一行的當下，`site-lang.js` 根本還沒被載入，這個函式還不存在，`typeof` 判斷失敗，呼叫直接被跳過
- 結果：**全站語系「登入後自動套用」這個機制，從功能一開始存在就沒有真正生效過**，不是只有韓文，繁體/簡體切換的自動套用同樣受影響（使用者如果去「個人化設定」手動點切換是有效的，因為那是使用者點擊當下觸發，所有腳本早就載入完成了；只有「登入後第一次自動套用」這個時機點會失效）

### 修正

`nav.js` 改成輪詢等待 `loadAndApplySiteLanguage` 出現（最多等 3 秒，每 50ms 檢查一次），不再依賴 `<script>` 標籤順序：

```js
(function waitForSiteLangAndApply(retriesLeft) {
  if (typeof loadAndApplySiteLanguage === "function") {
    loadAndApplySiteLanguage();
    return;
  }
  if (retriesLeft <= 0) return;
  setTimeout(() => waitForSiteLangAndApply(retriesLeft - 1), 50);
})(60);
```

已用模擬瀏覽器環境驗證：刻意模擬「`site-lang.js` 晚 200ms 才載入完成」這種比正常情況更糟的時序，輪詢機制依然正確等到函式出現後呼叫，成功把導覽選單文字翻譯成韓文。

### 已知限制（沿用 v1.28.0 的既有設計，非本次修正範圍）

翻譯字典是「整段文字完全比對」，沒辦法翻譯含有動態資料的句子（例如「共 502 種藥材」這種數字混在句子裡的情況），這類句子會維持繁體中文顯示，這是字典比對法本身的限制。

## v1.28.0 — 2026-08-06（新增全站語系：英文、韓文；登入頁語系選單）

### 新增功能

- **全站語系新增英文、韓文**：`site_language` 個人化設定從「繁中/簡中」兩種擴充為「繁中/簡中/英文/韓文」四種
- **登入頁新增「Language」選單**：預設英文，選項含繁體中文/简体中文/한국어。登入頁在使用者驗證身分前無法呼叫需要登入的 API，所以語系選擇先存在瀏覽器 `localStorage`，**登入成功後自動呼叫 API 把選定的語系存回這個帳號的個人化設定**，之後登入系統其他頁面都會沿用這個語系
- 個人化設定頁面的語系選項同步擴充為四種

### 技術做法：字典比對翻譯

繁中/簡中沿用既有的 OpenCC 字形轉換（不需要字典，任何句子都能轉）。**英文/韓文是完全不同的技術問題**——不是字形轉換而是真正翻譯，OpenCC 完全幫不上忙，改用新增的 `frontend/js/i18n-dict.js` 翻譯字典，逐一比對頁面上的文字節點，整段文字完全對應字典詞條就替換，比對不到的維持繁體中文原文。

**目前字典涵蓋 79 個詞條**：導覽選單全部項目（Dashboard、角色管理、暗黑基因管理...）、登入頁、常用操作按鈕（新增/編輯/刪除/儲存/搜尋等）、Dashboard 少量文字。

### 已測試驗證

- 後端：`PUT /user-preferences/site_language` 四種語系值都能正確設定，不支援的語系代碼（測試用 `fr`）會被擋（400），**這次也順便修正了一個既有漏洞**——原本這個 API 完全沒有驗證傳入值是否合法，任何字串都會被接受寫入資料庫
- 前端：用模擬瀏覽器環境驗證字典比對機制，確認英文/韓文轉換都正確運作，且「共 502 種藥材」這種混著動態數字的句子正確維持繁體中文原文（不會被誤判斷或轉出破碎的翻譯）

### 已知限制（範圍說明）

**這次沒有做到「全站所有功能名稱、欄位名稱」100% 覆蓋**，這是刻意的範圍控制，原因：全站有數十個頁面、每個頁面可能有數十個欄位標籤，全部人工翻譯是遠超這次工作量的大工程。目前優先覆蓋使用者最常看到的地方（導覽選單、常用按鈕、登入頁），像「客戶資料管理」表單裡「病患識別碼」「證件類型」這類特定頁面的細節欄位，目前還是顯示繁體中文。之後要擴充覆蓋率，直接持續往 `i18n-dict.js` 加詞條即可，不需要更動核心轉換邏輯。

另外，四個查詢站頁面（`tcmsp_query.html`／`disease_query.html`／`darkgene_query.html`／`ginseng_darkgene.html`）目前**沒有**串接這個全站語系機制，因為它們已經有自己獨立的繁/簡切換（針對藥材/疾病/基因資料本身），這次沒有處理這幾頁的導覽列跟按鈕文字要不要額外響應英文/韓文設定，避免跟既有機制互相干擾。

### `rules.md` 更新

新增「五之二、全站語系機制」章節，記錄 tw/cn 跟 en/ko 是兩套完全不同的技術手法、字典比對的限制、以及新增頁面要怎麼接上這個機制。

### 部署注意事項

沒有新增資料庫欄位，不需要跑遷移指令（`site_language` 沿用既有的 `user_preferences` 通用機制）。

## v1.27.3 — 2026-08-06（暗黑基因查詢站、藥材與暗黑基因關聯，同步套用分階段載入修正）

### 補齊範圍

v1.27.2 只修了藥材/疾病查詢站的「載入全部」流程。這次把同一套 `yieldToUI()` 分階段處理機制，一併套用到另外兩個查詢站的**初始載入流程**（雖然這兩站沒有「載入全部」按鈕，但初始載入同樣是「API 抓資料 → 立刻同步渲染整份清單」黏在一起執行，一樣有卡住的疑慮）：

- **暗黑基因關聯查詢站**（`darkgene_query.html`）：抓取 1245 個基因清單後，中間加一個「正在統計 1245 個基因並建立清單...」的過渡畫面 + `yieldToUI()`，再進行整份清單的渲染
- **藥材與暗黑基因關聯**（`ginseng_darkgene.html`）：抓取 502 種藥材清單後，中間加一個「正在建立藥材清單...」的過渡畫面 + `yieldToUI()`，再渲染清單、自動選取人參

### 已測試驗證

兩個頁面語法檢查通過，伺服器端頁面載入測試（200 OK）通過。

### 部署注意事項

沒有動資料庫，不需要跑遷移指令。

## v1.27.2 — 2026-08-06（修正「載入全部」下載完成後看起來卡住的問題，藥材/疾病查詢站）

### 問題

使用者回報疾病查詢站「載入速度慢，卡在統計靶點數量」。追查後發現：下載進度條本身沒問題（v1.17.1 已經修過 CORS，能正確顯示下載百分比），**問題出在下載完成之後**——`JSON.parse(11MB文字)` → 重建索引（`rebuildIndexes()`，要從幾千筆關聯資料建立好幾個 Map）→ 重新繪製整份 564 筆清單（含統計每筆疾病的靶點數量），這三個步驟全部是同步執行，中間沒有讓瀏覽器喘息重繪的機會。就算程式碼裡有呼叫 `setProgress()` 更新進度文字，因為 JavaScript 是單執行緒，瀏覽器要等整段同步程式碼執行完才有機會重繪畫面，所以使用者看到的是：進度條卡在 100% 不動，過一陣子畫面才突然跳出結果，體感上像當機。

藥材查詢站也有一樣的結構性問題（下載完後同樣是 `JSON.parse` → 重建索引 → 重繪 502 筆清單），這次一併修正。

### 修正

三個處理階段之間插入 `await yieldToUI()`（讓出主執行緒給瀏覽器重繪），並且每個階段各自更新進度文字：

1. 「下載完成，正在解析資料...」→ `JSON.parse`
2. 「正在建立索引...」→ `rebuildIndexes()`
3. 「正在統計成分/靶點/疾病數量並更新清單...」→ 重繪清單

已用 Node.js 模擬驗證：三個階段的進度訊息確實各自有獨立的時間點（0ms／43ms／52ms），不是全部黏在一起最後才顯示，證實 `yieldToUI()` 機制有效。

暗黑基因查詢站、藥材與暗黑基因關聯頁面沒有「載入全部」這個流程，不受影響。

### `rules.md` 更新

查詢站標準功能清單新增第 6.5 項，記錄這個「下載完成後處理階段要分段 yield」的規則，避免以後新增查詢站類頁面又踩到同樣的體感當機問題。

## v1.27.1 — 2026-08-06（修正藥材查詢站：選過藥材後點「載入全部」拋出例外）

### 問題

使用者回報點「載入全部」按鈕時遇到 `TypeError: Cannot read properties of null (reading 'style')`。追查後發現：`#loadProgressWrap`／`#loadProgressBar`（下載進度條）被巢狀放在 `#emptyState` 裡面，而 `#emptyState` 又被巢狀在 `#main` 容器裡。頁面一載入就會自動選取人參（或後台設定的預設藥材），`selectHerb()` 會把 `#main.innerHTML` 整個換成藥材詳情內容，這個動作連帶把巢狀在裡面的進度條元素一起從畫面上刪除。

也就是說，**這個 bug 從頁面一開始自動選取人參的那一刻就已經生效**——只要有選過任何一個藥材（包括自動選取的預設藥材），進度條元素就永久消失了，之後點「載入全部」想更新進度時，操作的是一個已經不存在的元素，直接拋出例外中斷。

這是這個 session 第三次踩到同一種結構性問題（v1.17.2 疾病查詢站、v1.20.1 暗黑基因查詢站都是同樣的巢狀刪除模式），差別在於這次不是「空白狀態提示」，而是「進度條」——代表這個規則要涵蓋的範圍比原本記錄的更廣。

### 修正

- 把 `#loadProgressWrap`／`#loadProgressBar` 從 `#emptyState` 裡面移出來，改放在左側面板「載入全部」按鈕正下方（`#herbPanel` 底下，固定位置，不會被任何 `innerHTML` 覆寫動作波及）
- 已透過程式碼靜態檢查確認：所有會覆寫 `main.innerHTML` 的地方（共 4 處）都限定在 `#main` 容器範圍內，現在進度條元素已經在這個範圍之外，結構上保證不會再被刪除
- `rules.md` 規則擴大範圍：不是只有 `#emptyState` 要注意，任何「需要跨次渲染持續存在的元素」（進度條、統計文字、固定按鈕）都適用同一條規則

## v1.27.0 — 2026-08-06（中藥行地理推薦：九種排序功能按鈕、打卡、收藏、店家後台）

### 新增功能（按鈕列，九種排序模式）

- **📍 距離**：沿用既有依使用者定位排序
- **⭐ 綜合評價**：三種模式可切換——總星等／平均星等／加權星等（貝式平均，評價數少的店不會因為一兩則五星就衝到第一名）
- **✅ 到訪次數**：新增**打卡功能**，可選填本次消費金額；排序可切換「全部使用者」或「只看我自己」
- **🔥 熱門程度**：綜合瀏覽、收藏、分享、導航點擊、到店打卡次數的加權分數
- **🕐 營業狀態**：正在營業／即將打烊（30分鐘內）／即將開門（30分鐘內）優先，依 `opens_at`/`closes_at` 結構化時間欄位判斷
- **💰 價格水準**：依打卡時填寫的消費金額算平均消費，可選由低到高或由高到低
- **🏷️ 促銷優惠**：依折扣幅度排序，**新增店家後台功能**，可輸入折扣百分比、優惠說明、優惠期限
- **🆕 最新開幕**：依開幕日期（店家可自行填寫）或平台收錄日期排序
- **💡 個人化推薦**：簡化的規則式排序（優先顯示尚未收藏但熱門度高的店，其次是已收藏的店），**不是**完整的協同過濾/機器學習模型
- **🔍 相關程度**：關鍵字比對店名/地址/簡介

### 新增：路線規劃、分享、收藏

- 「🧭 路線規劃」按鈕開啟 OpenStreetMap 導航連結並記錄點擊次數
- 「📤 分享」按鈕使用瀏覽器原生分享（`navigator.share`），不支援的瀏覽器改成複製連結
- 「🤍/❤️ 收藏」可切換收藏狀態

### 資料庫變更

- `pharmacies` 表新增：`opens_at`／`closes_at`（結構化營業時間）、`view_count`／`favorite_count`／`share_count`／`nav_click_count`（熱門度統計）、`opening_date`、`discount_percent`／`discount_description`／`discount_valid_until`（店家促銷功能）
- 新增資料表 `pharmacy_checkins`（打卡紀錄，含消費金額）、`pharmacy_favorites`（收藏）

### 已測試驗證

TestClient 完整流程：更新營業時間 → 營業狀態正確判斷為「營業中」→ 打卡兩次（含消費金額）→ 平均消費正確計算 → 收藏 → 瀏覽/分享/導航追蹤 → 單店詳情統計數字全部正確 → 九種排序模式全部正常回應 → 相關程度關鍵字搜尋正確篩出符合的店家 → 促銷優惠設定正確。

### 安全設計說明：「相關程度」排序範圍縮減

原始需求裡「相關程度」希望串接「登入者自行上傳的 DNA 資料」與「店家商品的疾病關聯」做匹配。**這次沒有做這塊**，原因：這會把「一般消費者自助上傳基因資料」跟「導向特定店家購買特定商品」的商業推薦綁在一起，風險層級比目前系統既有的「醫療團隊管理病患DNA資料、附帶明確研究免責聲明」更高——等於是沒有醫師/專業人員把關的情況下，直接依個人基因資料引導消費行為。這次「相關程度」先做了純關鍵字比對（店名/地址/簡介），DNA 關聯部分建議另外討論設計，確保有適當的免責聲明與資料保護機制再實作。

### 部署注意事項

需要重新執行遷移腳本（回填 `pharmacies` 表的新欄位，正式環境是舊 schema，需要真的執行到 ALTER；`pharmacy_checkins`／`pharmacy_favorites` 屬於全新資料表，`create_all` 會自動建立）：

```bash
$env:DATABASE_URL="你的 Neon 連線字串"
python -m app.migrate_schema
```

## v1.26.1 — 2026-08-06（中藥行地理推薦：最近N家篩選、地圖彈出視窗顯示評價、擴充樣本資料）

### 新增功能

- **「只顯示最近 N 家」**：新增 0~100 數字輸入框，需先取得使用者位置（依距離排序後）才能套用，輸入 0 或留空代表不限制，篩選後的清單同步套用到地圖標記與下方卡片清單
- **地圖標記彈出視窗顯示綜合評價與留言**：點地圖上的圖標，彈出視窗除了名稱/地址，還會顯示星等評分（★☆圖示）+ 評價則數，以及最多 5 則使用者評價留言（含評分星數、帳號、留言內容）
- **擴充樣本資料**：中藥行測試資料從 3 家擴充到 12 家，涵蓋雙北、桃園地區，讓地圖與「最近N家」功能有更多資料可以測試。改成依名稱 upsert（原本是「資料表為空才建立」，正式環境已經有舊的 3 筆，改成 upsert 才能讓新增的樣本補進去，不會重複也不會動到既有資料）

### 已測試驗證

新環境從 0 筆種子出 12 筆；模擬已有舊 3 筆資料的正式環境重跑 seed 邏輯，正確補齊到 12 筆且無重複名稱。

### 部署注意事項

沒有新增資料庫欄位，不需要跑遷移指令，重新部署後端服務、下次啟動時 seed 邏輯就會自動補齊新樣本資料。

## v1.26.0 — 2026-08-06（DNA 資料匯入、測試資料產生、暗黑基因比對、候選中藥、報告、病患排行）

### 新增功能（六大項）

1. **DNA 資料匯入 + CRUD + 多次匯入比較**：新頁面 `dna-data.html`。新增資料表 `specimens`（檢體）、`dna_import_batches`（匯入批次）、`variants`（變異，簡化整合品質/解讀欄位）。支援手動新增或上傳 CSV 匯入，同一病患可以有多筆批次，`GET /dna/patients/{id}/compare` 比對任兩個以上批次之間變異的異同（有無/是否全部批次都有）
2. **DNA 測試資料產生**：新頁面 `dna-test-data.html`，可複選多位病患，勾選是否要讓產生的變異包含暗黑基因（可調整比例）與每人變異筆數，批次來源標記為 `synthetic` 跟真實匯入區分
3. **DNA 資料檢測（暗黑基因比對）**：變異清單裡命中暗黑基因清單的列，前端用**紅字標註**，`VariantOut.is_dark_gene` 由後端即時比對 `gene_symbol` 與 `dark_genes.hugo_symbol`
4. **中藥材建議**：`GET /dark-genes/patient-herb-suggestions/{patient_id}`，彙總病患命中的所有暗黑基因，沿用既有的機制關聯比對邏輯算出候選中藥（依覆蓋基因數排序）
5. **DNA 檢測報告**：新頁面 `dna-report.html`，單一病患報告整合命中基因清單＋候選中藥，**畫面最上方有醒目的橘色警語橫幅**，明確說明「非臨床診斷書、非用藥處方，需醫師與中醫師共同審核」，支援瀏覽器列印/另存 PDF
6. **病患基因統計排行**：新頁面 `patient-dark-gene-ranking.html`，排行哪位病患命中最多不重複的暗黑基因，欄位可點擊排序

### 已測試驗證

TestClient 完整流程測試：新增病患 → CSV 上傳匯入（含真實 BRCA1 變異）→ 變異清單正確標記 `is_dark_gene` → 病患暗黑基因摘要正確 → 候選中藥建議正確（BRCA1+BCL2 測試案例得到川牛膝、葛花、女贞子、葛根等候選藥材）→ 多次匯入比較的 presence/in_all_batches 邏輯驗證通過。伺服器端測試：4 個新頁面全部 200、CSV 上傳真實流程（BRCA1 變異正確標記為暗黑基因）、遷移腳本正確回填 37 筆功能項目。

### 安全設計說明

第 4、5 項（中藥材建議、檢測報告）沿用「機制關聯查詢」的安全框架——呈現的是研究層級的資料關聯（TCMSP 成分-靶點預測資料），**不是臨床療效證據**，明確標示「非醫療建議」。

**這次明確跳過沒做**：正式的「配方審核流程」（中醫師簽核機制，`formula_recommendations` 表 + draft/approved 狀態機）。目前候選中藥是即時計算的參考資訊，還沒有「醫師核准後才能被視為正式建議」的審核閘門。**這是正式對外提供任何治療建議前必須補上的下一步**，屬於目標三/四已經記錄多次的法規限制範疇。

這四個新頁面（DNA 資料管理/測試資料產生/檢測報告/病患排行）目前設定為**僅後台可見**（`show_frontend=False`），因為含有病患個資與基因資料，不開放前台一般使用者存取。

### 部署注意事項

需要重新執行遷移腳本（回填新增的 F3-7～F3-10 功能項目；`specimens`／`dna_import_batches`／`variants` 屬於全新資料表，一般啟動流程的 `create_all` 會自動建立，不需要遷移腳本處理）：

```bash
$env:DATABASE_URL="你的 Neon 連線字串"
python -m app.migrate_schema
```

## v1.25.0 — 2026-08-06（新增「中藥暗黑基因覆蓋統計」，找出覆蓋最多癌症基因的藥材）

### 新增功能

新頁面 `darkgene-herb-ranking.html`（F3-6），跟「暗黑基因統計」（以基因為主）方向相反，這次**以藥材為主**：統計每種藥材連結到幾個「不重複」的暗黑基因，排出「哪種中藥能對抗最多癌症基因」的排行榜。

- KPI 卡片：有暗黑基因關聯的藥材數、單一藥材最多覆蓋基因數、覆蓋最多的藥材
- 排行榜表格：排名／中文名稱／拼音／English Name／覆蓋暗黑基因數／比對到的基因清單（標籤形式），**每個欄位都能點擊排序**
- 點藥材列開新分頁跳到藥材查詢站看完整說明（沿用既有的 `?herb=` 深連結）
- 搜尋框可以用中文/拼音/英文篩選

### 後端

新增 `GET /dark-genes/public/herb-stats`，演算法分三步驟避免對 502 種藥材各自查一次資料庫：
1. 建立「靶點 → 命中的暗黑基因集合」（重用既有比對邏輯，約 50~100 個靶點會命中）
2. 掃一次「成分-靶點」關聯，換算成「成分 → 命中的暗黑基因集合」
3. 掃一次「藥材-成分」關聯，把基因歸到藥材身上（用集合自動去重，同一基因透過多個成分/靶點連到同一藥材只算一次）

### 測試結果

本機測試耗時約 0.26 秒，483 種藥材有暗黑基因關聯。**覆蓋最多的是葛花，連結到 32 個不重複的暗黑基因**（包括 BRCA1、TP53、MYC、EGFR、PTEN 等重要癌症基因），其次是麻黄（27 個）、山豆根與葛根（各 26 個）。

### 部署注意事項

需要重新執行遷移腳本（回填新增的 F3-6 功能項目）：

```bash
$env:DATABASE_URL="你的 Neon 連線字串"
python -m app.migrate_schema
```

## v1.24.1 — 2026-08-06（暗黑基因統計改為以 Hugo Symbol 為主要欄位）

### 變更

原本統計頁面第一層是依 Gene Type（ONCOGENE/TSG...）分組彙總，改成**以每個基因（Hugo Symbol）為主要清單**，逐一列出每個基因的靶點統計與中藥統計：

- 新增 `GET /dark-genes/public/gene-stats`：逐基因統計靶點數（`target_count`）與候選中藥數（`herb_count`），預設只列出有中藥靶點的 49 個基因（`only_with_target=false` 可以看全部 1245 個）
- 統計表格欄位：Hugo Symbol／Gene Type／靶點統計／中藥統計／Entrez ID，**每欄都能點擊排序**
- 點「靶點統計」數字 → 下鑽看這個基因比對到的靶點清單
- 點「中藥統計」數字 → 下鑽看候選中藥清單，點中藥可以開新分頁看該藥材完整說明（沿用 v1.24.0 已經做好的 `tcmsp_query.html?herb=` 深連結）
- 新增搜尋框（Hugo Symbol/別名）與「只顯示有中藥靶點的基因」勾選框
- KPI 總覽卡片沿用原本的 Gene Type 彙總端點（`/dark-genes/public/stats`）

### 測試結果

本機測試：MS4A1 是關聯最廣的基因（3 個靶點、294 種候選中藥），逐基因統計端點耗時約 0.3 秒（含全部 49 個基因逐一計算中藥數）。

### 部署注意事項

沒有新增資料庫欄位，不需要跑遷移指令。

## v1.24.0 — 2026-08-06（新增暗黑基因統計頁面，三層下鑽 + 欄位排序）

### 新增功能

新頁面 `darkgene-stats.html`（F3-5），統計 1245 個暗黑基因裡有多少比對到 TCMSP 中藥靶點：

- **統計總覽**：KPI 卡片（基因總數/有中藥靶點/無中藥靶點）+ 依 Gene Type 分組的表格（ONCOGENE/TSG/ONCOGENE_AND_TSG/NEITHER/INSUFFICIENT_EVIDENCE/未分類），每欄都有百分比
- **欄位可點擊排序**：點表格欄位名稱（基因類型/總數/有中藥靶點/無中藥靶點/有靶點比例）就能排序，再點一次反向排序，會顯示排序方向箭頭
- **三層下鑽**：
  1. 點統計表格裡藍色的數字（例如「ONCOGENE 有中藥靶點：27」）→ 顯示符合條件的基因清單
  2. 點清單裡任一個基因 → 顯示這個基因比對到的候選藥材清單（依關聯成分數排序）
  3. 點候選藥材 → 開新分頁跳到「TCMSP 藥材關聯查詢站」，直接顯示該藥材的完整詳細說明與關聯網絡圖
- 每一層下鑽都有麵包屑導覽可以回上一層

### 藥材查詢站支援網址參數深連結

為了讓統計頁面點藥材能直接跳轉到正確的藥材，`tcmsp_query.html` 新增支援 `?herb=<herb_id>` 網址參數，優先順序：網址參數 > 後台設定的預設藥材 > 退回搜尋人參。

### 後端

新增 `GET /dark-genes/public/stats`，依 Gene Type 分組統計，重用既有的「單詞索引」比對演算法（不用每個基因重新掃一次全部靶點），本機測試耗時約 0.18 秒。基因清單下鑽直接沿用既有的 `GET /dark-genes/public/list`（`gene_type` + `has_tcmsp_target` 篩選參數，v1.20.0 就有了）。

### 部署注意事項

需要重新執行遷移腳本（回填新增的 F3-5 功能項目）：

```bash
$env:DATABASE_URL="你的 Neon 連線字串"
python -m app.migrate_schema
```

## v1.23.0 — 2026-08-06（新增「藥材與暗黑基因關聯」頁面，預設呈現人參）

### 新增功能

新前台頁面 `ginseng_darkgene.html`（F3-4），做「暗黑基因-靶點關聯」的反向查詢：不是從基因出發找藥材，而是**從藥材出發，找出這個藥材可能作用在哪些癌症基因上**。

- 頁面載入時**自動選中人參**（Panax Ginseng），左側也可以搜尋切換成其他 502 種藥材裡的任何一種
- 關聯層級：Herb → Related Ingredients → Related Targets → Matched Dark Genes，比對邏輯跟「暗黑基因-靶點關聯」是同一套演算法（基因符號/別名比對 TCMSP 靶點名稱），只是方向相反
- 完整比照查詢站標準功能清單（`rules.md` 第四之一節）：繁簡切換、淺色/深色主題、全螢幕放大、顯示層級勾選（Ingredients/Targets/Genes 逐層鎖定）、節點數量上限讀取後台設定、欄寬與高度可拖曳調整、版面命名儲存、JSON/CSV 下載

### 後端

- 新增 `GET /dark-genes/herb-links/{herb_id}`：給一個藥材，回傳它的成分、連結到的 TCMSP 靶點（每個靶點附上比對到的暗黑基因）、比對到的暗黑基因彙總清單（依關聯靶點數排序），以及完整的關聯邊資料供前端畫網絡圖
- 本機測試：人參（herb_id=336）190 個成分、196 個靶點，比對到 **9 個暗黑基因**（SRC、TP53、BAX、MS4A1、RUNX1T1、AKT1、RELA、RHOA、PTEN），查詢耗時約 0.08 秒

### 測試

已透過模擬瀏覽器環境的自動化測試驗證：頁面自動載入人參且資料正確顯示、全螢幕開關正常、顯示層級勾選正確影響 KPI 與網絡圖、連續切換 5 種不同藥材沒有中斷。

### 部署注意事項

需要重新執行遷移腳本（回填新增的 F3-4 功能項目）：

```bash
$env:DATABASE_URL="你的 Neon 連線字串"
python -m app.migrate_schema
```

## v1.22.0 — 2026-08-06（查詢站節點上限改為後台可設定參數、暗黑基因站補齊顯示層級與版面調整功能）

### 1. 網絡圖節點數量上限改為後台可設定參數

之前寫死在前端程式碼裡的數字（例如 v1.21.1 把預設值從 3 改成 15），改成透過「系統設定」頁面的數字輸入框設定，三個查詢站共用：

- 新增 `GET/PUT /system-settings/graph-limits`（`graph_limit_level1/2/3`，對應網絡圖第一/二/三層節點數量上限，數值需介於 1~9999）
- 系統設定頁面新增「查詢站關聯網絡圖：節點數量上限預設值」卡片，直接輸入數字即可，不用改程式碼重新部署
- 三個查詢站頁面載入時都會抓這組設定，動態把管理者設定的數字加進下拉選單並預設選中（`ensureOptionAndSelect()`），使用者仍可以自行在畫面上臨時調整

### 2. 暗黑基因關聯查詢站補齊「顯示層級」勾選

- 新增 Related Targets／Related Ingredients／Related Herbs 顯示層級勾選（跟疾病查詢站同一套邏輯），逐層關係，關掉上層自動關閉並鎖定下層
- 主畫面與全螢幕 Modal 各自一份勾選框，共用同一份 `layerVisibility` 狀態，改變任一邊都會同步套用到兩邊的網絡圖、表格、KPI 統計

### 3. 疾病查詢站／暗黑基因查詢站補齊欄寬與高度可拖曳調整

比照藥材查詢站原本就有的功能，這次移植過去：

- 表格欄寬可拖曳調整（`.col-resizer`），切換分頁籤各自記住欄寬
- 網絡圖／表格區塊之間的高度比例可拖曳調整（`#netResizeHandle`）
- 版面配置可以命名儲存、套用、刪除（存在瀏覽器 `localStorage`，每個查詢站用不同的儲存鍵值，不會互相覆蓋）

### 4. 完整測試

已透過模擬瀏覽器環境的自動化測試驗證（以 BRCA1 為例）：關閉「Related Herbs」層級後 KPI 與網絡圖正確歸零、關閉「Related Targets」正確連動鎖定下層、欄寬拖曳把手正確產生、版面儲存/套用正常運作、連續切換 5 個不同基因沒有中斷。

### 5. `rules.md` 更新為完整的查詢站標準功能清單

新增第 8~11 項（顯示層級勾選、欄寬拖曳、高度拖曳、版面儲存）與「後台可設定的查詢站參數」說明，並記錄「節點數量上限不應該寫死在前端程式碼」這個已經真實發生過的教訓（v1.21.1 的 BRCA1 案例）。

### 部署注意事項

需要重新執行遷移腳本（`system_settings` 資料表已存在，這次沒有新增資料庫欄位，不過保險起見還是建議跑一次確保功能項目回填正常）：

```bash
$env:DATABASE_URL="你的 Neon 連線字串"
python -m app.migrate_schema
```

## v1.21.1 — 2026-08-06（修正暗黑基因查詢站關聯圖節點數量寫死，補上可調整選單）

### 問題

使用者實測 BRCA1（1 個靶點、1 個成分、12 種關聯藥材）發現網絡圖只畫出 3 種藥材。原因是 v1.20.0 建立這個頁面時，每層節點顯示數量是寫死的（每個成分最多只畫 3 個藥材），沒有比照藥材/疾病查詢站做成可調整的下拉選單——這正是 v1.21.0 稽核清單裡漏掉的一項。

### 修正

- 工具列新增「網絡圖每層最多顯示」三個下拉選單（靶點/成分/藥材），`buildGraphData()` 改讀取選單值，不再寫死數字
- 藥材上限預設值調高到 15（原本 3），一般情況下不用手動調整就能看到完整關聯；仍保留「全部」選項因應極端情況
- 已用模擬 BRCA1 真實資料驗證：KPI 顯示 12 種藥材，網絡圖預設也能完整畫出全部 12 個藥材節點
- `rules.md` 的查詢站標準功能清單補上這一項（節點數量可調整），並註明這個教訓：**寫死的顯示上限，在關聯數量剛好不多的情況下反而會誤導使用者覺得「資料只有這麼少」**

## v1.21.0 — 2026-08-06（暗黑基因查詢站補齊語系/主題/全螢幕功能，並建立查詢站標準功能清單）

### 稽核結果

逐項比對三個查詢站（藥材／疾病／暗黑基因）的功能，發現「暗黑基因關聯查詢站」（v1.20.0 剛建立）缺少另外兩站已有的三項功能：語系切換、淺色/深色主題、網絡圖全螢幕放大。

### 補齊功能（暗黑基因關聯查詢站）

- **繁體/簡體語系切換**：搜尋欄旁新增語系下拉選單，比照另外兩站的 OpenCC 即時轉換做法
- **淺色/深色主題切換**：頂部導覽列新增「🌗 切換淺色/深色」，串接個人化設定的查詢站配色偏好；新增 `body[data-page-theme="light"]` 淺色 CSS 覆寫
- **網絡關聯圖全螢幕放大**：新增「⛶ 放大檢視關聯圖」按鈕與全螢幕 Modal，可點節點看詳情（基因/靶點/成分/藥材），選取項目變更時全螢幕畫布會同步更新
- 已透過模擬瀏覽器環境的自動化測試驗證：選取基因、全螢幕開關、連續切換 5 個不同基因、語系切換，全部正常運作無中斷

### 新增：查詢站類頁面標準功能清單

在 `rules.md` 新增「查詢站類頁面標準功能清單」章節，列出 7 項所有查詢站都必須具備的功能（語系切換、主題切換、個人化設定連結、全螢幕放大、選取變更同步全螢幕畫布、JSON/CSV 下載、空白狀態提示的正確結構），做為之後新增查詢站類頁面的驗收標準，避免再發生「新頁面漏做別的頁面已有的功能」的情況。

## v1.20.1 — 2026-08-06（修正暗黑基因查詢站：點選第二個以後的基因沒反應）

### 問題

跟 v1.17.2 修過的疾病查詢站是**同一種 bug**：`#emptyState`（「請從左側選擇一個基因」提示）被巢狀放在 `#geneHeader` 裡面。第一次選擇基因時，程式碼會把 `#geneHeader` 的內容整個換成基因標題，這個動作連帶把巢狀在裡面的 `#emptyState` 一起從畫面上刪除。之後每次呼叫 `selectGene()`，第一步都會嘗試「隱藏 `#emptyState`」，但這個元素已經不存在，直接拋出例外中斷——後面真正負責更新 KPI、表格、網絡圖的程式碼完全沒機會執行。這解釋了使用者回報「點選 BRCA1 之後右側沒有正確顯示」的現象（第一個點的基因是 A1CF，之後點 BRCA1 就是「第二次點選」，正好踩到這個 bug）。

### 修正

- 把 `#emptyState` 從 `#geneHeader` 裡面移出來，改成平行的兄弟元素，覆寫 `#geneHeader` 不會再牽連到 `#emptyState`
- 已透過模擬瀏覽器環境的自動化測試驗證：依序點擊 10 個不同基因，全部正確更新畫面，不再中斷

### 提醒

這是這個 session 第二次出現同一種結構性 bug（`#emptyState` 巢狀在會被覆寫的容器裡），之後新增查詢站類頁面時，`#emptyState` 應該一律設計成跟內容容器（`#xxxHeader`／`#bodyWrap`）平行的兄弟元素，不要巢狀在裡面，已記錄在此提醒未來開發注意。

## v1.20.0 — 2026-08-06（暗黑基因關聯查詢站：比照藥材/疾病查詢站的呈現方式）

### 問題

v1.19.1 把「基因-靶點關聯」做成 Modal 彈出視窗，跟藥材/疾病查詢站「左側清單、右側說明與關聯」的呈現方式不一致。

### 修正：新增專屬查詢站頁面

新頁面 `darkgene_query.html`（F3-3，前台功能），完全比照 `tcmsp_query.html`／`disease_query.html` 的視覺風格與版面配置：

- **左側**：1245 個暗黑基因清單，可搜尋基因符號/別名，新增「**只顯示有中藥靶點的基因**」勾選框（篩選出目前比對到 49 個基因）
- **右側**：
  - 基因說明：Hugo Symbol、Gene Type 標籤、Entrez Gene ID、別名、「是否有中藥靶點」狀態標籤
  - **串連 OncoKB**：「在 OncoKB 查看完整基因說明」按鈕，連到 `https://www.oncokb.org/gene/{symbol}/somatic`
  - KPI 統計卡片、Gene → Targets → Ingredients → Herbs 網絡關聯圖、分頁籤表格（Related Targets／Ingredients／Herbs）、JSON/CSV 下載
  - 畫面上明確標示「機制層級資料關聯，不是臨床療效證據」的警語

### 後端調整

- `GET /dark-genes/public/list` 新增 `has_tcmsp_target` 欄位（是否比對到 TCMSP 靶點）與對應篩選參數，用「單詞索引」一次算完 1245 個基因的比對結果（不用每個基因各自重新掃 1751 個靶點），本機測試整份清單含比對結果耗時約 0.2 秒
- `GET /dark-genes/{gene_id}/tcmsp-links` 補上 `ingredient_target`、`herb_ingredient` 原始關聯邊資料，供前端畫網絡圖使用

### 部署注意事項

需要重新執行遷移腳本（回填新增的 F3-3 功能項目）：

```bash
$env:DATABASE_URL="你的 Neon 連線字串"
python -m app.migrate_schema
```

## v1.19.1 — 2026-08-06（新增「暗黑基因-靶點關聯」功能）

### 新增功能

在「暗黑基因管理」頁面每一列新增「靶點關聯」按鈕，點下去會：

- 拿基因符號（含 Gene Aliases 別名）去比對 TCMSP 資料庫裡 1751 個靶點的名稱（例如 `KRAS` 比對到 `GTPase KRas`、`TP53` 透過別名 `P53` 比對到 `Cellular tumor antigen p53`）
- 找出比對到的靶點後，往下追蹤該靶點連結的成分、再往下追蹤這些成分來自哪些藥材，列出**候選藥材清單**（依關聯成分數排序）
- 新增後端端點 `GET /dark-genes/{gene_id}/tcmsp-links`

畫面上明確標示這是「機制層級的資料關聯（成分-靶點預測資料），不是臨床療效證據」，僅供研究篩選參考，呼應目標三/四已記錄的「正式提供醫療建議前需要醫師/專業人員審核」規劃。

### 已測試驗證的比對結果（供參考）

- KRAS → 1 個靶點、1 個成分、8 種候選藥材
- BCL2 → 1 個靶點、4 個成分、102 種候選藥材
- PTEN → 1 個靶點、7 個成分、339 種候選藥材
- MYC → 1 個靶點、12 個成分、258 種候選藥材

### 已知限制

- 比對邏輯是「基因符號/別名」與「靶點英文全名」的字面比對（全字比對，非子字串），TCMSP 靶點名稱是蛋白質全名（例如 "Serine/threonine-protein kinase mTOR"）而非統一的基因符號格式，所以只有名稱裡剛好包含符合的字詞才會比對到——目前 1245 個暗黑基因裡約 36 個能比對到至少一個 TCMSP 靶點，其餘沒有比對到不代表沒有關聯，只是 TCMSP 靶點命名方式沒有直接寫出那個基因符號
- 沒有做成永久的關聯資料表，是每次點擊當下即時計算（1751 個靶點的比對成本很低，效能沒有問題），如果之後想要離線批次跑完整套基因對照可以再另外設計

## v1.19.0 — 2026-08-06（暗黑基因管理：資料匯入 + 完整 CRUD）

### 新增功能

分析使用者提供的 `cancerGeneList.tsv`（OncoKB 癌症基因參考清單，1245 個基因），建立正式的資料庫層與後台管理功能，對應目標三「NVIDIA BioNeMo + Google AlphaGenome 暗黑基因組分析」的基礎參考資料。

- 新增資料表 `dark_genes`，欄位對應原始 TSV 的全部 16 個欄位：Hugo Symbol、Entrez Gene ID、GRCh37/38 Isoform 與 RefSeq、Gene Type（ONCOGENE/TSG/ONCOGENE_AND_TSG/NEITHER/INSUFFICIENT_EVIDENCE）、出現次數、OncoKB/MSK-IMPACT/MSK-HEME/FOUNDATION ONE/FOUNDATION ONE HEME/Vogelstein/COSMIC CGC 各資源標記（Yes/No 轉為布林值）、基因別名
- 新頁面 `dark-genes.html`（F3-2，僅限管理者）：
  - **拖拉或點選上傳 TSV 檔案匯入**，依 Hugo Symbol 自動 upsert（已存在的基因會更新欄位、不存在的會新增），**不會清空重建整張表**——因為這張表之後可能會有後台手動新增/編輯的資料，清空重建會把這些洗掉（跟 TCMSP 藥材資料「先清空再匯入」的做法不同，這裡刻意設計成 upsert）
  - 完整 CRUD：新增/編輯/軟刪除，搜尋（基因符號/別名）、依類型/狀態篩選
- 後端新增 `POST /dark-genes/import`（上傳檔案匯入）與完整 CRUD 端點，另外也提供 CLI 版本 `python -m app.import_dark_genes data_import/cancer_gene_list.tsv`（部署時可直接執行，效果與網頁上傳完全一樣）
- 原始 TSV 檔案存放於 `data_import/cancer_gene_list.tsv`，供之後重新匯入或更新版本使用
- 已透過完整測試驗證：匯入 1245 筆全數正確（型別統計：ONCOGENE 443、TSG 340、ONCOGENE_AND_TSG 66、INSUFFICIENT_EVIDENCE 149、NEITHER 22、未分類 225）、搜尋、前台篩選、CRUD、重複匯入的 upsert 冪等性（不會累加/重複）

### 部署注意事項

需要重新執行遷移腳本（回填新增的 F3-2 功能項目；`dark_genes` 屬於全新資料表，一般啟動流程的 `create_all` 會自動建立，不需要遷移腳本處理）：

```bash
$env:DATABASE_URL="你的 Neon 連線字串"
python -m app.migrate_schema
```

接著匯入正式的基因參考資料（跟遷移腳本分開執行）：

```bash
python -m app.import_dark_genes data_import/cancer_gene_list.tsv
```

也可以不跑指令，改成部署完成後直接到「暗黑基因管理」頁面用網頁上傳 `data_import/cancer_gene_list.tsv` 這個檔案，效果相同。

### 已知限制

- 這批資料是公開的 OncoKB 癌症基因參考清單，屬於基因層級的中性參考資訊（不含任何病患個資），不像病患資料需要遮罩保護
- 目前跟客戶資料管理（`patients.html`）之間還沒有實際串接（例如病患的基因檢測結果連結到這張表），這需要等檢體/定序/分析/變異/解讀/品質那六層資料模型實作後才能真正串起來

## v1.18.0 — 2026-08-05（全站語系設定、中藥行地圖、客戶資料管理）

### 1. 全站語系設定（繁體中文／简体中文）

- 「個人化設定」頁面新增「全站語系」卡片，套用範圍是**整個後台系統**（所有頁面），不再只限查詢站
- 做法：不是逐頁維護翻譯字串（維護成本太高，容易漏），改用 `frontend/js/site-lang.js`，透過 OpenCC-JS 對頁面已渲染出來的文字節點做即時繁簡轉換，並用 `MutationObserver` 持續監看後續動態新增的內容（例如表格重新整理、Modal 開啟），確保切換語系後任何時間點新增的文字也會套用
- 新頁面/新增文字完全不需要額外處理，天然涵蓋全站
- 後端新增設定鍵值 `site_language`（沿用 v1.17 的 `user_preferences` 通用機制），依帳號各自獨立、記住最後一次選擇
- 登入頁（`index.html`）因為尚未有登入身分，無法套用個人化設定，維持原本語言

### 2. 中藥行地理推薦加上實際地圖

- `finder.html` 新增地圖（Leaflet + OpenStreetMap 圖磚，免費、不需要 API Key），標示所有中藥行的實際地理位置
- 點地圖上的標記會彈出中藥行名稱/地址/評分，並自動捲動到對應的清單卡片；點清單卡片也會反向讓地圖定位並開啟該標記
- 使用者授權定位後，地圖會一併顯示使用者目前位置，並自動縮放至涵蓋所有標記與使用者位置的範圍

### 3. 客戶資料管理（病患基本資料 + 就診紀錄）

範圍很大的需求（完整基因體資料 8 層模型：病患／就診／檢體／定序／分析／變異／解讀／品質）這次先聚焦在核心的**病患**與**就診**兩層，其餘六層屬於目標三（DNA 檢測）範疇，規劃於後續版本擴充。

- 新增資料表 `patients`、`encounters`，欄位命名參照中國國家衛健委《電子病歷基本數據集》標準
- **敏感個資保護**：`id_number`（證件號碼）在所有列表/查詢回應一律遮罩（例如 `A12****789`），需要另外呼叫專門的「顯示明碼」端點才能看到完整號碼，且該操作會寫入稽核紀錄
- 新頁面 `patients.html`（F3-1，僅限管理者）：病患列表 CRUD（軟刪除）、內嵌就診紀錄管理（新增/刪除，同樣軟刪除）
- 已透過完整測試驗證：新增/編輯/軟刪除、遮罩與明碼查詢、重複識別碼防呆（病患識別碼、就診識別碼皆唯一）、一般使用者無法存取（403）

### 部署注意事項

需要重新執行遷移腳本（回填新增的 F3-1 功能項目；`patients`／`encounters` 屬於全新資料表，一般啟動流程的 `create_all` 會自動建立，不需要遷移腳本處理）：

```bash
$env:DATABASE_URL="你的 Neon 連線字串"
python -m app.migrate_schema
```

### 已知限制

- `id_number` 目前是**明碼儲存在資料庫**（只有 API 回應層做遮罩），還沒做欄位層級的加密；正式上線前如果要符合更嚴格的個資保護規範，建議加上資料庫層級加密或改用第三方金鑰管理服務
- 病患／就診資料目前僅限管理者存取（`require_admin`），還沒有更細緻的角色權限控管（例如「只能看自己科別的病患」），如果之後有這類需求需要另外設計
- 基因體資料六層（檢體/定序/分析/變異/解讀/品質）尚未實作，且這些欄位一旦串接目標三的 DNA 檢測與 AlphaGenome，正式對外提供解讀報告前需要建立醫師/專業人員審核機制（沿用先前已記錄的規劃）

## v1.17.2 — 2026-08-05（修正疾病查詢站：點選任何第二個以後的疾病完全沒反應）

### 問題

使用者回報：載入全部資料後，點選左側清單裡的疾病，右半部資料無法呈現。追查後找到真正原因：`#emptyState`（「請從左側選擇一個疾病」那個提示區塊）被巢狀放在 `#disHeader` 裡面。第一次選擇疾病時，程式碼會把 `#disHeader` 的內容整個換成疾病標題，這個動作會**連帶把巢狀在裡面的 `#emptyState` 一起從畫面上刪除**。

之後每次呼叫 `selectDisease()`，程式碼第一步都會嘗試「隱藏 `#emptyState`」（`document.getElementById("emptyState").style.display = "none"`），但這個元素已經不存在於畫面上了，這行會直接拋出 `TypeError`，導致整個函式在這裡中斷——後面真正負責渲染 KPI 統計、表格、網絡圖的程式碼完全沒有機會執行。這解釋了「第一個疾病（預設疾病）看起來正常，但點其他疾病都沒反應」的現象。

### 修正

- 把 `#emptyState` 從 `#disHeader` 裡面移出來，改成 `#disHeader`／`#emptyState`／`#bodyWrap` 三個平行的兄弟元素，覆寫 `#disHeader` 的內容不會再牽連到 `#emptyState`
- 已透過模擬瀏覽器環境的自動化測試驗證：載入全部資料後，依序切換 12 種不同疾病，全部正確顯示對應的 KPI 統計與標題，不再拋出例外

### 已知限制

- 這次修正過程中也確認過「TCMSP 藥材關聯查詢站」沒有同樣的結構性問題（它的做法是整個重建 `#main` 內容，不是針對特定元素做局部修改），不需要比照修正

## v1.17.1 — 2026-08-05（修正進度條卡在 0%，新增預設藥材/疾病後台設定，疾病站同步支援漸進式載入）

### 1. 修正「載入全部」進度條卡在 0% 的真正原因

前端與後端分屬不同網域（Cloudflare Pages / Render），瀏覽器基於安全性，跨網域請求**預設不會把 `Content-Length` 這類回應標頭公開給 JavaScript 讀取**，除非伺服器明確在 CORS 設定裡加上 `Access-Control-Expose-Headers`。這導致進度條算式裡的「總大小」永遠是 0，一直停在「載入中...0%」，看起來像當機。

- `app/main.py` 的 CORS 設定加上 `expose_headers=["Content-Length"]`
- 前端同步強化：就算真的拿不到總大小，也會改顯示「已下載 X MB」而不是卡住不動，避免再次看起來像當機

### 2. 新增「預設藥材」「預設疾病」後台設定

- 系統設定頁面新增「查詢站預設項目」卡片：可搜尋並選擇 TCMSP 藥材／疾病查詢站首次載入要顯示的預設項目，取代原本寫死搜尋「Ginseng」／清單第一筆的邏輯
- 後端新增 `GET/PUT /system-settings/default-herb`、`GET/PUT /system-settings/default-disease`
- 查詢站左側清單現在**只顯示預設項目**（不再列出全部 502/564 筆），要找其他項目直接搜尋即可（清單名稱本身已全部載入，搜尋不受影響），或按「載入全部」看完整清單

### 3. TCMSP 疾病關聯查詢站也補上漸進式載入

比照藥材站的做法：

- 新增輕量後端端點：`GET /tcmsp/diseases/public/list`、`GET /tcmsp/diseases/public/{dis_id}/detail`
- 首次載入只抓疾病清單 + 預設疾病的完整關聯資料，其餘疾病點擊時隨選載入
- 新增「載入全部」按鈕與即時下載進度條

### 已知限制

- 疾病站的單一疾病詳情端點，最差情況（例如靶點連結到上千個成分時）回應可達約 1.7MB，雖然仍比完整 11.6MB 資料集小很多，但沒有藥材站的單一藥材詳情（通常僅數十~數百 KB）那麼輕量——這是資料關聯本質上的差異（單一靶點可能連結全資料庫上千種成分），非程式邏輯問題

## v1.17.0 — 2026-08-05（查詢站效能優化、繁簡切換、淺色/深色主題與個人化設定）

### 1. TCMSP 藥材關聯查詢站載入效能大幅優化

- 新增輕量後端端點：`GET /tcmsp/herbs/public/list`（僅藥材清單，~0.1秒）、`GET /tcmsp/herbs/public/{herb_id}/detail`（單一藥材完整關聯資料，~0.07秒）
- **首次載入只抓藥材清單 + 人參詳情**，不再一次下載全部 502 種藥材的完整關聯資料（原本要等 5 秒以上）
- 點擊清單裡任何其他藥材時，會自動隨選載入該藥材的資料（同樣很快），瀏覽過的藥材會快取在記憶體不重複請求
- 新增「載入全部」按鈕：需要一次拿到全部資料時（例如離線使用、批次分析）可以手動觸發，並顯示**即時下載進度條**（實際位元組進度，非假動畫）

### 2. 動態進度條

- 頁面右側空白狀態下方新增進度條，「載入全部」下載過程中即時顯示百分比與已下載/總大小（MB）

### 3. 繁體中文／簡體中文切換

- 兩個查詢站都新增語系下拉選單（搜尋欄位旁），透過 OpenCC-JS 即時轉換：藥材/疾病/分類中文名稱、網絡圖節點文字、節點詳情面板，全部套用選定的字形
- 選擇會記住在瀏覽器（localStorage），不需要每次重新選
- 疾病站的網絡圖節點文字與「中文名稱」欄位，現在優先使用資料庫裡真實的 `disease_cn_name`（v1.15 已 100% 覆蓋），取代原本只有 116 筆的舊靜態字典

### 4. 淺色／深色主題 + 個人化設定（新功能）

- 兩個查詢站新增 `body[data-page-theme="light"]` 淺色主題樣式，覆蓋原本固定的深色主題
- 新增「個人化設定」頁面（F0-18，任何登入帳號都能使用，不受角色權限限制），目前有「關聯查詢站 CSS 設定」一項（深色/淺色）
- 查詢站頂部導覽列也新增「🌗 切換淺色/深色」快速按鈕，不用特地跳頁面
- 設定儲存在新增的 `user_preferences` 資料表（依帳號各自獨立），**只影響自己看到的畫面，不影響其他使用者**；下次登入會自動套用上次的選擇
- 後端新增 `GET/PUT /user-preferences/{key}` API，通用 key-value 設計，之後要加其他個人化設定項目可以直接沿用

### 部署注意事項

需要重新執行遷移腳本（回填新增的 F0-18 功能項目；`user_preferences`／`announcements` 等全新資料表會在一般啟動流程自動建立，不需要遷移腳本處理）：

```bash
$env:DATABASE_URL="你的 Neon 連線字串"
python -m app.migrate_schema
```

### 已知限制

- 淺色主題目前是手動撰寫的覆寫樣式，涵蓋頁面上大部分區塊，但少數較少見的元素（例如照片彈出視窗某些邊角）可能還是深色配色殘留，之後發現可以再補
- OpenCC-JS 透過外部 CDN（jsdelivr）載入，如果使用者網路環境擋掉該網域，語系切換會直接原樣顯示文字（不會壞掉，只是不會轉換），不影響其他功能

## v1.16.1 — 2026-08-05（修正全螢幕檢視時顯示層級勾選無反應的問題）

### 問題

在全螢幕檢視關聯網絡圖時，勾選/取消「顯示層級」（Related Targets/Ingredients/Herbs）或調整節點數量下拉選單，畫面沒有任何變化。原因是重繪邏輯寫死只更新背景那個小的網絡圖容器（`network`），沒有同步更新全螢幕模式實際顯示的畫布（`networkModalCanvas`），所以狀態其實有正確改變，只是使用者看不到畫面更新。

### 修正

- 新增 `modalOpen` 狀態旗標，追蹤全螢幕檢視目前是否開啟
- 顯示層級勾選、節點數量下拉選單變更時，如果全螢幕檢視正開著，會同步重繪 `networkModalCanvas`，不會再只更新看不到的背景小圖

## v1.16.0 — 2026-08-05（疾病查詢站補齊與藥材查詢站對等的互動功能）

### 新增功能

「TCMSP 疾病關聯查詢站」原本只是精簡版，這次補齊跟「TCMSP 藥材關聯查詢站」對等的核心互動功能：

- **靶點挑選器**：左側新增可勾選的 Related Targets 清單（對應藥材站的成分挑選器），每個項目顯示關聯成分數量
- **挑選器工具列**：全選靶點／全不選／只選有成分資料／只選有藥材資料／「顯示 Selected」套用按鈕（顯示目前套用筆數）
- **顯示層級勾選**（Related Targets／Related Ingredients／Related Herbs）：逐層關係，關掉上層會自動一併關閉並鎖定下層，主畫面與全螢幕檢視共用同一套規則與狀態
- **網絡圖節點數量可調整**：每層最多顯示幾個靶點/成分/藥材，透過下拉選單即時調整（原本是寫死的數字）
- **全螢幕檢視點擊節點看詳情**：點任何節點（疾病/靶點/成分/藥材）會在右側面板顯示該節點詳細欄位
- 表格內容會反映目前挑選器＋顯示層級的篩選結果，不是固定顯示全部

### 已知限制（相較藥材站，這次沒有做的部分）

- 沒有做欄寬/圖表高度的拖曳調整與「儲存版面配置」功能
- 沒有做成分數值篩選（MW/OB%/DL/ALogP 等門檻篩選），因為疾病站的挑選對象是「靶點」而非「成分」，數值篩選的適用性較低
- 沒有做圖上文字語言切換（繁体/简体/English），因為靶點與成分目前資料庫裡沒有中文名稱欄位（只有疾病與藥材有），語言切換的效果有限，之後如果靶點/成分也建立中文名稱欄位，可以再補上

## v1.15.2 — 2026-08-05（修正遷移腳本：ALTER 語句連坐失敗導致新欄位從未真正建立）

### 問題

使用者實測回報 `disease_cn_name` 欄位在 Neon 上仍然不存在，即使已經執行過 `migrate_schema.py` 並看到「完成」訊息。追查 Render 日誌發現真正原因：**所有 `ALTER TABLE` 語句原本共用同一個資料庫交易**，PostgreSQL 的交易機制是「一句失敗，同一交易裡後面所有語句都會被連坐拖累失敗」（錯誤代碼 `InFailedSqlTransaction`）——只要清單裡第一句（例如 `enabled` 欄位已存在）失敗，後面真正需要新增的欄位（包括這次的 `disease_cn_name`）就永遠不會真的執行到，即使腳本最後仍印出「完成」字樣，掩蓋了這個問題。

### 修正

- `app/migrate_schema.py`：每一句 `ALTER TABLE` 改為**各自獨立的資料庫交易**（獨立的 `engine.begin()`），一句失敗不會再拖累其他語句
- 已在本機重複執行兩次驗證：腳本能正常跑完，不會中途崩潰

### 部署注意事項

這次修正的是遷移腳本本身，**請重新執行一次遷移指令**，這次應該會看到 `disease_cn_name` 欄位真正被建立：

```bash
$env:DATABASE_URL="你的 Neon 連線字串"
python -m app.migrate_schema
```

接著重新執行匯入指令拿到完整的疾病中文翻譯：

```bash
python -m app.import_tcmsp_data data_import/tcmsp_data.json
```

## v1.15.1 — 2026-08-05（補齊全部 564 種疾病的中文翻譯，覆蓋率 100%）

### 新增內容

- `data_import/disease_cn_name_seed.json` 從 116 筆擴充到 **564 筆（全部疾病）**，翻譯依標準醫學術語逐筆處理
- 已重新匯入本機測試資料庫驗證：564 筆疾病、564 筆有中文名稱，覆蓋率 100%

### 已知限制

- 這批翻譯是一次性人工整理，未經專業醫學審定；如果之後發現不夠精準或有更通用的譯名，可以直接到「疾病中文名稱管理」頁面（F1-3）逐筆修正——修正後即使重新執行匯入腳本也不會被覆蓋（v1.15.0 已建立的保護機制）
- 少數罕見疾病、症候群、遺傳學專有名詞（例如 `DIS01001` 血型系統類目、`DIS01011` 未明確命名的症候群）維持較直譯或保留原文縮寫的翻法，之後如有更精確的中文醫學名詞可以再更新

## v1.15.0 — 2026-08-05（疾病中文名稱：資料庫欄位 + 後台管理頁面）

### 新增功能

- `tcmsp_diseases` 資料表新增 `disease_cn_name` 欄位
- 從原本「TCMSP 藥材關聯查詢站」內建的靜態翻譯字典，抽取出 **116 筆**既有的疾病中文翻譯，改為正式匯入資料庫作為初始種子資料（`data_import/disease_cn_name_seed.json`），不再只是寫死在前端 JS 裡
- 新增後台管理頁面 `tcmsp-diseases.html`（F1-3，疾病中文名稱管理）：可搜尋、篩選「尚無中文名稱」的項目、逐筆編輯中文名稱與備注
- 新增後端 API：`GET /tcmsp/diseases`、`PUT /tcmsp/diseases/{dis_id}`
- 「TCMSP 疾病關聯查詢站」左側列表、詳情標題、網絡圖節點，都會優先顯示中文名稱（沒有翻譯的疾病維持顯示英文名稱）
- **重新匯入保護機制**：`app/import_tcmsp_data.py` 匯入前會先備份既有的疾病中文名稱，重新執行匯入腳本（例如之後要更新 TCMSP 原始資料）不會洗掉管理者手動修正過的翻譯——已透過完整測試驗證此行為

### 部署注意事項

需要重新執行遷移腳本（新增 `disease_cn_name` 欄位、新功能項目 F1-3、回填種子翻譯資料）：

```bash
$env:DATABASE_URL="你的 Neon 連線字串"
python -m app.migrate_schema
```

如果之前已經匯入過 TCMSP 資料，這次遷移只會回填「目前是空值」的疾病翻譯，不會覆蓋任何既有內容。如果想要一次拿到完整的 116 筆種子翻譯，也可以直接重新執行一次匯入腳本：

```bash
python -m app.import_tcmsp_data data_import/tcmsp_data.json
```

## v1.14.0 — 2026-08-05（新增 TCMSP 疾病關聯查詢站）

### 新增功能

- 新頁面 `disease_query.html`（F1-2）：與「TCMSP 藥材關聯查詢站」資料同源（同一個 `/tcmsp/data/full` API），但以**疾病**為查詢起點，左側改列出 564 種疾病，右側顯示反向關聯：Related Targets → Related Ingredients → Related Herbs
- 呈現方式與藥材查詢站一致：同樣的深色主題、KPI 統計卡片、分頁籤表格、Disease→Targets→Ingredients→Herbs 網絡關聯圖（可全螢幕檢視）、單一疾病 JSON/CSV 下載
- 後端不需要新增 API，直接複用既有的 `/tcmsp/data/full`，前端在瀏覽器端建立反向索引（`targetsByDisease`／`ingredientsByTarget`／`herbsByIngredient`）
- 登入後即可在導覽選單看到這個功能項目（前台），管理者可到「角色管理」的權限矩陣視窗調整哪些角色可以使用、要不要停用/顯示於前台或後台
- 網絡圖為避免節點過於密集，僅顯示部分節點（每個疾病最多 8 個靶點、每個靶點最多 4 個成分、每個成分最多 3 個藥材），完整清單仍可在下方表格查看

### 部署注意事項

需要重新執行遷移腳本（新增 F1-2 功能項目）：

```bash
$env:DATABASE_URL="你的 Neon 連線字串"
python -m app.migrate_schema
```

## v1.13.6 — 2026-08-05（無角色帳號預設也能看到 Dashboard，不再是空白畫面）

### 問題

第三方登入（Google/Facebook）新建立的帳號經審核啟用後，如果沒有另外指派角色，會因為 `/nav/menu` 的邏輯「沒有角色 = 完全看不到任何功能」，登入後只看到一片空白（連 Dashboard 都看不到）。

### 修正

- `app/routers/nav.py` 新增 `BASELINE_CODES`（Dashboard 本體 + 5 個小工具）：這些項目只要帳號是登入中的有效狀態就一定看得到，不需要額外指派角色或權限矩陣設定，作為全站的保底落地頁
- 其餘功能（角色管理、中藥行管理等）仍然完全依角色權限矩陣控制，這次修正不影響既有的權限管控

## v1.13.5 — 2026-08-05（修正併發重複請求仍會撞到「授權碼已使用過」的問題）

### 問題

v1.13.4 加了授權碼重放快取，但使用者實測仍然出現同樣的錯誤。追查後發現 v1.13.4 的做法不夠：兩個重複的 callback 請求幾乎是**同時**進來的，各自檢查快取時都看到「還沒有結果」（因為對方也還沒寫入），於是兩邊都跑去跟 Facebook 交換 token，其中一個必然失敗。單純檢查快取擋不住這種併發競爭（race condition）。

### 修正

- 新增 `_get_oauth_code_lock()`：為每一組授權碼建立一個 `asyncio.Lock`
- `google_callback`／`facebook_callback` 用這個鎖把「檢查快取 → 交換 token → 寫入快取」整段包起來：第二個重複請求會**排隊等待**第一個請求完成，而不是同時衝去對 Facebook/Google 發請求；等鎖釋放後，第二個請求會直接讀到快取結果，不會再重新呼叫第三方 API
- 已透過模擬併發請求的測試驗證：兩個同時打進來的重複請求，確認只有一個會真正執行 token 交換

## v1.13.4 — 2026-08-05（修正 Facebook 登入「授權碼已使用過」的間歇性失敗）

### 問題

使用者實測時 Render 日誌顯示：`Facebook token 交換失敗：{"error":{"message":"This authorization code has been used."...}}`，但當下只操作了一次登入。追查後研判是 Facebook（或部分瀏覽器）的登入完成頁，在特定情境下會**觸發兩次**跳轉回我們的 `callback` 網址、帶著同一組授權碼——授權碼是一次性的，第一次成功交換後，第二次必然會被 Facebook 判定「已使用過」而失敗，使用者只會看到失敗那一次的畫面。

### 修正

- `google_callback`／`facebook_callback` 新增授權碼重放快取（`_cache_oauth_result` / `_get_cached_oauth_result`）：同一組授權碼在 60 秒內重複進來時，直接回放第一次成功的登入結果，不會再重新跟 Google/Facebook 交換 token，自然也不會撞到「已使用過」的錯誤
- 附帶好處：避免了重複的 `login_log`／稽核紀錄寫入

### 已知限制

- 這組快取一樣是伺服器記憶體內的暫存（規模很小、生命週期只有 60 秒，風險遠低於先前的 CSRF state 問題），如果兩個重複請求剛好落在 Render 重新部署交接的新舊程序上，快取不會共享，理論上還是可能重現這個錯誤，但機率非常低（需要「重複請求」與「部署交接」兩個時間窗口疊在一起）

## v1.13.3 — 2026-08-05（登入頁第三方登入按鈕加上重試機制，因應 Render 冷啟動）

### 問題

使用者回報登入頁完全沒有顯示 Google/Facebook 登入按鈕，看起來像「沒有整合第三方登入」。實際原因是 Render 免費方案閒置一段時間會休眠，喚醒需要 30~60 秒；登入頁載入當下如果後端還在睡，查詢 `/auth/google/enabled`／`/auth/facebook/enabled` 的請求會直接失敗，而原本的程式碼是「查詢失敗就悄悄不顯示按鈕」，沒有任何提示或重試，容易誤導使用者以為功能不存在或設定遺失。

### 修正

- `frontend/js/login.js` 的 `checkOAuthEnabled()` 改為查詢失敗時每 8 秒重試一次，最多重試 6 次（涵蓋 Render 冷啟動最長約 1 分鐘的喚醒時間）
- 第一次查詢失敗時，登入頁會顯示提示文字「第三方登入服務啟動中，請稍候...」，查到結果或重試次數用盡後自動隱藏
- 不影響一般帳號密碼登入，這段邏輯全程都是非阻塞的背景查詢

## v1.13.2 — 2026-07-31（第三方登入失敗時記錄實際錯誤訊息）

### 問題

Google／Facebook 登入若在「交換 token」這步失敗，畫面只會顯示通用的「與第三方服務交換登入憑證失敗」，實際上 Google/Facebook 回傳的詳細錯誤原因完全沒有被記錄下來，導致除錯時完全看不到問題出在哪。

### 修正

- `google_callback`、`facebook_callback` 在 token 交換失敗時，改為把 Google/Facebook 回應的實際錯誤內容印到伺服器日誌（`print`，會顯示在 Render 的 Logs 分頁），格式為 `[Google OAuth] token exchange failed: ...` / `[Facebook OAuth] token exchange failed: ...`
- 使用者看到的畫面訊息不變（仍是友善的通用訊息），但管理者現在可以到 Render 後台的 Logs 分頁查到具體原因（例如 redirect_uri 不匹配、client_secret 錯誤等）

## v1.13.1 — 2026-07-31（修正第三方登入「驗證逾時或失效」的真正原因）

### 問題

使用者實測 Facebook 登入時出現「登入驗證逾時或失效，請重新嘗試」，追查後發現根因：CSRF state 原本存在伺服器記憶體的 `set` 裡（v1.5.0 就存在的已知限制），Render 重新部署交接新舊程序時，登入請求（產生 state）跟回呼請求（驗證 state）如果剛好落在不同程序，新程序的記憶體是空的，驗證就會失敗——這不是使用者操作問題，是架構本身的限制在部署交接的時間點被踩到了。

### 修正

- `app/security.py` 新增 `create_oauth_state_token()` / `verify_oauth_state_token()`：CSRF state 改用簽章 + 短效期（5 分鐘）的 JWT token，驗證時純粹靠簽章與時效判斷，**不需要查任何伺服器端儲存狀態**，因此不受多程序/重新部署影響
- Google、Facebook 登入流程都改用這個新機制，移除原本的 `_oauth_states` 記憶體暫存
- 已通過測試：模擬「產生 state 的程序」與「驗證 state 的程序」是完全不同的程序（重新載入整個模組），驗證仍然正確通過

## v1.13.0 — 2026-07-31（Facebook OAuth 登入、Render 主機資訊連結改指向 Swagger）

### 新增功能

- **真正的 Facebook OAuth 登入**（比照 Google 的 Authorization Code Flow）：
  - `GET /auth/facebook/enabled`、`GET /auth/facebook/login`、`GET /auth/facebook/callback`
  - 新增 `app/oauth_facebook.py` 封裝 Facebook OAuth 網址建構與 token/使用者資訊交換邏輯
  - 登入頁新增「使用 Facebook 帳號登入」按鈕，僅在後端已設定 Facebook 金鑰時顯示
  - 帳號治理規則跟 Google 完全一致（首次登入建立待審核帳號、email 相同自動綁定既有帳號、帳號停用一樣會被擋下）
  - Google／Facebook 共用的登入綁定邏輯抽成 `_handle_oauth_login()`，避免重複程式碼
  - `render.yaml` 新增 `FACEBOOK_CLIENT_ID`／`FACEBOOK_CLIENT_SECRET`（`sync: false`，需於 Render 後台手動填入）、`FACEBOOK_REDIRECT_URI`
  - README 新增完整「Facebook OAuth 設定」章節，含 Facebook App 申請步驟
  - `oauth-status.html` 更新 Facebook 的串接狀態為「已完成」
- Dashboard「主機資訊」卡片的 Render 項目連結，從單純的網址改為直接開啟 Swagger API 文件頁（`/docs`）

### 部署注意事項

這次資料庫變動除了 `features` 表照常回填，還新增了 `OAuthProvider` enum 型別的 `facebook` 列舉值。**正式環境（Neon PostgreSQL）需要重新執行遷移腳本**，腳本已更新為會自動處理這個 enum 新增（SQLite 環境會自動略過這步，不影響本機測試）：

```bash
$env:DATABASE_URL="你的 Neon 連線字串"
python -m app.migrate_schema
```

### 已知限制

- 部分 Facebook 帳號沒有綁定 email（例如手機號碼註冊），登入時會被系統擋下，因為帳號體系以 email 作為識別
- Facebook App 預設為「開發模式」，只有加到測試人員名單的帳號能登入，要開放一般大眾使用需要送 Facebook 審核（這部分需要你自己在 Facebook Developers 後台操作，程式碼已就緒）

## v1.12.1 — 2026-07-31（修正公告時區 bug，Dashboard 支援直接編輯版面）

### 問題與修正

**公告新增後不顯示在前台的真正原因**：`<input type="datetime-local">` 給的是瀏覽器「當地時間」字串，沒有時區資訊；前端原本直接把這串文字送給後端，後端會誤把它當成 UTC 時間比對，導致「現在」被誤判成還沒到（例如台灣時區 UTC+8，等於系統以為公告要 8 小時後才開始顯示）。修正為前端送出前用 `new Date(value).toISOString()` 轉換成正確的 UTC 時間字串。

### 新增功能：Dashboard 直接編輯版面

- Dashboard 頂部新增「🛠 編輯版面」按鈕（僅管理者看得到）
- 進入編輯模式後，每張卡片上方會出現工具列：
  - **顯示開關**：勾選/取消即時儲存（對應管理者視角的 `show_backend`）
  - **拖曳排序**：直接拖曳卡片調整順序，放開滑鼠自動儲存新順序（`sort_order`，同步影響「系統設定」頁面看到的排序）
- 編輯模式下，即使是目前被設定為隱藏的卡片也會顯示出來（半透明），方便管理者重新打開；離開編輯模式後恢復正常顯示規則

### 已知限制

- Dashboard 拖曳排序目前只有管理者看得到「編輯版面」按鈕，且排序共用同一組 `sort_order`（後台視角跟前台視角看到的順序相同），如果之後需要「前台/後台各自不同排序」，需要另外加欄位

## v1.12.0 — 2026-07-31（公告管理系統 + Dashboard 顯示規則簡化面板）

### 新增功能

- **公告管理系統（全新）**：
  - 後端新增 `Announcement`、`AnnouncementFile` 資料表；`AnnouncementFile` 內容以 base64 直接存資料庫，不依賴 Render 免費方案不持久的檔案系統
  - 後台 CRUD：新增/編輯/下架（軟刪除）/查詢公告，可設定開始/結束顯示時間，**時間到自動下架**（查詢時即時依 `start_at`/`end_at`/`status` 計算 `is_currently_visible`，不需要排程工作）
  - 歷史公告查詢：後台列表可篩選「目前顯示中」/「未顯示（含已過期/未開始/已下架）」/全部
  - 每則公告可上傳多個附件（單檔上限 5MB），可下載、可個別刪除
  - 新頁面 `announcements.html`（F0-17）
  - Dashboard 新增第五張卡片「公告」，顯示目前生效中的公告與附件下載連結，代碼 F0-13-5，走跟其他小工具一樣的前台/後台顯示規則機制
- **系統設定頁面新增「Dashboard 顯示規則」面板**：直接列出 5 張 Dashboard 卡片，逐一勾選「啟用/前台/後台」即可儲存，不用再繞去角色管理的權限矩陣視窗設定（角色管理那邊仍然保留同樣的編輯能力，兩處資料互通，改任一邊都會同步反映）

### 部署注意事項

需要重新執行遷移腳本（新增 `announcements`、`announcement_files` 資料表由一般啟動流程的 `create_all` 自動建立，不需特別處理；但 `features` 資料表需要回填新增的 F0-13-5、F0-17 兩筆功能項目）：

```bash
$env:DATABASE_URL="你的 Neon 連線字串"
python -m app.migrate_schema
```

### 已知限制

- 公告附件用 base64 存資料庫，單則公告建議附件總大小不要超過幾 MB，避免公告列表 API 回應變大變慢（目前公告列表 API 本身不包含檔案內容，只有附件的檔名/大小中繼資料，下載才會真正讀取 base64 內容，所以列表效能不受影響，但資料庫本身儲存空間會累積）
- 沒有排程通知機制（例如公告快到期前提醒管理者），純粹是查詢當下即時判斷是否顯示

## v1.11.0 — 2026-07-31（補齊未完成功能頁面、開合式選單、可命名配色主題）

### 新增功能

- **補齊先前沒有頁面的功能項目**：
  - `architecture.html`：系統架構規劃（F0-1/F0-3/F0-9 共用，直接渲染 `rules.md` 內容）
  - `oauth-status.html`：第三方登入整合狀態總覽（F0-6，顯示 Google/Facebook/小紅書/WeChat 目前串接狀態）
  - `security.html`：資安規劃（F0-7，列出已完成與規劃中的資安項目）
  - `reports.html`：報表設計（F0-8，列出規劃中的報表項目）
  - `system-settings.html`：系統設定（F0-16，新頁面，見下方主題系統）
  - F0-10（資料庫備份與還原）、F0-12（登入紀錄查詢）、F5-3（評價管理）改為指向既有頁面（`logs.html`／`pharmacies.html`），避免重工也避免導覽選單出現重複連結
- **權限矩陣表格顯示完整頁面路徑**：角色管理的權限矩陣視窗，每個功能項目現在會標示 `frontend\xxx.html` 完整路徑，取代原本模糊的「頁面：xxx」
- **導覽選單改為開合式設計**：`js/nav.js` 把「前台功能」「後台管理」各自做成可收合區塊，展開狀態記在瀏覽器 `localStorage`；目前所在頁面所屬的分區會強制展開
- **可命名的配色主題系統**：
  - 新增 4 組主題（森林綠/海洋藍/暖橘/石墨灰），定義在 `frontend/css/style.css` 的 `[data-theme="xxx"]` 區塊
  - 新增 `SystemSetting` 通用 key-value 資料表，`GET/PUT /system-settings/theme` API（GET 不需登入，登入頁也能套用主題；PUT 僅限管理者）
  - 新增 `frontend/js/theme.js`，所有頁面 `<head>` 載入時套用目前主題
  - 管理者可在「系統設定」頁面即時切換主題，全站套用

### 部署注意事項

需要重新執行遷移腳本（新增了 `system_settings` 資料表，以及 F0-16 功能項目、既有功能項目的頁面路徑調整）：

```bash
$env:DATABASE_URL="你的 Neon 連線字串"
python -m app.migrate_schema
```

## v1.10.0 — 2026-07-31（「功能項目管理」併入「權限矩陣」，移除重複頁面）

### 問題

角色管理的「權限矩陣」（設定單一角色的 can_view/can_execute）跟獨立的「功能項目管理」頁面（設定全站共用的 enabled/show_frontend/show_backend）都在管理同一份 `features` 資料，兩個入口容易搞混。

### 變更

- **移除** `frontend/features.html`、`frontend/js/features.js`，以及對應的導覽項目 F0-14
- **角色管理「權限矩陣」視窗整合為單一表格**：同時顯示、編輯「這個角色專屬」的可見/可執行權限，以及「全站共用」的啟用/前台/後台/導覽文字/排序設定，一次儲存
- 後端 `GET /roles/{id}/permissions`、`PUT /roles/{id}/permissions` 擴充：回傳與接受全站功能設定欄位（`enabled`/`show_frontend`/`show_backend`/`nav_label`/`page_url`/`sort_order`），PUT 時全站欄位只在有帶值時才更新，避免其他角色沒帶這些欄位時被意外清空
- `/features` 系列 API（GET/POST/PUT/DELETE）維持不變，供未來程式化管理使用

### 部署注意事項

需要重新執行遷移腳本，這次除了照常回填欄位，還會**清除既有資料庫裡的舊版 F0-14**（含它的角色權限紀錄）：

```bash
$env:DATABASE_URL="你的 Neon 連線字串"
python -m app.migrate_schema
```

## v1.9.1 — 2026-07-31（Dashboard 小工具支援前台/後台差異化顯示）

### 問題

v1.9.0 的 Dashboard 小工具只有單一「啟用」開關，管理者跟一般使用者看到的 Dashboard 小工具組合完全一樣，沒辦法個別設定「這張卡片只給管理者看」或「這張卡片只給一般使用者看」。

### 修正

- `frontend/js/dashboard.js`：改為依「目前登入者是否為管理者」，分別檢查小工具的 `show_backend`（管理者視角）或 `show_frontend`（一般使用者視角）欄位，決定卡片要不要顯示——同一個 Dashboard 頁面，管理者跟一般使用者現在可以看到不同組合的小工具
- `frontend/features.html` / `js/features.js`：開放小工具項目的「前台」「後台」勾選框（先前因為小工具沒有獨立頁面，這兩個勾選框被鎖住不能改）
- `app/feature_config.py`：小工具的 `show_frontend` / `show_backend` 預設值改為皆為 `True`（維持與 v1.8.0 一致的預設行為：兩種身分預設都看得到，管理者可再自行調整成不同組合）
- `rules.md` 更新「Dashboard 小工具」章節，說明小工具的 `show_frontend`/`show_backend` 語意跟一般頁面不同（不是「導覽選單分區」，而是「管理者視角 vs 一般使用者視角」）

### 部署注意事項

這次沒有新增資料庫欄位，但既有資料的預設值不對（v1.9.0 部署時已經把小工具的 `show_frontend`/`show_backend` 回填成 `False`）。**建議重新執行一次遷移指令**，它會依最新的 `feature_config.py` 重新回填：

```bash
$env:DATABASE_URL="你的 Neon 連線字串"
python -m app.migrate_schema
```

⚠️ 提醒：`migrate_schema.py` 的回填邏輯目前是「無條件覆寫」，如果之後在「功能項目管理」頁面手動調整過某些項目的顯示設定，重跑這支腳本會把那些手動調整覆蓋回設定檔的預設值。目前階段（尚未有正式使用者依賴自訂設定）重跑沒問題，之後如果要在正式環境保留管理者的自訂設定，這支腳本需要改成只回填「資料庫裡還沒有值」的欄位，而不是整批覆寫。

## v1.9.0 — 2026-07-31（功能項目顯示控制、前台/後台導覽區隔、動態選單）

### 新增功能

- **功能項目管理**（新頁面 `features.html`，F0-14）：管理者可對每個功能項目勾選「啟用」「顯示於前台」「顯示於後台」，並編輯導覽文字與排序，即時生效
- **Dashboard 小工具開關**：Dashboard 四張卡片（主機資訊/版本資訊/專案文件/2026年目標）現在各自對應一個功能代碼（F0-13-1~F0-13-4），可在「功能項目管理」個別停用/啟用
- **前台／後台導覽區隔**：新增 `GET /nav/menu`，依目前使用者的角色權限（管理者永遠全見；一般角色需要 `can_view` 權限）動態回傳可見的功能項目，側邊選單改由 `js/nav.js` 統一動態渲染，分成「前台功能」「後台管理」兩區塊
- 「一般使用者」角色種子資料現在會自動取得所有 `show_frontend=true` 功能的 `can_view` 權限（目前為：Dashboard、中藥行地理推薦、TCMSP 藥材關聯查詢站）
- `Feature` 資料表新增欄位：`enabled`、`show_frontend`、`show_backend`、`nav_label`、`page_url`、`sort_order`
- 新增 `app/feature_config.py`：全站功能項目設定單一資料來源，`seed.py` 與 `migrate_schema.py` 共用
- 新增 `app/migrate_schema.py`：既有資料庫（尤其正式環境 Neon）需要手動執行一次，補齊 `features` 資料表新欄位並回填正確的導覽設定
- 新增 `/features` PUT 端點（先前只有新增/刪除，沒有編輯）
- 新增 `rules.md`：記錄「每個功能獨立頁面」「功能顯示控制三層機制」等開發規範，並列入 Dashboard 專案文件清單

### 部署注意事項

**正式環境（Render + Neon）這次更新後，除了照常 push 程式碼，還需要額外執行一次遷移指令**：

```bash
$env:DATABASE_URL="你的 Neon 連線字串"
python -m app.migrate_schema
```

沒有執行的話，`features` 資料表會缺少新欄位，`/nav/menu`、`/features` 等 API 會報錯。

### 已知限制

- 目前只有「選單看不看得到」是動態控制的，個別 API 的存取權限仍然是各自路由用 `require_admin`／`require_permission()` 明確檢查——兩者需要一起維護，不能只靠隱藏選單當作安全機制
- 按鈕層級的操作權限（`can_execute`）後端已有檢查機制，但前端還沒有依此動態隱藏「編輯/刪除」按鈕

## v1.8.0 — 2026-07-31（Dashboard 全面改版：不再是施工中佔位頁）

### 新增功能

- **主機資訊卡片**：列出 GitHub、Cloudflare Pages、Render、Neon 四個服務的角色說明與連結（`GET /project-info/hosts`）
- **版本資訊卡片**：顯示目前版本號徽章，下方列出所有歷史版本，每筆固定長度摘要 + 「詳細」按鈕開啟 Modal 顯示該版本完整 CHANGELOG 內容（Markdown 渲染）。後端新增 `GET /project-info/version`，會自動解析 `CHANGELOG.md` 切成結構化版本清單，不需要手動維護重複資料
- **專案文件卡片**：README、版本更新紀錄、2026 年工作目標三份文件，點按鈕開啟 Modal 直接閱讀（Markdown 渲染），後端新增 `GET /project-info/docs`、`GET /project-info/docs/{doc_id}`，直接讀取專案內對應的 `.md` 檔案內容
- **2026 年工作目標卡片**：五大目標摘要 + 完成狀態標示（目標五已完成，其餘進行中/規劃中），可點「查看完整文件」看完整內容
- 新增 `docs/2026_goals.md`：整理五大目標的詳細說明與目前狀態
- 前端加入 `marked.js`（CDN）做 Markdown 轉 HTML 渲染

### 已知限制

- 「2026 年工作目標」摘要卡片的五筆資料是寫死在前端 JS 裡（`GOALS_SUMMARY`），跟 `docs/2026_goals.md` 內容需要手動保持同步；如果之後目標異動頻繁，可考慮改成後端統一解析
- 版本資訊的「摘要」是取 CHANGELOG 標題括號內文字，若某版本沒有標題文字，會退回取內文第一行，可能不夠精簡

## v1.7.3 — 2026-07-31（TCMSP 查詢站效能優化 + 移除「本地資料庫版」字樣）

### 效能優化

- `app/main.py` 加入 `GZipMiddleware`：大型 JSON 回應（如 `/tcmsp/data/full`）傳輸前自動壓縮，減少網路傳輸量
- `/tcmsp/data/full` 加入伺服器端記憶體快取（10 分鐘 TTL），並直接快取「已序列化好的 JSON bytes」，命中快取時完全跳過資料庫查詢與序列化開銷
- 本機測試：首次請求（無快取）約 5.5 秒，快取命中後降到約 1.1 秒；正式環境（Render + Neon，兩者皆有網路延遲）預期改善幅度會更明顯
- 後台編輯／下架藥材時會主動清除快取，確保管理者的異動立即反映在前台查詢站，不用等 10 分鐘 TTL 過期

### 介面調整

- 移除頁面標題與 `<title>` 裡的「（本地資料庫版）」／「（本地資料庫）」字樣

## v1.7.2 — 2026-07-31（緊急修正：TCMSP 查詢站「完全無資料」的真正原因）

### 問題

`tcmsp_query.html` 裡有一行遺留的樣板佔位符沒有替換：

```js
const VERSION_SNAPSHOTS = __VERSION_SNAPSHOTS_JSON__;
```

瀏覽器執行到這行會拋出 `ReferenceError: __VERSION_SNAPSHOTS_JSON__ is not defined`，**導致整個 `<script>` 後續程式碼全部中斷**，包括負責載入資料的 `bootstrapData()` 完全沒有機會執行。這就是使用者回報「查詢站完全沒有資料、畫面卡在請選擇藥材」的真正原因——**跟 Neon 資料庫是否有匯入資料無關**，之前排查方向（懷疑資料庫沒匯入）是誤判，特此更正。

### 修正

- `VERSION_SNAPSHOTS` 固定改為空物件 `{}`：版本歷史說明文字仍可查看，但不提供舊版本下載（舊版樣板的下載功能需要外部快照檔案，此次整合沒有對應資料來源）
- 全文搜尋確認沒有其他遺漏的 `__XXX__` 樣板佔位符

## v1.7.1 — 2026-07-31（暫停 staging 環境，先專心搞定正式站）

v1.7.0 建置的正式站／測試站分離架構評估後覺得太複雜，暫時停用，先集中處理正式站：

- `render.yaml`：staging 服務整段註解掉（設定保留，之後要恢復把每行開頭的 `# ` 拿掉即可）
- `frontend/js/api.js`：拿掉依網域判斷 API 位置的邏輯，改回固定指向正式站後端
- `app/main.py`：CORS 移除 staging 網域，只保留正式站與本機測試
- README「正式站／測試站分離」章節保留供之後參考，並加註目前暫停使用

## v1.7.0 — 2026-07-31（正式站／測試站環境分離）

### 新增功能

- `render.yaml` 新增第二個服務 `tcm-onco-backend-staging`，綁定 `staging` 分支，環境變數與正式站完全獨立（各自的 `DATABASE_URL`、`FRONTEND_BASE_URL`、`GOOGLE_REDIRECT_URI`、`TCM_JWT_SECRET`）
- `app/main.py` CORS 設定新增允許 `https://fwc-tcmsp-staging.pages.dev`（含其 Cloudflare Pages 預覽網址）
- `frontend/js/api.js` 新增 `resolveApiBase()`：依前端目前部署的網域（正式站/測試站/本機）自動決定要呼叫哪個後端 API，同一份前端程式碼可以同時部署到兩個環境
- README 新增完整「正式站／測試站分離」設定章節，涵蓋 Git 分支策略、Neon 測試專用資料庫、Render Blueprint 新增服務、Google OAuth 多加一筆重新導向 URI、Cloudflare Pages 新建獨立專案等步驟

### 使用方式

日常開發／測試都先 push 到 `staging` 分支，在 `https://fwc-tcmsp-staging.pages.dev` 驗證沒問題後，再合併回 `main` 觸發正式站更新，降低直接動到正式環境的風險。

### 已知限制

- 兩個環境目前共用同一組 Google OAuth 用戶端（只是在 Google Cloud Console 多加一筆測試站的重新導向 URI），如果之後想讓兩邊完全獨立（例如不同的 OAuth 應用程式審核狀態），需要另外申請一組新的 Client ID/Secret
- 測試站的 TCMSP 資料、帳號、角色等都需要在測試資料庫另外手動建置/匯入一次，不會自動從正式站同步

## v1.6.0 — 2026-07-31（TCMSP 藥材關聯資料改為資料庫化，取代本機端 JSON 檔案）

### 背景

先前版本（v1.4.0～v1.5.1）的 TCMSP 藥材關聯資料是直接放在前端可讀取的靜態 JSON 檔案（`frontend/data/tcmsp_data.json`），純粹由瀏覽器端 fetch 讀取後在本機運算查詢/篩選，資料庫本身完全沒有這批資料，也無法透過後台管理。這次依需求把資料正式匯入資料庫，並改由後端 API 提供查詢。

### 新增功能

- `app/models.py` 新增 6 張 TCMSP 相關資料表：`tcmsp_herbs`（藥材）、`tcmsp_ingredients`（成分）、`tcmsp_targets`（靶點）、`tcmsp_diseases`（疾病）、`tcmsp_herb_ingredient`（藥材-成分關聯）、`tcmsp_ingredient_target`（成分-靶點關聯）、`tcmsp_target_disease`（靶點-疾病關聯）
- 新增 `app/import_tcmsp_data.py` 匯入腳本：`python -m app.import_tcmsp_data data_import/tcmsp_data.json`，可重複執行（先清空再匯入），本機 SQLite 測試匯入全部資料（502 藥材、13,728 成分、1,751 靶點、564 疾病、共約 9 萬筆關聯）耗時約 4.5 秒
- 新增後端 API：
  - `GET /tcmsp/data/full`（登入即可查詢，供前台查詢站使用，取代原本讀取靜態 JSON 檔案）
  - `GET /tcmsp/herbs`、`PUT /tcmsp/herbs/{id}`、`DELETE /tcmsp/herbs/{id}`（後台管理，僅限管理者：查詢/編輯備注與狀態/軟刪除下架）
- `tcmsp_query.html` 改為呼叫 `/tcmsp/data/full` API，不再讀取本機端 JSON 檔案
- 原始資料檔搬移到 `data_import/tcmsp_data.json`（不會隨前端部署到 Cloudflare Pages，只作為匯入來源保留）

### 已知限制

- 成分/靶點/疾病/各類關聯表資料量龐大（合計約 9 萬筆），屬於批次匯入的參考資料，暫不提供逐筆後台編輯介面；如需更新內容，需重新執行匯入腳本（會清空重建）
- `/tcmsp/data/full` 目前是即時查詢資料庫組裝完整資料回傳（未加快取），資料量大時每次請求都需要重新查詢組裝，如果之後使用者數增加、效能出現問題，可考慮加上伺服器端快取
- 正式環境（Render + Neon）需要手動執行一次匯入腳本（並指向正式的 `DATABASE_URL`），才會有資料；程式碼部署本身不會自動觸發資料匯入

## v1.5.1 — 2026-07-31（修正：改用真正的原始完整資料取代重建版本）

### 背景

v1.4.0 因為手上只有 `tcmsp_herb_details_merged_500.json`（原始爬取資料，未含中文名稱與 ICD 編碼），只能自己寫轉換腳本重建一份簡化版資料，並在多處用預設值/推斷值頂替缺漏欄位（例如 495 種藥材沒有中文名稱、疾病沒有 ICD9/10）。

使用者後續提供了完整的原始專案原始碼（`HERB_Q_1.7z`），裡面的 `tcmsp_herb_query_site.html` 是當時實際上線運作的完整版本，內嵌了真正正式的資料。比對後發現核心關聯數量（成分/靶點/疾病/各類關聯表）完全一致，證實資料源頭相同，但正式版額外包含：

- **全部 502 種藥材的正式中文名稱、拼音、分類**（v1.4.0 版本只有人參屬 5 種藥材有中文名稱，其餘用英文學名頂替）
- **疾病的 ICD9 / ICD10 編碼**（v1.4.0 版本此欄位全部是空值）
- 靶點的 DrugBank ID、KEGG 對照等額外欄位
- 成分的 TPSA、RBN 等額外 ADME 欄位
- 標準化的 `TAR00002`／`DIS00001` 格式編號（v1.4.0 版本靶點編號是未加前綴的原始數字）

### 修正內容

- `frontend/data/tcmsp_data.json` 改為直接使用從 `tcmsp_herb_query_site.html` 抽取出的正式資料（11.8MB），取代 v1.4.0 用腳本重建的簡化版（9.1MB）
- 頁面程式碼（`tcmsp_query.html`）與資料載入邏輯不需變動，直接相容新資料

### 已知限制（更新）

- 前一版「495 種藥材沒有中文名稱」「疾病沒有 ICD 編碼」的限制已解決
- 其餘限制維持不變：純前端運作、後台無法編輯此資料集，需要更新須替換 `tcmsp_data.json` 檔案本身

## v1.5.0 — 2026-07-31（真正的 Google OAuth 登入，取代先前的骨架）

### 新增功能

- 實作完整 Google OAuth 2.0 Authorization Code Flow：
  - `GET /auth/google/enabled` — 查詢是否已設定 Google 金鑰（供前端判斷是否顯示按鈕）
  - `GET /auth/google/login` — 導向 Google 登入頁（含 CSRF state 保護）
  - `GET /auth/google/callback` — Google 導回後交換 token、取得使用者 email，完成登入或建立待審核帳號
- 新增 `app/oauth_google.py`：封裝 Google OAuth 的網址建構與 token/使用者資訊交換邏輯
- 登入頁（`index.html`）新增「使用 Google 帳號登入」按鈕，僅在後端已設定 Google 金鑰時顯示
- 新增 `oauth_callback.html` 中繼頁面，處理 Google 登入完成後的 token 寫入與導轉
- **帳號治理與一般註冊一致**：第一次用 Google 登入會建立「審核中」帳號＋一筆帳號申請紀錄，需管理者於「帳號審核」頁核准後才能登入；若 Google email 對應到既有帳號則自動綁定
- `render.yaml` 新增 `GOOGLE_CLIENT_ID`、`GOOGLE_CLIENT_SECRET`（皆為 `sync: false`，需於 Render 後台手動填入）、`GOOGLE_REDIRECT_URI`、`FRONTEND_BASE_URL` 環境變數
- README 新增完整「Google OAuth 設定」章節，含 Google Cloud Console 申請步驟

### 重要說明：修正先前的誤解

先前對話中容易被誤以為「第三方登入已經完成」，但實際上此前只有：(1) 早期規劃文件把它列為 F0-6 待辦項目、(2) 一個純資料表 CRUD 骨架（`/oauth-accounts`，只能手動輸入 provider_user_id，沒有真正的登入按鈕與授權流程）。本版本才是第一次真正實作可運作的 Google 登入。

### 已知限制

- CSRF state 使用伺服器記憶體內暫存，僅適合單一伺服器程序；多 worker/多實例部署需改用 Redis 等共享暫存
- 小紅書（RED）、WeChat 第三方登入仍未實作，維持先前的資料表 CRUD 骨架，需另外確認開發者資格與串接規範
- 尚未實測真實 Google 帳號完整登入流程（本次僅完成程式碼邏輯與錯誤路徑的自動化測試），需要你申請好 Client ID/Secret 後實際跑一次才能最終確認

## v1.4.0 — 2026-07-31（整合 TCMSP 藥材關聯查詢站，目標一/二）

### 新增功能

- 新增 `frontend/tcmsp_query.html`：整合先前獨立開發的「TCMSP 藥材關聯查詢站」（v00.01.05 版樣板），涵蓋：
  - 左側 500 種藥材清單搜尋
  - 右側 Ingredients／Related Targets／Related Diseases 分頁表格（含欄位說明、排序、數值篩選）
  - 成分-靶點-疾病關聯網絡圖（vis-network），可全螢幕檢視、點節點看詳情、列印、下載圖片
  - 圖上文字語言切換（繁中／簡中／English）、版面自訂配置、單一藥材 JSON/CSV 下載、版本歷史查詢
- 資料來源：使用者提供的 `tcmsp_herb_details_merged_500.json`（500 種藥材，TCMSP 網站爬取彙整），經正規化去重後產出 `frontend/data/tcmsp_data.json`（9.1MB，13,728 個不重複成分、1,751 個不重複靶點、564 個不重複疾病）
- **架構調整**：原始樣板是把資料直接內嵌在 HTML 裡（`const DATA = __DATA_JSON__`），考量到 500 種藥材的完整資料量偏大，改為讓頁面用 `fetch()` 非同步載入外部 `data/tcmsp_data.json`，避免單一 HTML 檔案過於肥大、影響載入速度
- **登入整合**：頁面加入與平台共用的登入驗證（沿用 `js/api.js`），未登入會導回登入頁；頁首新增導覽列（返回 Dashboard／登出），並已加入所有頁面的側邊選單連結

### 已知限制

- 500 種藥材中，僅人參屬 5 種（人參、三七、西洋參、竹節參、紅參）有對照中文名稱，其餘 495 種暫以英文學名顯示，待後續人工翻譯補充
- 疾病資料的 ICD9/ICD10 欄位原始資料中沒有提供，目前顯示為空值
- 靶點的「彙總／補充」來源分類為簡化推斷（依資料出現的關聯類型判斷），非原始資料庫的精確分級標記
- 此頁面純前端運作（資料查詢、篩選、網絡圖皆在瀏覽器端處理），與後端資料庫無關，不支援後台管理（新增/編輯/刪除藥材資料）；若需要維護此資料集，需重新產生 `tcmsp_data.json` 並替換

## v1.3.0 — 2026-07-31（支援雲端 PostgreSQL，解決資料庫重置問題）

**狀態：已通過使用者實測驗證** —— 後端已接上 Neon 的免費 PostgreSQL（透過 Render 環境變數 `DATABASE_URL`），並實際測試「新增帳號 → 手動觸發 Render 重新部署 → 確認帳號仍存在」，證實資料不再因重新部署而消失，資料庫重置問題已解決。

### 新增功能

- `app/database.py` 改為讀取環境變數 `DATABASE_URL`：
  - 有設定時連接雲端 PostgreSQL（正式環境用）
  - 未設定時自動退回本機 SQLite 檔案（本機開發測試用，行為與之前版本相同）
  - 自動將 `postgres://` 開頭的連線字串轉換為 SQLAlchemy 2.0 要求的 `postgresql://`
- `requirements.txt` 新增 `psycopg2-binary`（PostgreSQL 驅動）
- `render.yaml` 新增 `DATABASE_URL` 環境變數欄位（`sync: false`，不寫入版本控制，需於 Render 後台手動填入）

### ⚠️ 重要提醒：Render 自帶的免費 PostgreSQL 也會過期，並非長久解法

原本以為「換成 PostgreSQL」就能徹底解決資料庫被重置的問題，但查證後發現：**Render 自己附的免費 PostgreSQL 資料庫本身只保留 30 天，到期後 14 天內沒升級為付費方案，資料庫連同所有資料會被整個刪除**。這跟原本 SQLite 在免費方案上「重新部署就重置」的問題性質類似，只是把期限從「每次重啟」延後到「30 天」，並非真正的永久解法。

**建議做法**：改接**永久免費**的第三方 PostgreSQL 服務（例如 Neon、Supabase 皆有不到期的免費額度），Render 後端只需要一組連線字串即可連接，資料庫實際放在哪個平台跟後端部署位置無關。這部分已經完成程式碼支援，下一步需要你選定要用哪家 Postgres 服務、建立資料庫後把連線字串填入 Render 的 `DATABASE_URL` 環境變數即可。

### 已知限制

- SQLAlchemy 的 Enum 型別在 PostgreSQL 會建立原生 ENUM 型別，未來若要異動欄位選項（例如新增角色狀態），需要額外處理資料庫遷移（migration），目前專案尚未導入 Alembic 等遷移工具
- Neon 免費方案本身也有額度限制（例如閒置一段時間會休眠、儲存空間上限），雖然不會像 Render 免費 Postgres 一樣「30 天到期就整個刪除」，但正式上線有大量使用者時仍需評估是否要升級付費方案

## v1.2.0 — 2026-07-31（前後端分離部署：Render + Cloudflare Pages）

**狀態：已通過使用者正式環境驗證** —— 前端 `https://fwc-tcmsp.pages.dev/`（Cloudflare Pages，GitHub 自動部署）+ 後端 `https://tcm-onco-backend.onrender.com`（Render，GitHub 自動部署，Blueprint 方式）已成功串接，使用 `admin`/`0000` 登入後台驗證通過，Dashboard 與左側選單（角色管理／帳號管理／帳號審核／中藥行管理／中藥行地理推薦／稽核登入紀錄）皆正常顯示。

- 後端已部署上線：`https://tcm-onco-backend.onrender.com`（Render Blueprint 部署）
- 前端 `js/api.js` 的 `API_BASE` 改為指向線上後端網址，不再依賴與後端同源
- CORS 設定從允許所有來源（`*`）收緊為僅允許：
  - `https://fwc-tcmsp.pages.dev`（Cloudflare Pages 正式網址）
  - `https://*.fwc-tcmsp.pages.dev`（Cloudflare Pages 預覽部署網址，正規表示式比對）
  - `http://localhost:8000` / `http://127.0.0.1:8000`（本機測試）
- ⚠️ 提醒：Render 免費方案為暫時性檔案系統，重新部署或服務休眠喚醒後 SQLite 資料會被重置（沿用 v1.1.1 的已知限制，尚未解決）

## v1.1.1 — 2026-07-31（新增 Render 部署設定）

- 新增 `render.yaml`，可透過 Render 的 Blueprint 功能一鍵部署後端 API
- README 新增「部署到 Render」「Cloudflare Pages 前端串接」兩個章節，說明完整部署流程
- ⚠️ 記錄已知限制：Render 免費方案檔案系統為暫時性，SQLite 資料庫會在重新部署/服務喚醒時被重置，正式上線前需改接雲端 PostgreSQL（已列入待辦）

## v1.1.0 — 2026-07-31（目標五：中藥行地理推薦，前台 + 後台）

### 新增功能

- **後台：中藥行管理**（`pharmacies.html`）：新增 / 編輯 / 刪除（軟刪除=下架）/ 查詢中藥行資料（名稱、地址、電話、營業時間、經緯度、簡介、備注）
- **後台：評價管理**：於中藥行管理頁可查看每間中藥行的所有評價、補充管理備注、刪除不當評價
- **前台：中藥行地理推薦**（`finder.html`）：
  - 使用瀏覽器定位（`navigator.geolocation`）取得使用者座標，以 Haversine 公式計算距離並排序（未授權定位時退回依名稱排序）
  - 顯示中藥行資訊、平均星等、所有評價
  - 登入使用者可對每間中藥行新增一則評價（1~5 星 + 文字），並可編輯/刪除自己的評價（每人每店限一則）
- 後端新增 API：`/pharmacies`（後台 CRUD）、`/pharmacies/{id}/reviews`、`/pharmacy-reviews/{id}`（後台評價管理）、`/public/pharmacies`、`/public/pharmacies/{id}`、`/public/pharmacies/{id}/reviews`、`/public/reviews/{id}`（前台）
- 新增功能代碼 F5-1（中藥行資料管理）、F5-2（中藥行地理推薦前台）、F5-3（評價管理），已登錄於權限矩陣的功能清單中
- 系統啟動時會建立 3 筆範例中藥行測試資料（僅供本機測試辨識畫面用）

### 已知限制

- 前台頁面尚未依「權限矩陣」動態隱藏/顯示選單（例如非管理者理論上也看得到「中藥行管理」連結，但後端 API 已用 `require_admin` 擋下，實際操作會被拒絕）——這是全站共通的前端限制，待後續統一處理
- 尚未整合實際地圖圖資（如 Google Maps/OpenStreetMap 嵌入），目前僅列表呈現 + 距離排序
- 經緯度需由後台人員手動輸入，尚未提供地址自動轉換座標（Geocoding）功能

## v1.0.1 — 2026-07-31（調整預設管理員測試帳號）

- 依需求將系統啟動時自動建立的預設管理員帳號密碼改為 `admin` / `0000`
- ⚠️ 提醒：`0000` 為極弱密碼，僅適合本機測試環境快速登入使用；正式環境上線前務必更換為高強度密碼，並建議移除「啟動時自動建立預設帳號」這段邏輯

## v1.0.0 — 2026-07-31（目標零：後台管理與系統共通功能，第一版上版）

**狀態：已通過使用者本機測試審查，正式列為 v1.0.0**

### 新增功能

- **Dashboard**：登入後可見的首頁，目前顯示「施工中」佔位內容（`dashboard.html` / `GET /dashboard`）
- **角色管理**（F0-2）：新增 / 編輯 / 刪除 / 查詢角色，並可查看角色底下已設定帳號（`roles.html`）
- **權限矩陣**：每個角色可針對每個功能模組個別設定「可見（can_view）」「可執行（can_execute）」（`roles.html` 權限矩陣視窗）
- **帳號管理**（F0-5）：新增 / 編輯 / 刪除（軟刪除，標記停用+原因）/ 查詢帳號，含角色指派、密碼重設（`users.html`）
- **帳號申請與審核**（F0-4）：使用者於登入頁提出申請 → 管理者於「帳號審核」頁核准／駁回，核准後自動啟用並指派預設角色（`applications.html`）
- **第三方登入綁定骨架**（F0-6）：Google / 小紅書 / WeChat 綁定關係 CRUD API（`/oauth-accounts`），尚未含前端頁面與正式 OAuth 流程
- **稽核紀錄**（F0-11）：系統重大操作自動寫入稽核紀錄，後台僅提供查詢與補充備注，不提供刪除／修改（`logs.html`）
- **登入紀錄**（F0-12）：登入/登出自動記錄 IP、裝置識別字串、登入與登出時間、停留秒數（`logs.html`）
- **資料庫備份紀錄骨架**（F0-10）：備份執行紀錄查詢、手動觸發紀錄、補充備注（`logs.html`），尚未串接實際自動備份程序

### 已知限制（詳見 README「重要設計說明與限制」）

- 登入紀錄的裝置識別為瀏覽器指紋，非真實網卡 MAC 位址
- 第三方登入僅完成綁定關係骨架，尚未串接真實 OAuth 授權流程
- 資料庫備份僅為紀錄骨架，尚未串接實際備份排程
- JWT 目前無黑名單機制，登出僅記錄登出時間，token 到期前理論上仍可使用

### 技術細節

- Backend：FastAPI + SQLAlchemy（SQLite，開發環境）+ JWT（python-jose）+ bcrypt 密碼雜湊
- Frontend：純 HTML/CSS/JS（無框架），透過 REST API 與後端溝通
- 已完成端對端測試：登入、Dashboard、角色 CRUD、權限矩陣設定、帳號申請/審核、登入/稽核紀錄查詢、備份紀錄觸發

### 下一步（尚未排入本版本）

- 目標一 / 二平台化、目標三 DNA 檢測系統、目標四癌症病患中藥建議、目標五中藥行地理推薦
- 第三方登入正式串接（待小紅書 / WeChat 開發者資格確認）
- 自動化測試套件、正式環境部署設定
